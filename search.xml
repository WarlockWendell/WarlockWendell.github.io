<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C++基础语法</title>
    <url>/2020/05/18/C++%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95/</url>
    <content><![CDATA[<p>学习Cpp也有一段时间了，但是还是不是很清晰一些基础的东西，所以记录一下我易忘的知识点</p>
<a id="more"></a>

<h1 id="1-Overview"><a href="#1-Overview" class="headerlink" title="1. Overview"></a>1. Overview</h1><ul>
<li><p><strong>operator is function</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"Hello"</span>&lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//结果例子，其实是把&lt;&lt;重载了，但是这里好像也不是这么个函数，本地我没有运行成功，这就是个例子而已</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span>.<span class="built_in">print</span>(<span class="string">"Hello"</span>);</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span>.<span class="built_in">print</span>(<span class="built_in">std</span>::<span class="built_in">endl</span>);</span><br></pre></td></tr></table></figure>
</li>
<li><p>.cpp才会被编译，头文件是不会被编译的，其被使用的方式是通过include Copy之后才会起作用的</p>
</li>
<li><p>VS中：Ctrl + F7编译单个文件</p>
</li>
<li><p>How C++ Works?</p>
<p>编写一个文件，然后编译这个文件，再运行。</p>
<p>如果是多个文件，比如一个文件中引用另一个文件中的函数时。在这个 文件中要先给一个声明，告诉Compiler这里有一个函数长这个样子，然后linker会去将各个函数的声明和定义联系起来。如果只有声明没有定义，就会有Linking Error</p>
</li>
<li><p>How Compiler Works?</p>
<p>将source file 变成 Object file</p>
<p>预处理 -&gt;  标记解释、解析-&gt;机器码</p>
<p>Cpp并不依赖于文件，文件对于Cpp也没有什么特别的意义，.cpp也只是约定俗成，你只需要告诉编译器按照C++的方式编译，可以编译其他后缀的文件，只要规则对。</p>
<p>Vs中预处理之后会得到.i文件，是预处理之后的文件，可以在项目属性中选择是否输出预处理之后的文件，但是就不编译产生.obj了</p>
<p>在编译时选择Debug模式，不会进行优化，运行起来也是比较慢的，但是方便Debug在release模式，会进行大量的优化，去除无用的代码，可以提升运行的速度，但是不利于Debug。</p>
</li>
<li><p>How  Linker Works?</p>
<p>将每个独立单元的.obj文件link起来</p>
<p>程序需要一个入口点，这个一般是main()，但是也可以在VS中修改项目属性，在链接器一栏中可以修改程序的入口点，这个了解一下即可。</p>
<p>一些常见的问题：</p>
<ul>
<li><p>如果在同一个文件中定义了一个函数两次（Cpp的函数相同指的是函数名，返回类型，参数表都相同），那么会得到一个Compiler Error，因为不能生成.obj文件。当在不同文件定义了一个函数两次，那么会得到一个Link Error，因为linker不知道从哪一个.obj中链接对应的函数。</p>
</li>
<li><p>上面提到的一个典型的例子就是，当你在头文件中写函数定义时，再在两个文件中inlcude了，那么这两个函数都会有这么一个函数定义，会得到linking error。解决的方式有</p>
<ul>
<li>将函数加上 static关键字，表示这个函数只在当前文件中期作用，对其他.obj不可见，这样就不会有重复定义的问题了</li>
<li>加上inline关键字，此时相当于将函数内容直接写到源程序中，这种相当于没有调用函数，当然也不会有linking error</li>
<li>将函数定义写到一个文件中，头文件中只放声明，这样include时只会复制声明，告诉编译器当前编译单元（文件）有这么一个函数，长这个样子，而不会去定义它</li>
</ul>
</li>
<li><p>当在一个编译单元中声明了一个函数而没有给出定义，编译这个单个文件是能通过的，生成整个项目时就会有linking error，因为没有给出函数定义。但是如果这个编译单元中没有使用这个函数，仅仅给出了声明，那么编译也是能通过的，因为没有linking这个函数的过程。如果是程序中的一个非入口函数调用了另外一个只有声明的函数呢？这样编译不可以通过，因为当前文件中虽然没有调用这个函数，不会有linking的问题，但是其他文件中就不一定不会调用它，所以还是有linking error（可以理解为只要显式地调用了这个函数，不管是在哪里，那就会有error，而如果只给声明没有显式调用，就可以），那么如何解决呢?只需要给函数加上static关键字，那么说明此函数只在本文件中使用，而本文件中没有使用，那么这个函数里面调用其他函数并不会有问题。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//原始版本，会报错，因为Log找不到（Log在其他文件中也没有定义)</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Log</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* msg)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Multi</span><span class="params">(<span class="keyword">int</span> a,<span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Log(<span class="string">"Multiply"</span>);</span><br><span class="line">    <span class="keyword">return</span> a * b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Multi(<span class="number">4</span>,<span class="number">5</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//修改版本，不报错，以内Log没有使用</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Log</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* msg)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Multi</span><span class="params">(<span class="keyword">int</span> a,<span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//Log("Multiply");</span></span><br><span class="line">    <span class="keyword">return</span> a * b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Multi(<span class="number">4</span>,<span class="number">5</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//会报错，虽然本文件没有使用Multi，更不会使用Log，但是Multi中显式出现了Log，其他文件嗲用Multi时会出问题</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Log</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* msg)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Multi</span><span class="params">(<span class="keyword">int</span> a,<span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Log(<span class="string">"Multiply"</span>);</span><br><span class="line">    <span class="keyword">return</span> a * b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//Multi(4,5);</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//加上static后，只在当前文件中Multi有效，当前文件不调用Multi，所以不调用Log，所以没问题。</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Log</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* msg)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">Multi</span><span class="params">(<span class="keyword">int</span> a,<span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Log(<span class="string">"Multiply"</span>);</span><br><span class="line">    <span class="keyword">return</span> a * b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//Multi(4,5);</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当外部需要引用其他文件中的非静态变量，而且不想重新定义的话，可以使用extern关键字，这是一个声明，而不带extern的是定义，定义只能有一个</p>
</li>
</ul>
</li>
</ul>
<h1 id="2-Previous"><a href="#2-Previous" class="headerlink" title="2.  Previous"></a>2.  Previous</h1><h2 id="2-1-data"><a href="#2-1-data" class="headerlink" title="2.1 data"></a>2.1 data</h2><ul>
<li><p>整形</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>;<span class="comment">//4</span></span><br><span class="line">short;<span class="comment">//2</span></span><br><span class="line"><span class="keyword">char</span>;<span class="comment">//1</span></span><br><span class="line"><span class="keyword">long</span>;<span class="comment">//4</span></span><br><span class="line"><span class="keyword">long</span> <span class="keyword">long</span>;<span class="comment">//4</span></span><br><span class="line"><span class="keyword">unsigned</span>;<span class="comment">//无符号数</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>浮点型</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">float</span>;<span class="comment">//2</span></span><br><span class="line"><span class="keyword">double</span>;<span class="comment">//4</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//默认是double，即使声明成float也是一个double</span></span><br><span class="line"><span class="keyword">float</span> a = <span class="number">3.4f</span>; <span class="comment">//加上f/F表示float，否则是double</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>bool</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">bool</span> a = <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">bool</span> b = <span class="literal">false</span>;</span><br><span class="line"><span class="comment">//占用一个字节，不是一位，因为最下处理单元是一个字节，而且其实质也是一个数据，而不是单词</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>sizeof</p>
<p>上面的数字表示字节大小，但是这取决于编译器，可以使用sizeof来查看，sizeof可以加括号，也可以不加括号。</p>
</li>
</ul>
<h2 id="2-2-function"><a href="#2-2-function" class="headerlink" title="2.2 function"></a>2.2 function</h2><p>这个和C中的函数基本是一样的，需要返回类型、名称、参数表</p>
<h2 id="2-3-Header-File"><a href="#2-3-Header-File" class="headerlink" title="2.3 Header File"></a>2.3 Header File</h2><ul>
<li><p><code>#pragma once</code> 表示这个文件只会在同一个文件中被include一次，并不代表在整个demo中只能include一次</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//head.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> once</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">playe</span>&#123;</span>&#125;;</span><br><span class="line"><span class="comment">//这样如果被include两次了，就会发生这个结构体的重定义，会有Compiler Error</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>todo: 为什么不同文件中包含同一个struct的定义不算重定义呢？</p>
</blockquote>
</li>
<li><p><code>&lt;&gt;</code>和<code>&quot;&quot;</code>，后者表示一个相对路径，前者用于标准库</p>
</li>
<li><p><code>ifndef</code> <code>define</code> <code>endif</code>预处理指令也可防止重定义</p>
</li>
</ul>
<h2 id="2-4-Debug"><a href="#2-4-Debug" class="headerlink" title="2.4 Debug"></a>2.4 Debug</h2><p>VS在调试时，可以使用 调试-&gt;窗口-&gt;内存，查看当前内存的内容，方便调制以及理解程序运行的过程。</p>
<p>在做这个时，可以发现，在Debug模式下，为了方便调试，编译器做了很多的优化，将栈的初始值都初始化为CC，然后在一些必要的地方加上提示，这些在release模式下并没有，所以debug模式下确实是要慢很多。</p>
<p>在调试模式下，还可以转到反汇编编码，这个对理解底层很有帮助。</p>
<h2 id="2-5-New-a-Project"><a href="#2-5-New-a-Project" class="headerlink" title="2.5 New a Project"></a>2.5 New a Project</h2><p>VS项目资源管理器中的一些类似文件夹的并不是文件夹，而是一个过滤器，是方便阅读和管理的，实际上并没有这个文件夹。</p>
<p>在项目属性中可以调整VS的输出目录和中间目录，这个很有用，可以方便管理文件</p>
<h1 id="3-syntax"><a href="#3-syntax" class="headerlink" title="3. syntax"></a>3. syntax</h1><h2 id="3-1-if-else"><a href="#3-1-if-else" class="headerlink" title="3.1 if else"></a>3.1 if else</h2><p>条件控制语句</p>
<h2 id="3-2-loop"><a href="#3-2-loop" class="headerlink" title="3.2 loop"></a>3.2 loop</h2><p>for</p>
<p>while</p>
<p>do while</p>
<p>break</p>
<p>continue</p>
<p>return</p>
<h2 id="3-3-Pointer"><a href="#3-3-Pointer" class="headerlink" title="3.3 Pointer"></a>3.3 Pointer</h2><p><strong>pointer is address, it’s just a integer</strong>，它就是一个地址，然后通过类型名表示这个指针里存放的地址的内容所占用的空间，指针占用的size在同一个机器里是一样的，比如32位机器就是4Byte，为什么要指定它的类型呢？因为这样给这个地址中内容存放一个内容时，会根据指针的类型来指定大小，这时候就容易出现问题了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span>* s = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">8</span>];</span><br><span class="line"><span class="keyword">delete</span>[] s;</span><br><span class="line"><span class="comment">//其实也就是底层调用了malloc和free，但是这里还是调用构造函数和析构函数，也是一个特性吧</span></span><br></pre></td></tr></table></figure>



<h2 id="3-4-Reference"><a href="#3-4-Reference" class="headerlink" title="3.4 Reference"></a>3.4 Reference</h2><p>Refernence不会新构建一个对象，而是相当于取了一个别名，reference一定要初始化，它一定是参考一个<strong>已经存在</strong>的变量，本身不占空间</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">9</span>;</span><br><span class="line"><span class="keyword">int</span>&amp; ref = a;</span><br><span class="line"><span class="comment">//相当于a和ref是一样的变量，a是大名，ref是小名</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> b = <span class="number">9</span>;</span><br><span class="line">ref = b;</span><br><span class="line"><span class="comment">//这句话不是将ref的参考关系改成b，ref一旦指定了对象就不能更改了，这句话相当于将a = 9;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//int&amp; 这就是相当于一个类型，ref是它的名字，所以使用的时候直接用ref就好了</span></span><br></pre></td></tr></table></figure>

<p>看起来reference比pointer更加简便，为什么还要用指针？</p>
<p>因为指针的功能更强大，比如指针可以更改指向</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">8</span>;</span><br><span class="line"><span class="keyword">int</span> b = <span class="number">9</span>;</span><br><span class="line"><span class="keyword">int</span>* ptr = &amp;a;</span><br><span class="line">*ptr += <span class="number">2</span>;</span><br><span class="line">ptr = &amp;b;</span><br><span class="line">(*ptr)++;</span><br><span class="line"><span class="comment">//上面这段代码就是分别对a和b进行操作</span></span><br></pre></td></tr></table></figure>

<h2 id="3-5-Class"><a href="#3-5-Class" class="headerlink" title="3.5 Class"></a>3.5 Class</h2><ul>
<li><p>default ： private</p>
</li>
<li><p>封装</p>
</li>
<li><p>struct 和 class</p>
<blockquote>
<p>struct和class其实没有太大的区别</p>
<p>唯一的区别是，class默认是私有的，struct默认是公有的，struct里也可以加上private关键字，里面也可以放方法（函数）</p>
<p>c++保留struct仅仅是为了兼容C的语法，因为C没有class，只有struct</p>
</blockquote>
</li>
<li><p>static  for classes and structs in cpp</p>
<p>static的变量在这个类的所有实例中是共享的，也就是改变其中一个实例的这个变量的内容，所有实例中的这个变量的内容就会发生改变</p>
<p>static的函数（方法）可以被直接调用without instance，不依赖于实例</p>
<p>同样static的变量也不依赖于实例</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">int</span> x, y;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; x &lt;&lt; <span class="string">","</span> &lt;&lt; y &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> Entity::x;</span><br><span class="line"><span class="keyword">int</span> Entity::y;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Entity e;</span><br><span class="line">	e.x = <span class="number">2</span>;   <span class="comment">//相当于Enitiy::x  = 2; 下面的都是同理的</span></span><br><span class="line">	e.y = <span class="number">3</span>;</span><br><span class="line">	Entity e1;</span><br><span class="line">	e1.x = <span class="number">4</span>;</span><br><span class="line">	e1.y = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	e.Print();</span><br><span class="line">	e1.Print();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：静态方法不能使用静态的变量。因为非静态的变量是依赖于实例的，而静态的方法不依赖于实例，所以不能正确的运行。</p>
</blockquote>
</li>
</ul>
<h2 id="3-6-enum"><a href="#3-6-enum" class="headerlink" title="3.6 enum"></a>3.6 enum</h2><ul>
<li><p>enum可以指定类型的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Level:short</span><br><span class="line">&#123;</span><br><span class="line">    Error = <span class="number">0</span>,Warning,Info</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在类里的enum属于这个类的命名空间</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Log</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">enum</span> Level : short&#123;Error,Waring,Info&#125;;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Level A = Log::Error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="3-7-构造函数"><a href="#3-7-构造函数" class="headerlink" title="3.7 构造函数"></a>3.7 构造函数</h2><p>一个特殊的方法，每次例化一个类都要运行一次，自动运行的</p>
<p>即使没有定义构造函数，还是有一个default的构造函数的，虽然它并不干活</p>
<p>如果不希望构造实例怎么办？</p>
<ul>
<li><p>将构造函数隐藏在private中</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	A()&#123;&#125;</span><br><span class="line">   	<span class="keyword">public</span>:</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



</li>
</ul>
<ul>
<li><p>构造函数  =  delete；也可以</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	A() = <span class="keyword">delete</span>;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">()</span></span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>c++有默认的六个构造和析构函数</p>
<ul>
<li>缺省构造函数 A()</li>
<li>析构函数 ~A()</li>
<li>拷贝构造函数 A(const A&amp; ref)</li>
<li>拷贝赋值算子 A&amp; operator = (const A &amp; ref)</li>
<li>移动构造函数 A(const A &amp;&amp; ref)</li>
<li>移动赋值算子 A &amp; operator=(A &amp;&amp; ref)</li>
</ul>
<p>使用 = delete删除，使用 = default 保持默认</p>
<h2 id="3-8-析构函数"><a href="#3-8-析构函数" class="headerlink" title="3.8 析构函数"></a>3.8 析构函数</h2><p>在对象被销毁的时候，自动执行析构函数</p>
<p>用来释放内存</p>
<p>在堆上手工分配的内存要释放</p>
<p>如果手动调用析构函数，在被销毁时也还是会被调用一次，这样容易出错</p>
<h2 id="3-9-继承"><a href="#3-9-继承" class="headerlink" title="3.9 继承"></a>3.9 继承</h2><p>派生类继承了基类的所有元素</p>
<h2 id="3-10-虚函数"><a href="#3-10-虚函数" class="headerlink" title="3.10 虚函数"></a>3.10 虚函数</h2><p>虚函数允许覆盖基类中的方法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">GetName</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="string">"Entity"</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Player</span> :</span> <span class="keyword">public</span> Entity</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">string</span> m_Name;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Player(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span>&amp; name) :m_Name(name) &#123;&#125;</span><br><span class="line">	<span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">GetName</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> m_Name; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Entity* e = <span class="keyword">new</span> Entity();</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; e-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	Player* player = <span class="keyword">new</span> Player(<span class="string">"Bob"</span>);</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; player-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	Entity* p = player;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; p-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这种情况下，输出的是</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Entity</span><br><span class="line">Bob</span><br><span class="line">Entity</span><br></pre></td></tr></table></figure>

<p>将道理我们第三次传入的是player，也就是一个Player型的，应该输出的是playe.Name，也就是“Bob”，然而并没有</p>
<p>因为传入的是一个Entity*，所以调用的是Entity中的方法，而不是派生类中的方法</p>
<p>此时就需要引入虚函数，允许覆盖基类中的方法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">GetName</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="string">"Entity"</span>; &#125; <span class="comment">//加上virtual</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Player</span> :</span> <span class="keyword">public</span> Entity</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">string</span> m_Name;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Player(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span>&amp; name) :m_Name(name) &#123;&#125;</span><br><span class="line">	<span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">GetName</span><span class="params">()</span> <span class="keyword">override</span></span>&#123; <span class="keyword">return</span> m_Name; &#125; <span class="comment">//override，不是必要的，起标识作用，但是位置不要错</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Entity* e = <span class="keyword">new</span> Entity();</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; e-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	Player* player = <span class="keyword">new</span> Player(<span class="string">"Bob"</span>);</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; player-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	Entity* p = player;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; p-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里仅仅是加入了一个virtual，就让函数正确运行了</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Entity</span><br><span class="line">Bob</span><br><span class="line">Bob</span><br></pre></td></tr></table></figure>

<p>其实，加上virtual后，这时编译器会为它建一张Vtable，这个会牺牲时间空间效率，但是可以正确的干活</p>
<h2 id="3-11-纯虚函数"><a href="#3-11-纯虚函数" class="headerlink" title="3.11 纯虚函数"></a>3.11 纯虚函数</h2><p>就像是一个接口</p>
<p>仅有接口，没有实现</p>
<p>所以也不能将这个类实例化</p>
<p>只能在派生类中将这些接口实现后才能实例化</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">GetName</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Player</span> :</span> <span class="keyword">public</span> Entity</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">string</span> m_Name;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Player(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span>&amp; name) :m_Name(name) &#123;&#125;</span><br><span class="line">	<span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">GetName</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123; <span class="keyword">return</span> m_Name; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="comment">//Entity* e = new Entity();</span></span><br><span class="line">	<span class="comment">//std::cout &lt;&lt; e-&gt;GetName() &lt;&lt; std::endl;</span></span><br><span class="line">	Player* player = <span class="keyword">new</span> Player(<span class="string">"Bob"</span>);</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; player-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	Entity* p = player;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; p-&gt;GetName() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码中，如果Player Class中没有实现GetName()，那么这个类也不能实例化</p>
<h2 id="3-12-可见性"><a href="#3-12-可见性" class="headerlink" title="3.12 可见性"></a>3.12 可见性</h2><ul>
<li><p>private: 仅供这个类使用，派生类和其他地方都不可以使用，当然，friend可以使用、</p>
<p>这个一般是只能在内部修改的，外部的类都不可以修改这个</p>
</li>
<li><p>protected:：供这个类和它的派生类使用，类之外不可以使用</p>
</li>
<li><p>public: 任何地方都可以使用</p>
<p>这个供使用者使用</p>
</li>
</ul>
<h2 id="3-13-Arrays-in-C"><a href="#3-13-Arrays-in-C" class="headerlink" title="3.13 Arrays in C++"></a>3.13 Arrays in C++</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a[<span class="number">5</span>];</span><br><span class="line"><span class="keyword">int</span> *b = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">5</span>];</span><br><span class="line"><span class="keyword">int</span>* b = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">5</span>]&#123;<span class="number">1</span>,<span class="number">2</span>&#125;; <span class="comment">//带初始化</span></span><br><span class="line"><span class="keyword">delete</span>[] b;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">array</span>&lt;<span class="keyword">int</span>,5&gt; c;</span><br></pre></td></tr></table></figure>

<p>使用下面的表达式可以获得数组的长度，使用指针形式定义的貌似不可以使用这种方法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">sizeof</span>(<span class="built_in">array</span>) / <span class="keyword">sizeof</span>(ElemType)</span><br></pre></td></tr></table></figure>

<p>对于字符型的指针，使用std::cout输出时会自动查找下一个字符，最后输出的是一个字符串，要想输出它的地址。可以强制将这个指针转换成（void*)（Int<em>)等类型，这样可以输出地址。使用\</em>s的形式输出单个字符。</p>
<p>有关std::array</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">size</span>(); </span><br><span class="line">max_size(); <span class="comment">//长度</span></span><br><span class="line">data(); <span class="comment">//返回C style的数组</span></span><br><span class="line">at(); <span class="comment">//安全的[]接口，越界时会报错</span></span><br><span class="line">enpty(); <span class="comment">//检测N值是否是0，当std::array&lt;int,0&gt;才是true，没有赋值但是分配了空间也是false</span></span><br></pre></td></tr></table></figure>

<p>需要明确的是，数组的大小是静态的，不能修改</p>
<h2 id="3-15-Strings-in-C"><a href="#3-15-Strings-in-C" class="headerlink" title="3.15 Strings in C++"></a>3.15 Strings in C++</h2><p>需要注意的是，字面值常量是无法直接相加的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="string">"Hello"</span> + <span class="string">"World"</span>; <span class="comment">//错误</span></span><br><span class="line"><span class="comment">//要保证加号两侧至少一个string对象</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="built_in">std</span>::<span class="built_in">string</span>(<span class="string">"Hello"</span>) + <span class="string">"World"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//也可以这么用</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::string_literals;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="string">"Hello"</span>s + <span class="string">"World"</span>; <span class="comment">// s将一个const char * 转换成了string object</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span>* s = <span class="string">"Hello"</span>;</span><br><span class="line">s[<span class="number">2</span>] = <span class="string">'a'</span>;</span><br><span class="line"><span class="comment">//此时修改的是s的拷贝，而非s本身，当然新标准已经不允许这么定义字符串了</span></span><br></pre></td></tr></table></figure>

<h2 id="3-16-Const-in-C"><a href="#3-16-Const-in-C" class="headerlink" title="3.16 Const in C++"></a>3.16 Const in C++</h2><ul>
<li><p>定义一个不希望被修改的变量</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> MAX = <span class="number">90</span>;</span><br><span class="line"><span class="keyword">int</span> *a = <span class="keyword">new</span> <span class="keyword">int</span>;</span><br><span class="line">a = (<span class="keyword">int</span>*)&amp;MAX;</span><br></pre></td></tr></table></figure>
</li>
<li><p>指针</p>
<p>上面的代码中，a既可以改变内容，也可以改变指向</p>
<p>加上const</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>* a = <span class="keyword">new</span> <span class="keyword">int</span>;</span><br><span class="line"><span class="keyword">int</span> <span class="keyword">const</span>* a = <span class="keyword">new</span> <span class="keyword">int</span>;</span><br><span class="line">*a = <span class="number">2</span>; <span class="comment">//错误</span></span><br></pre></td></tr></table></figure>

<p>不能修改a指向地址的内容了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>* <span class="keyword">const</span> a = <span class="keyword">new</span> <span class="keyword">int</span>;</span><br><span class="line">a = &amp;b; <span class="comment">//错误</span></span><br></pre></td></tr></table></figure>

<p>不能修改a的指向</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>* <span class="keyword">const</span> a = <span class="keyword">new</span> <span class="keyword">int</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>class中的const</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> m_x, m_y;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">int</span> <span class="title">GetX</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> m_x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>在方法上加上const表示这个方法不允许修改类的成员</p>
<p>为什么要这么用？</p>
<p>因为在有些时候，我们传一个对象给函数时，不希望修改里面的内容，所以使用引用+const</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">(<span class="keyword">const</span> Entity&amp; e)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;e.GetX()&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个时候要保证函数里调用的方法不会修改成员，所以GetX()需要限定成const类型的</p>
<p>如果还是希望有一些变量可以修改怎么办？</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> m_x, m_y;</span><br><span class="line">    <span class="keyword">mutable</span> <span class="keyword">int</span> c;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">int</span> <span class="title">GetX</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        c = <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">return</span> m_x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>使用mutable可以使变量在const方法中被修改</p>
</li>
</ul>
<h2 id="3-17-Mutable-in-C"><a href="#3-17-Mutable-in-C" class="headerlink" title="3.17 Mutable in C++"></a>3.17 Mutable in C++</h2><ul>
<li><p>一种是上面提到的和const一起使用</p>
</li>
<li><p>在lambda表达式中使用</p>
</li>
</ul>
<h2 id="3-18-构造函数初始化列表"><a href="#3-18-构造函数初始化列表" class="headerlink" title="3.18  构造函数初始化列表"></a>3.18  构造函数初始化列表</h2><p>这个可以参照另一篇文章，我的另外一篇文章有详细笔记</p>
<h2 id="3-19-在C-中创造对象"><a href="#3-19-在C-中创造对象" class="headerlink" title="3.19 在C++中创造对象"></a>3.19 在C++中创造对象</h2><p>主要是分为在堆上和在栈上</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Entity* e;</span><br><span class="line">    &#123;</span><br><span class="line">        Entity entity;</span><br><span class="line">        e = &amp;entity;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在里层括号跳出后，就会发生错误，因为e的引用是分配在栈上的跳出后就被pop了</p>
<p>使用在堆上分配的方式可以</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Entity * e;</span><br><span class="line">    &#123;</span><br><span class="line">        Entity* entity = <span class="keyword">new</span> Entity();</span><br><span class="line">        e = entity;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">delete</span> e;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用堆分配的内存需要手动释放</p>
<p>new和delete在调用时不仅会调用malloc和free，还会调用构造函数和析构函数</p>
<p>new可以指定分配的地点</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>* b = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">100</span>];</span><br><span class="line">Entity *e = <span class="keyword">new</span>(b) Entity();</span><br></pre></td></tr></table></figure>

<p>就是在b的位置分配内存</p>
<h2 id="3-20-隐式转换"><a href="#3-20-隐式转换" class="headerlink" title="3.20 隐式转换"></a>3.20 隐式转换</h2><p>C++编译器会帮你完成一些类型转换的工作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">string</span> m_Name;</span><br><span class="line">	<span class="keyword">int</span> m_Age;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Entity(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span>&amp; name) :m_Name(name), m_Age(<span class="number">-1</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">	Entity(<span class="keyword">int</span> age) :m_Name(<span class="string">"Unkonow"</span>), m_Age(age) &#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::string_literals;</span><br><span class="line">	Entity a = <span class="number">20</span>;</span><br><span class="line">	Entity b = <span class="string">"Hello"</span>s;<span class="comment">//Entity b("Hello");可以</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的例子是可行的</p>
<p>使用explicit就不可以这么用了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">string</span> m_Name;</span><br><span class="line">	<span class="keyword">int</span> m_Age;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	explicit Entity(const std::string&amp; name) :m_Name(name), m_Age(-1) &#123;&#125;</span><br><span class="line"></span><br><span class="line">	explicit Entity(int age) :m_Name("Unkonow"), m_Age(age) &#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="3-21-操作符重载-Overloading"><a href="#3-21-操作符重载-Overloading" class="headerlink" title="3.21 操作符重载 Overloading"></a>3.21 操作符重载 Overloading</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Vector2</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">float</span> x, y;</span><br><span class="line">	Vector2(<span class="keyword">float</span> xa, <span class="keyword">float</span> ya) :x(xa), y(ya) &#123;&#125;</span><br><span class="line"></span><br><span class="line">	Vector2 <span class="keyword">operator</span>+ (<span class="keyword">const</span> Vector2&amp; other) <span class="keyword">const</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">return</span> Vector2(<span class="keyword">this</span>-&gt;x + other.x, <span class="keyword">this</span>-&gt;y + other.y);</span><br><span class="line">	&#125;</span><br><span class="line">	Vector2 <span class="keyword">operator</span>* (<span class="keyword">const</span> Vector2&amp; other)<span class="keyword">const</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">return</span> Vector2(<span class="keyword">this</span>-&gt;x * other.x, <span class="keyword">this</span>-&gt;y * other.y);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::ostream&amp; <span class="keyword">operator</span>&lt;&lt; (<span class="built_in">std</span>::ostream&amp; stream, <span class="keyword">const</span> Vector2&amp; vec) </span><br><span class="line">&#123;</span><br><span class="line">	stream &lt;&lt; vec.x &lt;&lt; <span class="string">","</span> &lt;&lt; vec.y;</span><br><span class="line">	<span class="keyword">return</span> stream;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">Vector2 <span class="title">pos</span><span class="params">(<span class="number">1.0f</span>, <span class="number">2.0f</span>)</span></span>;</span><br><span class="line">	<span class="function">Vector2 <span class="title">speed</span><span class="params">(<span class="number">2.0f</span>, <span class="number">4.0f</span>)</span></span>;</span><br><span class="line">	<span class="function">Vector2 <span class="title">power</span><span class="params">(<span class="number">3.0f</span>, <span class="number">4.0f</span>)</span></span>;</span><br><span class="line">	Vector2 result = pos + speed * power;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; result &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里还做了一个&lt;&lt;的重载，需要注意一下</p>
<h2 id="3-22-this"><a href="#3-22-this" class="headerlink" title="3.22 this"></a>3.22 this</h2><p>this只能由成员访问</p>
<p>代表一个指向实例的指针</p>
<h2 id="3-23-对象的lifetime"><a href="#3-23-对象的lifetime" class="headerlink" title="3.23 对象的lifetime"></a>3.23 对象的lifetime</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Entity()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Create"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	~Entity()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Destroyed"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ScopePtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	Entity* m_e;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	ScopePtr(Entity* e):m_e(e)</span><br><span class="line">	&#123;</span><br><span class="line">	&#125;</span><br><span class="line">	~ScopePtr()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">delete</span> m_e;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="function">ScopePtr <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> Entity())</span></span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>虽然是用了new，没有手工释放，但是使用这个指针，也实现了自动释放</p>
<h2 id="3-24-智能指针"><a href="#3-24-智能指针" class="headerlink" title="3.24 智能指针"></a>3.24 智能指针</h2><p>不用调用new和delete，自动实现内存分配和释放</p>
<ul>
<li><p>unique</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Entity()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Create"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	~Entity()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Destroyed"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="function"><span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;Entity&gt; <span class="title">entity</span><span class="params">(<span class="keyword">new</span> Entity())</span></span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>也可以这么写</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;Entity&gt; entity = <span class="built_in">std</span>::make_unique&lt;Entity&gt;();</span><br></pre></td></tr></table></figure>

<p>这个类没有拷贝构造函数，所以没有以下的用法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;Entity&gt; e0 = entity;</span><br></pre></td></tr></table></figure>
</li>
<li><p>shared_ptr</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Entity&gt; entity = <span class="built_in">std</span>::make_shared&lt;Entity&gt;();</span><br></pre></td></tr></table></figure>

<p>这个能为一个对象的指针指定很多reference</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Entity&gt; e0 = entity;</span><br></pre></td></tr></table></figure>

<p>并且，会有一个reference count的量，当所有的shared_ptr都消失时，这个对象的析构函数才会被调用</p>
</li>
<li><p>weak_ptr</p>
<p>这个就是不会增加reference count的值</p>
</li>
</ul>
<h2 id="3-25-Copy-constructor"><a href="#3-25-Copy-constructor" class="headerlink" title="3.25 Copy constructor"></a>3.25 Copy constructor</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">9</span>;</span><br><span class="line"><span class="keyword">int</span> b = a;</span><br><span class="line">b = <span class="number">10</span>;</span><br></pre></td></tr></table></figure>

<p>这里b是a的一份拷贝值，修改b的值不影响a的值</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Vector* a = <span class="keyword">new</span> Vector2();</span><br><span class="line">Vector* b = a;</span><br></pre></td></tr></table></figure>

<p>这里其实也是一个拷贝值，只不过拷贝的是地址</p>
<p>当一个类的成员有指针时，我们拷贝一个类，是浅拷贝，实际上变量都拷贝了一份，指针也被拷贝了一份，但是，拷贝之后里面存的地址是相同的，所以，一个释放了之后，另一个就没地址可以释放了，这样就会错误，比如下面的代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">char</span>* s;</span><br><span class="line">    <span class="keyword">int</span> len;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    A(<span class="keyword">char</span>* sa, <span class="keyword">int</span> lena)</span><br><span class="line">    &#123;</span><br><span class="line">        len = <span class="built_in">strlen</span>(sa);</span><br><span class="line">        sa = <span class="keyword">new</span> <span class="keyword">char</span>[len+<span class="number">1</span>];</span><br><span class="line">        <span class="built_in">memcpy</span>(s,sa,len+<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    A(<span class="keyword">const</span> A&amp; a):s(a.s),len(a.len)</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这里需要使用深度拷贝，因为有指针变量</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">char</span>* s;</span><br><span class="line">    <span class="keyword">int</span> len;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    A(<span class="keyword">char</span>* sa, <span class="keyword">int</span> lena)</span><br><span class="line">    &#123;</span><br><span class="line">        len = <span class="built_in">strlen</span>(sa);</span><br><span class="line">        sa = <span class="keyword">new</span> <span class="keyword">char</span>[len+<span class="number">1</span>];</span><br><span class="line">        <span class="built_in">memcpy</span>(s,sa,len+<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    A(<span class="keyword">const</span> A&amp; a):len(a.len)</span><br><span class="line">    &#123;</span><br><span class="line">        a.s = <span class="keyword">new</span> <span class="keyword">char</span>[a.len+<span class="number">1</span>];</span><br><span class="line">        <span class="built_in">memcpy</span>(s,a.s,len+<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>注意，当传值时候，其实都有这么一个过程发生了，所以更多的时候，使用引用传值</p>
<h2 id="3-26-Arrow-Operators"><a href="#3-26-Arrow-Operators" class="headerlink" title="3.26 Arrow Operators"></a>3.26 Arrow Operators</h2><p>首先来一个重载</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entity</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">int</span> x, y;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Entity() &#123;  &#125;;</span><br><span class="line">	~Entity() &#123; &#125;;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123;&#125; <span class="comment">//由于是const，所以const调用的方法也要是const</span></span><br><span class="line">&#125;; </span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ScopePtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	Entity* m_e;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	ScopePtr(Entity* e):m_e(e)</span><br><span class="line">	&#123;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	~ScopePtr()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">delete</span> m_e;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function">Entity* <span class="title">GetObject</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> m_e;</span><br><span class="line">	&#125;</span><br><span class="line">	Entity*&amp; <span class="keyword">operator</span>-&gt;()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">return</span> m_e;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">const</span> Entity* <span class="keyword">operator</span>-&gt;() <span class="keyword">const</span> <span class="comment">//由于不能仅凭返回值来重载，所以这里给输入也限定成const</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">return</span> m_e;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">ScopePtr <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> Entity())</span></span>;</span><br><span class="line">	ptr.GetObject()-&gt;Print();</span><br><span class="line">	<span class="comment">//这里这么调用实在是麻烦，是否可以使用-&gt;这种访问方式直接访问Object里的方法？</span></span><br><span class="line">	ptr-&gt;Print();</span><br><span class="line">	<span class="function"><span class="keyword">const</span> ScopePtr <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> Entity())</span></span>; <span class="comment">//这是一个const类型的，所以相应的操作符还需要重载一次</span></span><br><span class="line">	ptr2-&gt;Print();</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>还可用来查看变量存储的结构</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Vector3</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">float</span> x, y, z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> offset = &amp;((Vector3*)<span class="literal">nullptr</span>)-&gt;x;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; offset &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码可以表示x在struct中存储的offset</p>
<h2 id="3-27-Dynamic-Arrays-Vectors"><a href="#3-27-Dynamic-Arrays-Vectors" class="headerlink" title="3.27 Dynamic Arrays : Vectors"></a>3.27 Dynamic Arrays : Vectors</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Vectex</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">float</span> x, y, z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::ostream&amp; <span class="keyword">operator</span>&lt;&lt;(<span class="built_in">std</span>::ostream&amp; stream,  <span class="keyword">const</span> Vectex&amp; e)</span><br><span class="line">&#123;</span><br><span class="line">	stream &lt;&lt; e.x &lt;&lt; <span class="string">", "</span> &lt;&lt; e.y &lt;&lt; <span class="string">", "</span> &lt;&lt; e.z;</span><br><span class="line">	<span class="keyword">return</span> stream;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Fun</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Vectex&gt;&amp; vec)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Vectex&gt; vectices;</span><br><span class="line">	vectices.push_back(&#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span> &#125;);</span><br><span class="line">	vectices.push_back(&#123; <span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span> &#125;);</span><br><span class="line">	Fun(vectices);</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; vectices.<span class="built_in">size</span>(); i++)</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; vectices[i] &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (Vectex&amp; v : vectices) <span class="comment">//这种写法倒是没有见过</span></span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; v &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">	vectices.erase(vectices.<span class="built_in">begin</span>() + <span class="number">1</span>);</span><br><span class="line">	vectices.<span class="built_in">clear</span>();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面再来看一个例子</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Vectex</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">float</span> x, y, z;</span><br><span class="line">	Vectex(<span class="keyword">float</span> xa, <span class="keyword">float</span> ya, <span class="keyword">float</span> za) :x(xa), y(ya), z(za)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Create!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	Vectex(<span class="keyword">const</span> Vectex&amp; e):x(e.x),y(e.y),z(e.z)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Copy!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::ostream&amp; <span class="keyword">operator</span>&lt;&lt;(<span class="built_in">std</span>::ostream&amp; stream,  <span class="keyword">const</span> Vectex&amp; e)</span><br><span class="line">&#123;</span><br><span class="line">	stream &lt;&lt; e.x &lt;&lt; <span class="string">", "</span> &lt;&lt; e.y &lt;&lt; <span class="string">", "</span> &lt;&lt; e.z;</span><br><span class="line">	<span class="keyword">return</span> stream;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Fun</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Vectex&gt;&amp; vec)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Vectex&gt; vectices;</span><br><span class="line">	vectices.push_back(&#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span> &#125;);</span><br><span class="line">	vectices.push_back(&#123; <span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span> &#125;);</span><br><span class="line">	vectices.push_back(&#123; <span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span> &#125;);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果是</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Create!</span><br><span class="line">Copy!</span><br><span class="line">Create!</span><br><span class="line">Copy!</span><br><span class="line">Copy!</span><br><span class="line">Create!</span><br><span class="line">Copy!</span><br><span class="line">Copy!</span><br><span class="line">Copy!</span><br></pre></td></tr></table></figure>

<p>Why? 首先不用怀疑的是，每一次push_back，都是在main（）的栈中构建一个Vectex，然后这个要想到入到Vector中，就得被copy一次，所以有了一次Create和一次Copy，但是第二次的时候，仍然是要Create一次，Vector需要将原先的都拷贝到另外一个地址中，来得到更多的空间，所以这里会发生两次Copy，同理第三次</p>
<p>如何避免这种需要分配？</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Vectex&gt; vectices;</span><br><span class="line">	vectices.reserve(<span class="number">3</span>);</span><br><span class="line">	vectices.push_back(&#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span> &#125;);</span><br><span class="line">	vectices.push_back(&#123; <span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span> &#125;);</span><br><span class="line">	vectices.push_back(&#123; <span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span> &#125;);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Create!</span><br><span class="line">Copy!</span><br><span class="line">Create!</span><br><span class="line">Copy!</span><br><span class="line">Create!</span><br><span class="line">Copy!</span><br></pre></td></tr></table></figure>

<p>使用reserve时，可以指定分配的内存空间的大小，可以看到，每次都只拷贝了当前的，没有全员拷贝、</p>
<p>那我连一次都不想被拷贝咋办？</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Vectex</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">float</span> x, y, z;</span><br><span class="line">	Vectex(<span class="keyword">float</span> xa, <span class="keyword">float</span> ya, <span class="keyword">float</span> za) :x(xa), y(ya), z(za)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Create!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	Vectex(<span class="keyword">const</span> Vectex&amp; e):x(e.x),y(e.y),z(e.z)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Copy!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	Vectex(<span class="keyword">float</span> xa) :x(xa), y(<span class="number">1</span>), z(<span class="number">3</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Create2!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::ostream&amp; <span class="keyword">operator</span>&lt;&lt;(<span class="built_in">std</span>::ostream&amp; stream,  <span class="keyword">const</span> Vectex&amp; e)</span><br><span class="line">&#123;</span><br><span class="line">	stream &lt;&lt; e.x &lt;&lt; <span class="string">", "</span> &lt;&lt; e.y &lt;&lt; <span class="string">", "</span> &lt;&lt; e.z;</span><br><span class="line">	<span class="keyword">return</span> stream;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Fun</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Vectex&gt;&amp; vec)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Vectex&gt; vectices;</span><br><span class="line">	vectices.reserve(<span class="number">3</span>);</span><br><span class="line">	vectices.emplace_back( <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span> );</span><br><span class="line">	vectices.emplace_back(<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span> );</span><br><span class="line">	vectices.emplace_back(<span class="number">7</span>);</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Create!</span><br><span class="line">Create!</span><br><span class="line">Create2!</span><br></pre></td></tr></table></figure>

<p>使用emplace_back可以做到，这里是传了一个参数表给它，然后它再调用构造函数，这样复制的就不是这个对象了</p>
<h2 id="3-28-Library"><a href="#3-28-Library" class="headerlink" title="3.28 Library"></a>3.28 Library</h2><ul>
<li><p>static linking: 将库导入到你的可执行程序中</p>
<p>其实就是将下载的文件包含过来，VS在项目属性中-&gt;C/C++-&gt;常规-&gt;附加包含目录里可以添加文件目录，这里一般会使用到一些宏，比如${SolutionDir}表示当前解决方案的目录，添加的路径是头文件路径</p>
<p>然后要使用时，只需要在文件中inlcude相应的库文件就好了</p>
<p>但是这仍然没有提供相关库的定义文件，只有头文件，还需要在项目属性-&gt;连接器-&gt;常规中添加库目录，这个和前面添加头文件是类似的方法，还需要在输入的添加依赖项中添加相应的依赖,就是lib的文件名，这个才是依赖文件，上面添加的是路径</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> <span class="function"><span class="keyword">int</span> <span class="title">glfwInit</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<p>如果是C的库，需要这么引用函数，当然用头文件就不用这么做了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;GLFW/glfw3.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = glfwInit();</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;a&lt;&lt;<span class="built_in">std</span>:;<span class="built_in">endl</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>dynamic linking: 在运行时链接，dll文件</p>
<p>代码方面基本一样，需要将静态库的依赖改成动态库依赖，然后将dll文件放到exe文件目录下面，就可以使用了。</p>
</li>
<li><p>静态链接可以单独运行一个exe，然而动态链接需要相关的依赖项</p>
</li>
</ul>
<h2 id="3-29-VS多项目"><a href="#3-29-VS多项目" class="headerlink" title="3.29 VS多项目"></a>3.29 VS多项目</h2><p>造自己的库文件…</p>
<p>在VS的一个项目右击，添加新项目就可以，然后可以在项目属性-&gt;常规-&gt;选择配置类型。点击生成的时候，生成的就不是exe了，而是你选择的库类型文件。</p>
<p>然后也就和上面一节中类似的使用方法了，当然，由于两个项目在同一个解决方案中，所以直接点击项目-&gt;添加-&gt;引用，也可以添加。而且后者可以避免忘记修改库之后忘记编译的问题，因为编译器会帮你做，前者需要手动编译。</p>
<h2 id="3-30-如何返回多个值"><a href="#3-30-如何返回多个值" class="headerlink" title="3.30 如何返回多个值"></a>3.30 如何返回多个值</h2><ul>
<li><p>可以使用引用传参，传入需要返回的值即可，这种方法很高效，当然也可以传指针</p>
</li>
<li><p>使用array的方式返回</p>
</li>
<li><p>上面两种方式返回的都是同一种类型，返回不同类型时，可以使用tuple</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;tuple&gt;</span></span></span><br><span class="line">std::tuple&lt;std::string,std::string,int&gt; fun()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">std</span>::tuple&lt;<span class="built_in">std</span>::<span class="built_in">string</span>,<span class="built_in">std</span>::<span class="built_in">string</span>,<span class="keyword">int</span>&gt; a;</span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>当然，一个比较简单，可读的解决方式是使用struct</p>
</li>
</ul>
<h2 id="3-31-模板"><a href="#3-31-模板" class="headerlink" title="3.31 模板"></a>3.31 模板</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">(T value)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;value&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Print(<span class="number">5</span>);</span><br><span class="line">    Print&lt;<span class="keyword">float</span>&gt;(<span class="number">5.5f</span>); <span class="comment">//尖括号可以省去，隐式的转换</span></span><br><span class="line">    Print(<span class="string">"Hello"</span>); <span class="comment">//为什么这里可以直接转换成std::string啊</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>也可以使用class代替typename</p>
<p>模板函数只有在被调用的时候才会被创造，在之前都是不存在的，所以编译不会通不过，甚至不会报错（在有错误的情况下）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Array</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> m_Array[N];  </span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">GetSize</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> N; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    </span><br><span class="line">    Array&lt;<span class="number">5</span>&gt; a;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;a.GetSize()&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T,<span class="keyword">int</span> N&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Array</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T m_Array[N];</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">GetSize</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> N; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    </span><br><span class="line">    Array &lt; <span class="built_in">std</span>::<span class="built_in">string</span> , <span class="number">5</span> &gt; a;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;a.GetSize()&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-32-宏"><a href="#3-32-宏" class="headerlink" title="3.32 宏"></a>3.32 宏</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOG(x) std::cout&lt;&lt;x&lt;&lt;std::endl;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> PR_DEBUG</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOG(x) std::cout&lt;&lt;x&lt;&lt;std:;endl;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOG(x)</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAIN int main()\</span></span><br><span class="line">&#123;\</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();\</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-33-别名"><a href="#3-33-别名" class="headerlink" title="3.33 别名"></a>3.33 别名</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">String</span> = <span class="built_in">std</span>::<span class="built_in">string</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="built_in">std</span>::<span class="built_in">string</span> <span class="keyword">String</span>;</span><br></pre></td></tr></table></figure>

<p>如果变量类型太长，可以使用auto关键字定义变量。</p>
<h2 id="3-34-std-array"><a href="#3-34-std-array" class="headerlink" title="3.34 std::array"></a>3.34 std::array</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">array</span>&lt;<span class="keyword">int</span>, 5&gt; a;</span><br><span class="line">a.<span class="built_in">size</span>();</span><br></pre></td></tr></table></figure>

<p>和其它容器一样有很多的迭代器</p>
<p>在较高级别的debug_level下，会进行越界检查</p>
<h2 id="3-35-函数指针"><a href="#3-35-函数指针" class="headerlink" title="3.35 函数指针"></a>3.35 函数指针</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">HelloWorld</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Hello World"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> function = HelloWorld; <span class="comment">//这里本来是&amp;HelloWorld， 但是有一个隐式转换</span></span><br><span class="line">    function();</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">HelloWorld</span><span class="params">(<span class="keyword">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Hello World with data "</span> &lt;&lt;a&lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">void</span>(*function)(<span class="keyword">int</span>) = HelloWorld;</span><br><span class="line">    function(<span class="number">8</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">HelloWorld</span><span class="params">(<span class="keyword">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Hello World with data "</span> &lt;&lt;a&lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">typedef</span> <span class="title">void</span><span class="params">(*function)</span><span class="params">(<span class="keyword">int</span>)</span></span>;<span class="comment">//在这里类型名是void (*) (int) ，别名是 function</span></span><br><span class="line"></span><br><span class="line">    function Fun = HelloWorld;</span><br><span class="line">    Fun(<span class="number">8</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-36-lambdas"><a href="#3-36-lambdas" class="headerlink" title="3.36 lambdas"></a>3.36 lambdas</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">[capture <span class="built_in">list</span>] (parameter <span class="built_in">list</span>) -&gt; <span class="keyword">return</span> type &#123;function body&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>捕获列表：当要使用函数体内的变量时，要使用这个</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">[a,&amp;b]</span><br><span class="line">[&amp;] : by reference</span><br><span class="line">[=] : all automatic variables <span class="keyword">and</span> objects by  copy</span><br></pre></td></tr></table></figure>

<p>其他的就和一个函数指针一样</p>
</li>
<li><p>除了捕获列表和函数体，其余的可以省略</p>
</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ForEach</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; values, <span class="keyword">int</span>(*func)(<span class="keyword">int</span>))</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> value : values)</span><br><span class="line">    &#123;</span><br><span class="line">        func(value);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Print</span><span class="params">(<span class="keyword">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; values = &#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span> &#125;;</span><br><span class="line">    <span class="keyword">auto</span> a = [](<span class="keyword">int</span> value) -&gt;<span class="keyword">int</span> &#123; <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; value &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>; <span class="keyword">return</span> value; &#125;;</span><br><span class="line">    ForEach(values, a);</span><br><span class="line">    <span class="keyword">auto</span> c = Print;</span><br><span class="line">    ForEach(values, c);</span><br><span class="line">    <span class="keyword">auto</span> b = [](<span class="keyword">int</span> var)-&gt;<span class="keyword">int</span> &#123;<span class="keyword">return</span> var; &#125;;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; b(<span class="number">5</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; values = &#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span> &#125;;</span><br><span class="line">    <span class="keyword">auto</span> a = <span class="built_in">std</span>::find_if(values.<span class="built_in">begin</span>(), values.<span class="built_in">end</span>(), [](<span class="keyword">int</span> value) &#123;<span class="keyword">return</span> value &gt; <span class="number">3</span>; &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; *a &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-37-namespace"><a href="#3-37-namespace" class="headerlink" title="3.37 namespace"></a>3.37 namespace</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> apple</span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">namespace</span> orange</span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    apple::Print();</span><br><span class="line">    <span class="keyword">namespace</span> b = orange;</span><br><span class="line">    b::Print();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以避免命名冲突</p>
<h2 id="3-38-线程"><a href="#3-38-线程" class="headerlink" title="3.38 线程"></a>3.38 线程</h2><p>可以简单理解成很多事一起做？</p>
<ul>
<li>join() :just like wait,本来是多个线程，但是现在join之后，表示等待这个线程完成再继续main thread</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">bool</span> s_Finished= <span class="literal">false</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DoWork</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::literals::chrono_literals;</span><br><span class="line">    <span class="keyword">while</span> (!s_Finished)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Work"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        <span class="built_in">std</span>::this_thread::sleep_for(<span class="number">1</span>s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::thread <span class="title">worker</span><span class="params">(DoWork)</span></span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">    s_Finished = <span class="literal">true</span>;</span><br><span class="line">    worker.join();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-39-timing"><a href="#3-39-timing" class="headerlink" title="3.39 timing"></a>3.39 timing</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>::literals::chrono_literals;</span><br><span class="line">    <span class="keyword">auto</span> start = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line">    <span class="built_in">std</span>::this_thread::sleep_for(<span class="number">1</span>s);</span><br><span class="line">    <span class="keyword">auto</span> <span class="built_in">end</span> = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">std</span>::chrono::duration&lt;<span class="keyword">float</span>&gt; duration = <span class="built_in">end</span> - start;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; duration.count() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Timer</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">     <span class="built_in">std</span>::chrono::steady_clock::time_point  start = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line">     <span class="built_in">std</span>::chrono::steady_clock::time_point  <span class="built_in">end</span> = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line">     <span class="built_in">std</span>::chrono::duration&lt;<span class="keyword">float</span>&gt;  duration;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    Timer()</span><br><span class="line">    &#123;</span><br><span class="line">        start = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line">    &#125;</span><br><span class="line">    ~Timer()</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">end</span> = <span class="built_in">std</span>::chrono::high_resolution_clock::now();</span><br><span class="line">        duration = <span class="built_in">end</span> - start;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; duration.count() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Function</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Timer t;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Hello"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  </span><br><span class="line">    Function();</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-40-std-sort"><a href="#3-40-std-sort" class="headerlink" title="3.40 std::sort"></a>3.40 std::sort</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; values = &#123; <span class="number">3</span>,<span class="number">2</span>,<span class="number">5</span>,<span class="number">4</span>,<span class="number">1</span> &#125;;</span><br><span class="line">    <span class="built_in">std</span>::sort(values.<span class="built_in">begin</span>(), values.<span class="built_in">end</span>(),<span class="built_in">std</span>::greater&lt;<span class="keyword">int</span>&gt;()); <span class="comment">//默认是升序，这样是降序</span></span><br><span class="line">    <span class="built_in">std</span>::sort(values.<span class="built_in">begin</span>(), values.<span class="built_in">end</span>(), [](<span class="keyword">int</span> a, <span class="keyword">int</span> b) &#123;<span class="keyword">return</span> a &gt; b; &#125;); <span class="comment">//默认是升序，这样是降序</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> value : values)</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; value &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>不会的参考<a href="https://en.cppreference.com/w/" target="_blank" rel="noopener">这个网站</a></p>
<h2 id="3-41-Union"><a href="#3-41-Union" class="headerlink" title="3.41 Union"></a>3.41 Union</h2><p>共用一块内存</p>
<p>共用体内可能有很多的变量，但是它们都是一块内存，至此就会涉及到类型双关的问题。</p>
<p>比如</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Union</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">union</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">float</span> x;</span><br><span class="line">        <span class="keyword">int</span> y;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line">Union u;</span><br><span class="line">u.x = <span class="number">2</span>;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;u.x&lt;&lt;<span class="string">" "</span>&lt;&lt;u.y&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">2</span> <span class="number">1073741824</span></span><br></pre></td></tr></table></figure>

<p>第二个数字正是00000002在float计数方式中的数值</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Vector2</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">float</span> x, y;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Vector4</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">union</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">        &#123;</span></span><br><span class="line">            <span class="keyword">float</span> x, y, z, w;</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">        &#123;</span></span><br><span class="line">            Vector2 a, b;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::ostream&amp; <span class="keyword">operator</span>&lt;&lt;(<span class="built_in">std</span>::ostream&amp; stream, <span class="keyword">const</span> Vector2 vec)</span><br><span class="line">&#123;</span><br><span class="line">    stream &lt;&lt; vec.x &lt;&lt; <span class="string">", "</span> &lt;&lt; vec.y &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> stream;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Vector4 v = &#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span> &#125;;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; v.a &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; v.b &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-42-虚析构函数"><a href="#3-42-虚析构函数" class="headerlink" title="3.42 虚析构函数"></a>3.42 虚析构函数</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#include&lt;iostream&gt;</span><br><span class="line"></span><br><span class="line">class Base</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">    Base()</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; &quot;Base Constuctor&quot; &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">    ~Base()</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; &quot;Base Destuctor&quot; &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class Derived :public Base</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">    Derived()</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; &quot;Derived Constuctor&quot; &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">    ~Derived()</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; &quot;Derived Destuctor&quot; &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    Base* a &#x3D; new Base();</span><br><span class="line">    delete a;</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; &quot;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&quot; &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">    Derived* b &#x3D; new Derived();</span><br><span class="line">    delete b;</span><br><span class="line"></span><br><span class="line">    std::cin.get();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Base Constuctor</span><br><span class="line">Base Destuctor</span><br><span class="line">=========================</span><br><span class="line">Base Constuctor</span><br><span class="line">Derived Constuctor</span><br><span class="line">Derived Destuctor</span><br><span class="line">Base Destuctor</span><br></pre></td></tr></table></figure>

<p>派生类销毁时也会调用基类的析构函数，构造时也会先调用基类的构造函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Base* a = <span class="keyword">new</span> Base();</span><br><span class="line">    <span class="keyword">delete</span> a;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"========================="</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    Derived* b = <span class="keyword">new</span> Derived();</span><br><span class="line">    <span class="keyword">delete</span> b;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"========================="</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    Base* c = <span class="keyword">new</span> Derived();</span><br><span class="line">    <span class="keyword">delete</span> c;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Base Constuctor</span><br><span class="line">Base Destuctor</span><br><span class="line">=========================</span><br><span class="line">Base Constuctor</span><br><span class="line">Derived Constuctor</span><br><span class="line">Derived Destuctor</span><br><span class="line">Base Destuctor</span><br><span class="line">=========================</span><br><span class="line">Base Constuctor</span><br><span class="line">Derived Constuctor</span><br><span class="line">Base Destuctor</span><br></pre></td></tr></table></figure>

<p>不像虚方法，析构函数改成虚函数，会同时调用两个析构函数</p>
]]></content>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>GDB 调试</title>
    <url>/2020/07/22/Gdb%E8%B0%83%E8%AF%95/</url>
    <content><![CDATA[<p>本篇博文记录一些GDB的基本使用方法</p>
<a id="more"></a>

<p>在使用GDB调试前，首先要生成可执行程序</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc *.c -o app -g</span><br></pre></td></tr></table></figure>

<p>使用<code>-g</code>参数，使生成的可执行程序中包含调试信息</p>
<p>然后使用<code>dgb app</code>进入调试截面</p>
<h1 id="1-打断点"><a href="#1-打断点" class="headerlink" title="1. 打断点"></a>1. 打断点</h1><ul>
<li><p>查看程序源代码</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">l</span><br></pre></td></tr></table></figure>

<p>上面的代码查看带<code>main</code>函数的文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">l a.c:20</span><br></pre></td></tr></table></figure>

<p>上面的代码查看<code>a.c</code>函数的第20行内容，行号也可以换成函数名称</p>
<p>继续使用<code>l</code>就能继续往下看</p>
</li>
<li><p>断点</p>
<p>使用<code>break n</code>在第n行设置断点，这个<code>break</code>可以简写成<code>b</code></p>
</li>
<li><p>带有条件的断点</p>
<p>比如在循环里面，不希望每次都断点</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">b 22 <span class="keyword">if</span> i==10</span><br></pre></td></tr></table></figure>

<p>在<code>i==10</code>时，才会有这个断点</p>
</li>
<li><p>使用<code>info b</code>查看断点信息，<code>info</code>可以简写为<code>i</code></p>
</li>
<li><p><code>d 编号</code>删除断点</p>
</li>
</ul>
<h1 id="2-执行"><a href="#2-执行" class="headerlink" title="2. 执行"></a>2. 执行</h1><ul>
<li><code>start</code>执行一步</li>
<li><code>n</code>单步执行</li>
<li><code>s</code>单步进入</li>
<li><code>c</code>继续执行</li>
<li><code>p i</code>查看<code>i</code>的值</li>
<li><code>ptype i</code>查看<code>i</code>类型</li>
<li><code>display i</code> 追踪变量<code>i</code>的值，使用<code>undisplay 编号</code> 不追踪，编号使用<code>info display</code>查看</li>
<li>进入循环，使用<code>u</code>跳出单次循环 </li>
<li><code>finish</code>跳出函数</li>
<li><code>set var i=10</code>将<code>i</code>设置成10</li>
</ul>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>gdb</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 库的制作</title>
    <url>/2020/07/21/Linux%20%E5%BA%93%E7%9A%84%E5%88%B6%E4%BD%9C/</url>
    <content><![CDATA[<p>本篇博文记录一下Linux的动态库和静态库的生成。</p>
<a id="more"></a>

<h1 id="1-静态库"><a href="#1-静态库" class="headerlink" title="1. 静态库"></a>1. 静态库</h1><h2 id="1-1-命名规则"><a href="#1-1-命名规则" class="headerlink" title="1.1 命名规则"></a>1.1 命名规则</h2><ul>
<li><code>lib+库的名字+.a</code>，例如<code>libmytest.a</code></li>
</ul>
<h2 id="1-2-制作步骤"><a href="#1-2-制作步骤" class="headerlink" title="1.2 制作步骤"></a>1.2 制作步骤</h2><ul>
<li><p>使用<code>-c</code>参数，将一个<code>.c</code>生成一个<code>.o</code></p>
</li>
<li><p>将生成的<code>.o</code>工具打包，使用工具ar</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ar -rsc  库名 生成的所有的.o</span><br><span class="line">ar -rsc libmytest.a a.o b.o c.o</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="1-3-发布和使用"><a href="#1-3-发布和使用" class="headerlink" title="1.3 发布和使用"></a>1.3 发布和使用</h2><ul>
<li><p>不要给<code>.c</code>的源代码，只用给<code>.o</code>文件</p>
</li>
<li><p>函数接口通过头文件给用户</p>
</li>
<li><p>一个例子</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -c *.c -I ../head  <span class="comment">#生成目标文件</span></span><br><span class="line">ar rcs libMyCalc.a *.o	<span class="comment">#生成静态库文件</span></span><br></pre></td></tr></table></figure>

<p>使用的时候可以有如下两种操作</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc main.c lib/libMyCalc.a -o out -I head</span><br><span class="line"><span class="comment">#or</span></span><br><span class="line">gcc main.c -L lib -l MyCalc -I head -o out</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="1-4-分析"><a href="#1-4-分析" class="headerlink" title="1.4 分析"></a>1.4 分析</h2><ul>
<li>使用<code>nm libMyCalc.a</code>查看库的<code>.o</code>文件</li>
<li>使用静态库的时候，程序中需要什么文件，那么可执行文件中就只会打包使用到的<code>.o</code>文件中</li>
<li>优点：<ul>
<li>发布时不需要提供库文件</li>
<li>库的加载速度很快</li>
</ul>
</li>
<li>缺点：<ul>
<li>库打包到应用程序中，会导致发布的文件很大</li>
<li>库发生了改变，需要重新编译程序</li>
</ul>
</li>
</ul>
<hr>
<h1 id="2-动态库（共享库）"><a href="#2-动态库（共享库）" class="headerlink" title="2. 动态库（共享库）"></a>2. 动态库（共享库）</h1><h2 id="2-1-命名规则"><a href="#2-1-命名规则" class="headerlink" title="2.1 命名规则"></a>2.1 命名规则</h2><ul>
<li><p><code>lib + 库名 + .so</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">libMyTest.so</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="2-2-制作步骤"><a href="#2-2-制作步骤" class="headerlink" title="2.2 制作步骤"></a>2.2 制作步骤</h2><ul>
<li><p>生成与位置无关的代码<code>.o</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -fPIC -c *.c -I ./head</span><br></pre></td></tr></table></figure>

<blockquote>
<p>什么叫做与位置无关？</p>
<p>Linux每个运行的程序操作系统都会为其分配一个0~4G(针对32位系统而言)的虚拟地址空间，大致排列如下</p>
<p>4G：Linux Kernel ：内存管理、进程管理、设备驱动管理、VFS虚拟文件系统</p>
<p>3G：环境变量（env）</p>
<p>​        命令行参数（<code>int main (int argc, char* argv[])</code>中的<code>argv[]</code>）</p>
<p>​        栈空间（小）</p>
<p>​        共享库空间（C标准库，Linux系统I/O函数）</p>
<p>​        堆空间（大）</p>
<p>​        .bss（未初始化全局变量）</p>
<p>​        .data（已初始化全局变量）</p>
<p>​        .text （代码段，二进制机器指令）</p>
<p>0：  受保护的地址(0~4k)</p>
<p>静态库每次被打包到可执行文件中，都会被分配空间，而且是固定的位置（使用的是绝对地址）</p>
<p>动态库的函数放到共享库空间，每次放到的地方都是不一定的（位置是相对地址）</p>
</blockquote>
</li>
<li><p>将<code>.o</code>打包成文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -share -o libMyTest.so *.o -I ./head</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="2-3-使用"><a href="#2-3-使用" class="headerlink" title="2.3 使用"></a>2.3 使用</h2><ul>
<li><pre><code class="bash">gcc main.c lib/libMyTest.so -o app -I ./head
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- &#96;&#96;&#96;bash</span><br><span class="line">  gcc main,c -I .&#x2F;head -L .&#x2F;lib -l MyTest -o app</span><br></pre></td></tr></table></figure>

使用的时候应该由一个提示，提示链接不到

使用`ldd`命令查看可执行程序所依赖的所有动态库

解决方法：

- 将`so`文件拷贝到`lib`中，就可以执行可执行程序了

  但是这种方法是不可以用的

- 将自己的动态库所在的文件夹赋值给`LD_LIBRARY_PATH`（使用`<span class="built_in">export</span>`命令）即可执行可执行程序

  这个方法在每次打开终端时都是需要操作的（此变量在关闭之后会被清空），这个一般是用于开发过程中使用的

- 永久的方法：配置到`.bashrc`文件中，在文件后使用

  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#export LD_LIBRARY_PATH=/绝对路径</span></span><br></pre></td></tr></table></figure>

  再`<span class="built_in">source</span> ./.bashrc`即可

- 常用的方法

  - 找到动态连接器的配置文件`/etc/ld.so.conf`
  - `sudo vim /etc/ld/so.conf`
  - 把动态库的绝对路径写入到这个文件夹
  - `sudo ldconfig -v` 就可以了
</code></pre>
</li>
</ul>
<h2 id="2-4-优缺点"><a href="#2-4-优缺点" class="headerlink" title="2.4 优缺点"></a>2.4 优缺点</h2><ul>
<li>优点<ul>
<li>执行程序体积变小，因为可执行程序不包含库文件</li>
<li>动态库更新了，如果接口不变，则不要重新编译</li>
</ul>
</li>
<li>缺点<ul>
<li>发布程序时，需要将动态库提供给客户</li>
<li>动态库加载时比静态库要慢一些</li>
</ul>
</li>
</ul>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 网络管理</title>
    <url>/2020/07/20/Linux%20%E7%BD%91%E7%BB%9C%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<p>有关Linux的FTP、NFS、SSH的知识</p>
<a id="more"></a>

<h1 id="1-查看网络相关信息"><a href="#1-查看网络相关信息" class="headerlink" title="1. 查看网络相关信息"></a>1. 查看网络相关信息</h1><h2 id="1-1-ifconfig"><a href="#1-1-ifconfig" class="headerlink" title="1.1 ifconfig"></a>1.1 ifconfig</h2><p>直接用这个命令可以查看到网络相关信息，下面是我的WSL使用这个命令查看到的一部分信息，下面以这个信息为例解读一下。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wendell@DESKTOP-R43K3TI:/mnt/e$ ifconfig</span><br><span class="line">eth3      Link encap:Ethernet  HWaddr 00:50:56:c0:00:01</span><br><span class="line">          inet addr:192.168.47.1  Bcast:192.168.47.255  Mask:255.255.255.0</span><br><span class="line">          inet6 addr: fe80::eddb:b4dd:6c4a:810b/64 Scope:Unknown</span><br><span class="line">          UP BROADCAST RUNNING MULTICAST  MTU:1500  Metric:1</span><br><span class="line">          RX packets:0 errors:0 dropped:0 overruns:0 frame:0</span><br><span class="line">          TX packets:0 errors:0 dropped:0 overruns:0 carrier:0</span><br><span class="line">          collisions:0</span><br><span class="line">          RX bytes:0 (0.0 B)  TX bytes:0 (0.0 B)</span><br><span class="line">lo        Link encap:Local Loopback</span><br><span class="line">          inet addr:127.0.0.1  Mask:255.0.0.0</span><br><span class="line">          inet6 addr: ::1/128 Scope:Unknown</span><br><span class="line">          UP LOOPBACK RUNNING  MTU:1500  Metric:1</span><br><span class="line">          RX packets:0 errors:0 dropped:0 overruns:0 frame:0</span><br><span class="line">          TX packets:0 errors:0 dropped:0 overruns:0 carrier:0</span><br><span class="line">          collisions:0</span><br><span class="line">          RX bytes:0 (0.0 B)  TX bytes:0 (0.0 B)</span><br></pre></td></tr></table></figure>

<ul>
<li><code>eth3</code>：网卡，0代表第一块网卡</li>
<li><code>HWaddr</code>：网卡的MAC地址</li>
<li><code>inet addr</code>：IP地址</li>
<li><code>bcast</code>：在摸一个网段内的广播地址，这里是40网段</li>
<li><code>lo</code>：本地回环</li>
</ul>
<h2 id="1-2-ping"><a href="#1-2-ping" class="headerlink" title="1.2 ping"></a>1.2 ping</h2><ul>
<li><p>ping一下IP，但是LInux下直接使用这个会一直ping，windows下是蹦4条</p>
</li>
<li><p><code>-c</code>：这个参数指定蹦几条</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping 192.168.47.109 -c 4</span><br></pre></td></tr></table></figure>
</li>
<li><p>可以ping网址</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping www.baidu.com</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="1-3-nslookup"><a href="#1-3-nslookup" class="headerlink" title="1.3 nslookup"></a>1.3 nslookup</h2><p>可以用这个命令查看网址的相关信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nslookup www.baidu.com</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="2-FTP服务器的搭建"><a href="#2-FTP服务器的搭建" class="headerlink" title="2. FTP服务器的搭建"></a>2. FTP服务器的搭建</h1><h2 id="2-1-vsftpd"><a href="#2-1-vsftpd" class="headerlink" title="2.1 vsftpd"></a>2.1 vsftpd</h2><ul>
<li><p>在使用前需要手动安装这个软件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt install vsftpd</span><br></pre></td></tr></table></figure>

<p>安装之后会发现使用<code>vim /etc/passwd</code>查看所有用户时，多了一个<code>ftp</code>用户</p>
</li>
<li><p>FTP服务器功能单一，负责文件的上传和下载</p>
</li>
<li><p>服务器端</p>
<ul>
<li><p>修改配置文件</p>
<ul>
<li><pre><code class="bash">sudo vim /etc/vsftpd.conf
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- &#96;&#96;&#96;bash</span><br><span class="line">  anonymous_enable&#x3D;YES  #允许匿名用户登录</span><br><span class="line">  local_enable&#x3D;YES  #允许本地用户登录</span><br><span class="line">  write_enable&#x3D;YES #实名用户拥有写权限，可以上传文件</span><br><span class="line">  local_umask&#x3D;022 #设置本地掩码为022</span><br><span class="line">  anon_upload_enable&#x3D;YES #匿名用户可以向服务器上传文件</span><br><span class="line">  anon_mkdir_write_enable&#x3D;YES #匿名用户可以穿件一个新目录</span><br></pre></td></tr></table></figure></code></pre>
</li>
<li><p>自己看说明文档全部配置好就可以了</p>
</li>
</ul>
</li>
<li><p>重启服务</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo service vsftpd restart</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>客户端</p>
<ul>
<li><p>实名用户登录：登陆之后可以在FTP服务器进行操作</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ftp IP(server)</span><br><span class="line">输入用户名(server)</span><br><span class="line">输入密码(server)</span><br></pre></td></tr></table></figure>
</li>
<li><p>匿名用户登录：这个才是常用的</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ftp ServerIP</span><br><span class="line">    用户名：anonymous</span><br><span class="line">    密码:直接回车</span><br></pre></td></tr></table></figure>

<p>只能在一个指定的目录下操作，这个目录就是匿名用户的根目录，在配置文件中设置</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/vsftpd.conf</span><br></pre></td></tr></table></figure>

<p>加上一句，指定一下目录，比如</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">anon_root=/home/wendell/path</span><br></pre></td></tr></table></figure>

<p>重启服务</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo service vsftpd restart</span><br></pre></td></tr></table></figure>
</li>
<li><p>上传文件：将本地文件上传到，上传的是登录时的路径下的文件，上传到的地方就是你登陆之后所在的目录</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">put pathname</span><br></pre></td></tr></table></figure>
</li>
<li><p>下载文件：不允许操作目录，如果想操作目录需要先打包再下载</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">get filename</span><br></pre></td></tr></table></figure>
</li>
<li><p>退出</p>
<p><code>quit</code> <code>bye</code> <code>exit</code>均可</p>
</li>
</ul>
</li>
</ul>
<h2 id="2-2-lftp"><a href="#2-2-lftp" class="headerlink" title="2.2 lftp"></a>2.2 lftp</h2><ul>
<li><p>安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install lftp</span><br></pre></td></tr></table></figure>
</li>
<li><p>登录服务器</p>
<ul>
<li><p>匿名登录</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lftp serverIP</span><br><span class="line">login</span><br></pre></td></tr></table></figure>
</li>
<li><p>实名登录</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lftp username@IP</span><br><span class="line">password</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>使用lftp登录成功后可以使用<code>lpwd</code>查看当前本地机器所在目录，使用<code>lcd</code>可以切换目录，比vsftpd方便一点</p>
</li>
<li><p>使用<code>mput</code>可以同时上传多个文件，使用<code>mget</code>下载多个文件</p>
</li>
<li><p><code>mirror -R</code> 上传整个目录</p>
</li>
<li><p><code>mirror</code>下载整个目录</p>
</li>
<li><p>注意：lftp只是一个客户端小工具</p>
</li>
</ul>
<hr>
<h1 id="3-NFS服务器的搭建"><a href="#3-NFS服务器的搭建" class="headerlink" title="3. NFS服务器的搭建"></a>3. NFS服务器的搭建</h1><ul>
<li><p>安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install nfs-kernel-server</span><br></pre></td></tr></table></figure>
</li>
<li><p>创建一个共享出去的目录（有点类似于Windows下的共享文件夹）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkdir dir</span><br></pre></td></tr></table></figure>

<p>修改配置文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/exports</span><br></pre></td></tr></table></figure>

<p>加上自己共享的目录即可</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pathname *(rw,sync) <span class="comment">#这个*表示网段，rw表示可读可写，ro是只读，sync表示是实时更新</span></span><br></pre></td></tr></table></figure>

<p>重启</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo service nfs-kernel-server restart</span><br></pre></td></tr></table></figure>
</li>
<li><p>客户端：访问，使用挂载的方式访问</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mount serverIP:pathname pathname</span><br></pre></td></tr></table></figure>

<p>第一个pathname是要共享的目录，第二个是要挂载到本地的哪个目录下面</p>
</li>
</ul>
<hr>
<h1 id="4-SSH服务器"><a href="#4-SSH服务器" class="headerlink" title="4. SSH服务器"></a>4. SSH服务器</h1><ul>
<li><p>服务器端</p>
<ul>
<li><p>安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install openssh-server</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>客户端</p>
<ul>
<li><p>登录</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh username@IP</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>退出 <code>logout</code></p>
</li>
</ul>
<hr>
<h1 id="5-scp命令"><a href="#5-scp命令" class="headerlink" title="5. scp命令"></a>5. scp命令</h1><ul>
<li><p>这个命令需要先安装SSH服务器</p>
</li>
<li><p>拷贝到本地</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scp -r 目标用户名@目标用户IP:目录 本地目录</span><br></pre></td></tr></table></figure>
</li>
<li><p>拷贝到远程</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scp -r 本地目录 目标用户名@目标用户IP:目录</span><br></pre></td></tr></table></figure>

</li>
</ul>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux命令</title>
    <url>/2019/09/08/Linux-%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<p>Linux中命令比较多，本篇博客记录在开发中常用的简单的Linux命令</p>
<a id="more"></a>

<h2 id="1-Linux-命令格式"><a href="#1-Linux-命令格式" class="headerlink" title="1.Linux 命令格式"></a>1.Linux 命令格式</h2><p><strong><em>command [-options] [parameter1] …</em></strong></p>
<ul>
<li><p>[] 表示是可选的，在命令中可以不用</p>
</li>
<li><p>options 表示命令的选项，可以按需执行一些操作</p>
</li>
<li><p>parameter 表示命令的参数，比如pathname等</p>
</li>
<li><p>对于不熟悉的命令，可以使用<code>man</code>命令，或者<code>--help</code>参数查看帮助手册的部分内容，在这个帮助文档里可以看到这些命令的功能和参数。但是注意到并不是所有的命令都有<code>--help</code>选项，但是所有的命令都可以在man手册中看到</p>
<blockquote>
<p>man手册中有命令和一些函数，这样就不可避免的有一些命令和函数是同名的，如此就需要使用不同的参数来查找man手册。可以使用 <strong><em>man [number] [name]</em></strong> 的形式来区分，比如查找命令使用数字1（缺省值），数字3表示库函数</p>
</blockquote>
</li>
<li><p>使用<code>tab</code>键可以自动补全命令，这对Linux的使用来说十分方便</p>
</li>
<li><p>使用方向键<code>↑</code>可以按序查找之前使用过的命令</p>
</li>
<li><p>使用<strong><em>history</em></strong>命令可以查看最近使用的多条命令</p>
</li>
<li><p><code>alias</code>  可以查看命令的别名</p>
</li>
</ul>
<hr>
<h2 id="2-Linux常用命令"><a href="#2-Linux常用命令" class="headerlink" title="2.Linux常用命令"></a>2.Linux常用命令</h2><h3 id="2-1-文件管理命令"><a href="#2-1-文件管理命令" class="headerlink" title="2.1 文件管理命令"></a>2.1 文件管理命令</h3><h4 id="2-1-1-ls"><a href="#2-1-1-ls" class="headerlink" title="2.1.1 ls"></a>2.1.1 ls</h4><p>用来查看命令（Windows中的dir命令）</p>
<ul>
<li><p><code>-a</code>: 用来查看包括隐藏目录的所有目录（以<code>.</code>开头的文件名、路径名就是隐藏的）</p>
</li>
<li><p><code>-l</code>: 就是以列表的方式查看详细目录，包括目录的类型、目录的子目录个数（普通文件的硬链接个数）、所属、大小、最后修改时间</p>
</li>
<li><p><code>-h</code>: 以人性化的方式查看文件的详细目录，更加具体地说是更人性化显示文件的大小（使用时需要配合<code>-l</code>使用）</p>
</li>
<li><p>通配符：</p>
<ul>
<li>* : 可以替代所有的字符，字符数不限</li>
<li>? : 可以替代一个字符</li>
<li>[abc] : 可以替代a、b、c中任意一个字符</li>
<li>[a-f] : 可以替代a到f中任意一个字符</li>
</ul>
<blockquote>
<p>注意当输入<code>a-f</code>时并不代表通配符</p>
<p>当需要查找含有<em>的目录名时，可以使用转义字符\ ，当然很多命令的\</em> 都是使用转义字符的</p>
</blockquote>
</li>
</ul>
<h4 id="2-1-2-输出重定向：-gt"><a href="#2-1-2-输出重定向：-gt" class="headerlink" title="2.1.2 输出重定向：&gt;"></a>2.1.2 输出重定向：&gt;</h4><p>举例说明输出重定向的作用</p>
<p><strong><em>ls &gt; test.txt</em></strong></p>
<p>表示将原来输出到屏幕上的输出到<code>test.txt</code>文件中，若不存在这个文件，则创建这个文件；若已经存在这个文件，则将删掉原先内容再重新输入内容</p>
<p><strong><em>ls &gt;&gt; test.txt</em></strong></p>
<p>使用这个命令，就可以在已经存在的文件后面追加输入内容</p>
<h4 id="2-1-3-显示命令"><a href="#2-1-3-显示命令" class="headerlink" title="2.1.3 显示命令"></a>2.1.3 显示命令</h4><ul>
<li><code>cat</code>：查看文件内容</li>
<li><code>more</code>:分屏显示文件内容，使用空格翻页、使用q退出</li>
<li><code>less</code>:分屏显示文件内容，功能更为齐全，可以上下翻页</li>
</ul>
<h4 id="2-1-4-管道命令"><a href="#2-1-4-管道命令" class="headerlink" title="2.1.4 管道命令: |"></a>2.1.4 管道命令: |</h4><p><strong>一个命令的输入是另一个命令的输出</strong></p>
<p>举例说明</p>
<p><strong><em>ls | grep std</em></strong></p>
<p>将ls命令的输出放在管道的一端作为grep命令的输入，grep命令是查找关键字（文本搜索）命令。那么这个命令的作用就是将在ls的输出内容中查找含有std的项</p>
<h4 id="2-1-5-清屏：clear"><a href="#2-1-5-清屏：clear" class="headerlink" title="2.1.5 清屏：clear"></a>2.1.5 清屏：clear</h4><p>使用<code>clear</code>命令就可以清除屏幕上的内容（Windows中是cls），但是注意并不是指删除，向上翻还是可以看到上面输出的内容的。可以使用快捷键<code>ctrl</code> <code>+</code> <code>L</code> 来替代这个命令</p>
<h4 id="2-1-6-切换工作目录：cd"><a href="#2-1-6-切换工作目录：cd" class="headerlink" title="2.1.6 切换工作目录：cd"></a>2.1.6 切换工作目录：cd</h4><p><strong><em>cd pathname</em></strong> : 切换到<code>pathname</code>表示的目录，表示方法可以用绝对路径，也可以使用相对路径</p>
<ul>
<li><code>~</code>：表示家目录</li>
<li><code>-</code>：上一次的目录</li>
</ul>
<h4 id="2-1-7-查看当前工作目录：pwd"><a href="#2-1-7-查看当前工作目录：pwd" class="headerlink" title="2.1.7 查看当前工作目录：pwd"></a>2.1.7 查看当前工作目录：pwd</h4><p>输入<strong><em>pwd</em></strong>命令就可以看到自己当前所在的工作目录</p>
<h4 id="2-1-8-创建目录：mkdir"><a href="#2-1-8-创建目录：mkdir" class="headerlink" title="2.1.8 创建目录：mkdir"></a>2.1.8 创建目录：mkdir</h4><p>相当于Windows中的新建文件夹操作</p>
<ul>
<li><code>-p</code>：嵌套创建文件夹</li>
</ul>
<h4 id="2-1-9-删除目录：rmdir"><a href="#2-1-9-删除目录：rmdir" class="headerlink" title="2.1.9 删除目录：rmdir"></a>2.1.9 删除目录：rmdir</h4><p>就是字面意思，删除一个目录</p>
<ul>
<li>只能删除空目录</li>
<li><code>-p</code>：连级删除空目录</li>
</ul>
<h4 id="2-1-10-删除命令：rm"><a href="#2-1-10-删除命令：rm" class="headerlink" title="2.1.10 删除命令：rm"></a>2.1.10 删除命令：rm</h4><p><strong><em>rm [option] pathname</em></strong></p>
<ul>
<li><code>-r</code>：删除目录</li>
<li><code>-f</code>：强制删除，目录</li>
<li><code>-i</code>：询问是否删除</li>
</ul>
<h4 id="2-1-11-创建链接：ln"><a href="#2-1-11-创建链接：ln" class="headerlink" title="2.1.11 创建链接：ln"></a>2.1.11 创建链接：ln</h4><p>可以理解为Windows中的创建快捷方式，Linux中的链接分为软链接和硬链接</p>
<p><strong><em>ln 源文件 链接文件</em></strong></p>
<p><strong><em>ln -s 源文件 链接文件</em></strong></p>
<ul>
<li>软链接并不占用磁盘空间，当源文件删除之后则软链接失效</li>
<li>硬链接：更像是将文件copy了一份，两个文件占据<strong>相同的空间</strong>，但是修改其中一个文件时，另一个文件也会被同步修改，当删除源文件，硬链接文件还在。硬链接只能链接普通文件，不能链接目录</li>
</ul>
<h4 id="2-1-12-查看或合并文本内容：cat"><a href="#2-1-12-查看或合并文本内容：cat" class="headerlink" title="2.1.12 查看或合并文本内容：cat"></a>2.1.12 查看或合并文本内容：cat</h4><p><strong><em>cat file</em></strong>  ：查看文件内容</p>
<p><strong><em>cat file1 file2 &gt; file3</em></strong> ：使用重定向命令，将<code>file1</code> <code>file2</code> 文件中的内容重定向合并到文件<code>file3</code> 中</p>
<h4 id="2-1-13-文本搜索：grep"><a href="#2-1-13-文本搜索：grep" class="headerlink" title="2.1.13 文本搜索：grep"></a>2.1.13 文本搜索：grep</h4><p><strong><em>grep [options] 搜索文本 filename</em></strong></p>
<ul>
<li><p><code>-v</code>：求反，显示不包括匹配文本的所有行</p>
</li>
<li><p><code>-n</code> ：显示匹配行和行号</p>
</li>
<li><p><code>-i</code> ：忽略大小写</p>
</li>
<li><p>搜索内容可以是正则表达式，下面是<code>grep</code> 命令常用的正则表达式</p>
<ul>
<li><code>^a</code> ：查找以<code>a</code>开头的行</li>
<li><code>ke$</code>：查找以<code>ke</code>结尾的行</li>
<li><code>[]</code>：匹配方括号中的一个字符的行，比如 <code>[Ss]in</code> 表示查找<code>Sin</code> 或者<code>sin</code></li>
<li><code>.</code>：匹配一个字符，相当于2.1.1中提到的通配符的<code>?</code></li>
</ul>
</li>
<li><p>搜索文本内容可以使用引号或双引号</p>
</li>
</ul>
<h4 id="2-1-14-查找文件：find"><a href="#2-1-14-查找文件：find" class="headerlink" title="2.1.14 查找文件：find"></a>2.1.14 查找文件：find</h4><p><strong><em>find pathname options expression</em></strong></p>
<ul>
<li><code>-name</code> ：按照名称查找，可以使用通配符     <code>find ./ -name *.c</code> 查找当前目录下后缀为<code>.c</code>的文件</li>
<li><code>-size</code> ：按照大小查找，<code>find ./ -size 2M</code> 查找当前目录下大小为2M的文件，<code>find ./ -size +4k -size -2M</code> 查找当前目录下大小大于4k、小于2M 的文件</li>
<li><code>-perm</code> ：按照权限查找，<code>find ./ -perm 0777</code> 查找当前目录下权限为777的文件或目录（前面的0表示8进制）</li>
<li><code>-type</code>：按照文件类型查找<ul>
<li><code>f</code> : 普通文件</li>
<li><code>d</code>：目录</li>
<li><code>b</code>：块设备</li>
<li><code>c</code>：字符设备</li>
<li><code>s</code>：socket</li>
<li><code>p</code>：管道</li>
</ul>
</li>
</ul>
<h4 id="2-1-15-复制文件：cp"><a href="#2-1-15-复制文件：cp" class="headerlink" title="2.1.15 复制文件：cp"></a>2.1.15 复制文件：cp</h4><p><strong><em>cp 源文件 目标文件</em></strong></p>
<ul>
<li><code>-r</code>：拷贝文件夹</li>
<li><code>-i</code>：有提示</li>
<li><code>-v</code>：显示拷贝进度，拷贝大文件时可用</li>
<li><code>-a</code>：拷贝所有信息，原封不动的拷贝一份，包括文件属性</li>
</ul>
<h4 id="2-1-16-移动文件：mv"><a href="#2-1-16-移动文件：mv" class="headerlink" title="2.1.16 移动文件：mv"></a>2.1.16 移动文件：mv</h4><p><strong><em>mv 源文件 目标文件</em></strong></p>
<ul>
<li><code>-f</code>：有覆盖不提示</li>
<li><code>-i</code>：有提示</li>
<li><code>-v</code> : 显示移动进度</li>
</ul>
<blockquote>
<p>目标文件若是一个路径就是移动，也可以指定一个名字，可以改名</p>
</blockquote>
<h4 id="2-1-17-查看文件属性：file"><a href="#2-1-17-查看文件属性：file" class="headerlink" title="2.1.17 查看文件属性：file"></a>2.1.17 查看文件属性：file</h4><p>使用此命令可以查看文件属性</p>
<h4 id="2-1-18-归档管理：tar"><a href="#2-1-18-归档管理：tar" class="headerlink" title="2.1.18 归档管理：tar"></a>2.1.18 归档管理：tar</h4><ul>
<li><code>-c</code>：创建打包文件</li>
<li><code>-v</code>：显示进度</li>
<li><code>-f</code>：后面跟文件名，使用时要放在后面</li>
<li><code>-x</code>：解包</li>
<li><code>-C</code>：解包到指定路劲时需要加这个选项</li>
<li><code>-z</code>：同时压缩或者解压</li>
</ul>
<p>常见使用方法</p>
<ul>
<li><code>tar -cvf files file1 file1</code> 将<code>file1</code> <code>file2</code> 打包到<code>files</code>文件中</li>
<li><code>tar -xvf files</code> 将<code>files</code>解包到当前路径</li>
<li><code>tar -xvf -C files ../c/</code> 将<code>files</code>解包到上一级目录中的<code>c</code>目录中</li>
</ul>
<blockquote>
<p>查看目录的大小 <strong><em>du pathname -h</em></strong></p>
</blockquote>
<h4 id="2-1-19-文件压缩工具：gzip（bzip2"><a href="#2-1-19-文件压缩工具：gzip（bzip2" class="headerlink" title="2.1.19 文件压缩工具：gzip（bzip2)"></a>2.1.19 文件压缩工具：gzip（bzip2)</h4><p>对于已经打包的文件进行压缩，也可以解压<code>.gz</code>（<code>.bz2</code>)压缩包</p>
<p><strong><em>gzip pathname</em></strong> ：在当前路径压缩已经打包文件</p>
<ul>
<li><code>-d</code> （<code>bzip2</code>是<code>-z</code>)：解压，<strong><em>gzip -d pathname</em></strong></li>
<li><code>-r</code>：递归压缩，压缩目录，使用此选项时要指定生成的压缩包的名称</li>
</ul>
<blockquote>
<p>注意：打包和压缩可以合并一起使用，<code>tar</code>命令加上<code>-z</code>选项就是压缩，同理也可以解压解包一起执行</p>
<p>对于<code>bzip2</code> 使用的选项是<code>-j</code></p>
<p>所以实际上常用的是</p>
<p>压缩：<code>tar -zcvf xxx.tar.gz file1 file2...</code>    </p>
<p>​            <code>tar -jcvf xxx.tar.bz2 file1 file2 ...</code></p>
<p>解压：<code>tar -zxvf xxx.tar.gz [-C pathname]</code>  </p>
<p>​            <code>tar -jxvf xxx.tar.bz2 [-C pathname]</code></p>
</blockquote>
<h4 id="2-1-20-压缩解压缩工具：zip-unzip-rar"><a href="#2-1-20-压缩解压缩工具：zip-unzip-rar" class="headerlink" title="2.1.20 压缩解压缩工具：zip unzip rar"></a>2.1.20 压缩解压缩工具：zip unzip rar</h4><p><strong><em>zip -r files file1 file2</em></strong> ：压缩</p>
<p><strong><em>unzip -d pathname files</em></strong>：解压</p>
<p><strong><em>rar -a file</em></strong> ：压缩</p>
<p><strong><em>rar -x file</em></strong> ：解压</p>
<h4 id="2-1-21-which"><a href="#2-1-21-which" class="headerlink" title="2.1.21 which"></a>2.1.21 which</h4><p>此命令用来查看命令的可执行程序是放在哪一个文件夹下的</p>
<p><strong><em>which command</em></strong></p>
<h4 id="2-2-22-echo"><a href="#2-2-22-echo" class="headerlink" title="2.2.22 echo"></a>2.2.22 echo</h4><p>输出命令，比如</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">"HELLO"</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>

<hr>
<h3 id="2-2-用户、权限管理"><a href="#2-2-用户、权限管理" class="headerlink" title="2.2 用户、权限管理"></a>2.2 用户、权限管理</h3><p>Linux十分适合多用户使用，Linux中有很好的用户和权限管理工具</p>
<h4 id="2-2-1-查看当前用户：whoami-、who、w"><a href="#2-2-1-查看当前用户：whoami-、who、w" class="headerlink" title="2.2.1 查看当前用户：whoami 、who、w"></a>2.2.1 查看当前用户：whoami 、who、w</h4><p>输入这三个命令都可以查看用户信息，一个命令比一个命令详细</p>
<h4 id="2-2-2-退出登录账号：exit"><a href="#2-2-2-退出登录账号：exit" class="headerlink" title="2.2.2 退出登录账号：exit"></a>2.2.2 退出登录账号：exit</h4><p>退出当前登录账号至上一个登录账号，若没有上一个登录账号，则退出终端</p>
<h4 id="2-2-3-切换用户：su"><a href="#2-2-3-切换用户：su" class="headerlink" title="2.2.3 切换用户：su"></a>2.2.3 切换用户：su</h4><p><strong><em>su username</em></strong></p>
<ul>
<li>如果是切换到root用户，只需要输入<code>su</code>，因为一个Linux系统只允许有一个root用户</li>
</ul>
<h4 id="2-2-4-添加、删除组：groupadd、groupdel"><a href="#2-2-4-添加、删除组：groupadd、groupdel" class="headerlink" title="2.2.4 添加、删除组：groupadd、groupdel"></a>2.2.4 添加、删除组：groupadd、groupdel</h4><ul>
<li><code>groupadd name</code>：添加组，名称为name</li>
<li><code>groupdel name</code>：删除名称为name的组，有用户就不可以删除</li>
<li><code>cat /etc/group</code>：查看用户组</li>
</ul>
<blockquote>
<p>创建一个用户时，熊默认创建一个用户组，组的名字与用户的名字相同</p>
</blockquote>
<h4 id="2-2-5-修改用户所在组：usermod"><a href="#2-2-5-修改用户所在组：usermod" class="headerlink" title="2.2.5 修改用户所在组：usermod"></a>2.2.5 修改用户所在组：usermod</h4><p><strong><em>usermod -g groupname username</em></strong></p>
<p>将username用户改到groupname组</p>
<blockquote>
<p>可以使用<code>cat /etc/passwd</code> 查看用户信息，下面是截取的一些用户信息</p>
<p>​    <code>wendell:x:1000:1000:,,,:/home/wendell:/bin/bash</code></p>
<p>其中wendell代表用户名，第一个1000代表用户id，第二个1000代表用户所在组的id</p>
</blockquote>
<h4 id="2-2-6-添加、删除用户：useradd、userdel"><a href="#2-2-6-添加、删除用户：useradd、userdel" class="headerlink" title="2.2.6 添加、删除用户：useradd、userdel"></a>2.2.6 添加、删除用户：useradd、userdel</h4><p>useradd：</p>
<ul>
<li><code>-d</code>：指定创建用户的主目录，如果不使用这个，则系统自动在<code>/home/</code>目录下创建与用户名同名的主目录</li>
<li><code>-m</code>：自动创建主目录</li>
<li><code>-g</code>：指定组名称</li>
<li><code>-s</code> ：指定一个shell，如<code>/bin/bash</code></li>
</ul>
<blockquote>
<p>每个用户必须有一个主目录</p>
<p>adduser是一个脚本命令，使用起来简单很多，但是这个脚本不能创建带有大小字母的用户名</p>
</blockquote>
<p>userdel：</p>
<ul>
<li><code>-r</code>：在删除用户时同时删除主目录</li>
</ul>
<h4 id="2-2-7-设定用户密码：passwd"><a href="#2-2-7-设定用户密码：passwd" class="headerlink" title="2.2.7 设定用户密码：passwd"></a>2.2.7 设定用户密码：passwd</h4><p><strong><em>passwd username</em></strong></p>
<h4 id="2-2-8-修改文件权限：chmod"><a href="#2-2-8-修改文件权限：chmod" class="headerlink" title="2.2.8 修改文件权限：chmod"></a>2.2.8 修改文件权限：chmod</h4><p><strong><em>chmod u/g/o/a+/-rwx file</em></strong></p>
<p><strong><em>chmod u/g/o/a=rwx file</em></strong></p>
<p><strong><em>chmod 三位数字代码 file</em></strong></p>
<blockquote>
<p>数字法也可以使用加减号来操作</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod -001 filename</span><br><span class="line">chmod +001 filename</span><br></pre></td></tr></table></figure>
</blockquote>
<h4 id="2-2-9-修改文件所属：chown、chgrp"><a href="#2-2-9-修改文件所属：chown、chgrp" class="headerlink" title="2.2.9 修改文件所属：chown、chgrp"></a>2.2.9 修改文件所属：chown、chgrp</h4><p><strong><em>chown username file</em></strong></p>
<p><strong><em>chgrp groupname file</em></strong></p>
<hr>
<h3 id="2-3-系统管理"><a href="#2-3-系统管理" class="headerlink" title="2.3. 系统管理"></a>2.3. 系统管理</h3><h4 id="2-3-1-查看系统日历：cal"><a href="#2-3-1-查看系统日历：cal" class="headerlink" title="2.3.1 查看系统日历：cal"></a>2.3.1 查看系统日历：cal</h4><ul>
<li><code>cal</code>：可查看日历，可指定具体地日期</li>
<li><code>date</code>：可以显示现在的时间</li>
</ul>
<h4 id="2-3-2-静态查看进程信息：ps"><a href="#2-3-2-静态查看进程信息：ps" class="headerlink" title="2.3.2 静态查看进程信息：ps"></a>2.3.2 静态查看进程信息：ps</h4><p>可以理解成Windows中的人物管理器，进程卡死时可能会用到这个命令</p>
<p>这个命令的选项可以不加<code>-</code>，也可以加<code>-</code></p>
<ul>
<li><code>-a</code>：显示终端上运行的所有进程，包括其他用户运行的进程</li>
<li><code>-u</code>：显示进程的详细状态</li>
<li><code>-x</code>：显示没有控制终端的进程</li>
<li><code>-w</code>：显示加宽，以便显示更多的信息</li>
<li><code>-r</code>：只显示正在运行的进程</li>
</ul>
<p>下面是使用<code>ps -aux</code> 后显示的信息</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND</span><br><span class="line">root         1  0.0  0.2  37592  5592 ?        Ss    2019   0:12 &#x2F;sbin&#x2F;init splash</span><br></pre></td></tr></table></figure>

<p>字面意思，第一个root表示这个进程是root用户执行的；PID表示进程号；CPU表示CPU占用率；MEM表示内存占用率；VSZ表示占用的虚拟内存的大小，申请内存值；RSS表示占用的内存大小，实际占用的物理内存；TTY表示终端的次要装置号码；STAT表示进程的状态；START表示进程开始时间；TIME表示执行的时间；COMMAND表示执行的指令。</p>
<p>下面对表示状态的字母进行一个解读：</p>
<ul>
<li>D：不可中断的静止</li>
<li>R：正在执行</li>
<li>S：静止状态</li>
<li>T：暂停执行</li>
<li>Z：不存在但暂时无法消除</li>
<li>W：没有足够的内存分页可以分配</li>
<li>&lt;：高优先序的进程</li>
<li>N：低优先序的进程</li>
<li>L：有内存分页分配并锁在内存中</li>
</ul>
<h4 id="2-3-3-动态显示进程：top"><a href="#2-3-3-动态显示进程：top" class="headerlink" title="2.3.3 动态显示进程：top"></a>2.3.3 动态显示进程：top</h4><p>动态显示进程，<code>q</code> 退出显示</p>
<h4 id="2-3-4-终止进程：kill"><a href="#2-3-4-终止进程：kill" class="headerlink" title="2.3.4 终止进程：kill"></a>2.3.4 终止进程：kill</h4><p><strong><em>kill PID</em></strong>：PID就是查看到的进程号</p>
<ul>
<li><code>-9</code>：强制杀死进程</li>
</ul>
<h4 id="2-3-5-后台执行：-、jobs、fg"><a href="#2-3-5-后台执行：-、jobs、fg" class="headerlink" title="2.3.5 后台执行：$、jobs、fg"></a>2.3.5 后台执行：$、jobs、fg</h4><p>在执行的命令后面加上一个<code>$</code> 符号之后，程序会在后台执行，终端依旧可以使用</p>
<p>在执行过程中也可以使用快捷键<code>ctrl+z</code> 切换到后台执行，使用<strong><em>jobs</em></strong>命令可以查看后台正在运行的程序，使用<strong><em>fg id</em></strong> 可以重新打开后台命令，其中<code>id</code>指的是使用<code>jobs</code>查看到的后台程序前的id</p>
<h4 id="2-3-6-查看大小命令：df、du"><a href="#2-3-6-查看大小命令：df、du" class="headerlink" title="2.3.6 查看大小命令：df、du"></a>2.3.6 查看大小命令：df、du</h4><ul>
<li><p><code>df pathname</code>：查看目录所挂载分区的目录的使用情况</p>
</li>
<li><p><code>du pathname</code>：查看目录、文件所占用的磁盘空间</p>
</li>
<li><p><code>-h</code>：以更人性化的方式查找</p>
</li>
</ul>
<h4 id="2-3-7-查看或配置网络信息：ifconfig"><a href="#2-3-7-查看或配置网络信息：ifconfig" class="headerlink" title="2.3.7 查看或配置网络信息：ifconfig"></a>2.3.7 查看或配置网络信息：ifconfig</h4><p>使用<code>ifconfig</code> 能够查看一些本机的网络信息，当然，一些修改一些网络配置可能也需要使用此命令</p>
<h4 id="2-3-8-ping"><a href="#2-3-8-ping" class="headerlink" title="2.3.8 ping"></a>2.3.8 ping</h4><p>这个命令和Windows中的ping命令是一样的，能够ping通表示能够给对方发送信息，但不代表对方能给你发送信息，要相互能给ping通才可以双向通信</p>
<h4 id="2-3-9-挂载、卸载：mount-umount"><a href="#2-3-9-挂载、卸载：mount-umount" class="headerlink" title="2.3.9 挂载、卸载：mount umount"></a>2.3.9 挂载、卸载：mount umount</h4><p>挂载U盘命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo mount deviceName dirName</span><br></pre></td></tr></table></figure>

<p>卸载目录命令，但是卸载时你不能在你那个目录中</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo umount dirName</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-Ubuntu"><a href="#3-Ubuntu" class="headerlink" title="3. Ubuntu"></a>3. Ubuntu</h2><h4 id="3-1-软件"><a href="#3-1-软件" class="headerlink" title="3.1 软件"></a>3.1 软件</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install software  <span class="comment">#下载</span></span><br><span class="line">sudo apt-get remove software <span class="comment">#移除</span></span><br><span class="line">sudo apt-get update <span class="comment">#更新软件列表</span></span><br><span class="line">sudo apt-get clean <span class="comment">#清理所有安装包</span></span><br><span class="line">sudo apt-get upgrade <span class="comment">#更新软件</span></span><br></pre></td></tr></table></figure>

<p>deb包安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo dpkg -i *.deb	<span class="comment">#安装</span></span><br><span class="line">sudo dpkg -r sublime-text <span class="comment">#卸载</span></span><br></pre></td></tr></table></figure>

<p>源码安装：看readme文件一般可以找到安装方法</p>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 进程管理</title>
    <url>/2020/07/20/Linux%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<p>本文主要记录Linux进程管理的一些概念和命令。</p>
<a id="more"></a>

<h1 id="1-终端"><a href="#1-终端" class="headerlink" title="1. 终端"></a>1. 终端</h1><p>Linux下有7个终端 tty1 - tty7，tty7是图形界面终端，tty1 - tty6都是命令行终端。</p>
<ul>
<li><p>在不同终端之间切换使用命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctrl + alt + F1~F7</span><br></pre></td></tr></table></figure>



</li>
</ul>
<h1 id="2-进程"><a href="#2-进程" class="headerlink" title="2. 进程"></a>2. 进程</h1><h2 id="2-1-ps"><a href="#2-1-ps" class="headerlink" title="2.1 ps"></a>2.1 ps</h2><ul>
<li><p><code>-a</code> : 列出当前所有的用户</p>
</li>
<li><p><code>-u</code> ：列出详细的信息</p>
</li>
<li><p><code>-x</code>：列出不带终端的即不需要与用户进行交互的应用程序的信息</p>
</li>
<li><p>一般使用时都是使用<code>-aux</code>同时使用，然后使用管道进行筛选</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps aux | grep bash</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="2-2-kill"><a href="#2-2-kill" class="headerlink" title="2.2 kill"></a>2.2 kill</h2><ul>
<li><p><code>-9</code>： 强制杀死进程</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">kill</span> -9 PID</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="2-3-env"><a href="#2-3-env" class="headerlink" title="2.3 env"></a>2.3 env</h2><p>查看当前进程下的所有环境变量，也是经常配合管道使用</p>
<blockquote>
<p>Linux下的环境变量</p>
<p>格式：key = value</p>
<p>value可以有很多个，中间使用<code>:</code>间隔</p>
<p>key = value : value : value : value</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH=/home/wendell/bin:/home/wendell/.<span class="built_in">local</span>/bin:/usr/<span class="built_in">local</span>/sbin:/usr/<span class="built_in">local</span>/bin</span><br></pre></td></tr></table></figure>

<p>上面是我的WSL中的部分环境变量</p>
</blockquote>
<h2 id="2-4-top"><a href="#2-4-top" class="headerlink" title="2.4 top"></a>2.4 top</h2><p>相当于WIndows中的任务管理器，是动态的，但是只能看，不能终止，使用<code>ctrl + c</code>退出查看。</p>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux文件系统</title>
    <url>/2019/09/03/Linux%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<p>Linux的文件系统和Windows的文件系统有一些区别，这篇博客主要用来记录Linux的文件系统的一些知识。</p>
<a id="more"></a>

<h2 id="1-目录和路径"><a href="#1-目录和路径" class="headerlink" title="1. 目录和路径"></a>1. 目录和路径</h2><h3 id="1-1目录"><a href="#1-1目录" class="headerlink" title="1.1目录"></a>1.1目录</h3><ul>
<li>目录就是一组相关文件的集合，这个概念和Windows系统中的目录类似</li>
<li>精确一个文件的位置，可以用“路径+文件名“的方式</li>
<li>Linux中以<code>.</code>开头的文件表示隐藏文件或隐藏文件夹</li>
<li><code>..</code>表示上一级文件夹 ，<code>.</code>表示本级文件夹</li>
</ul>
<h3 id="1-2-路径"><a href="#1-2-路径" class="headerlink" title="1.2 路径"></a>1.2 路径</h3><ul>
<li>绝对路径：就是以根目录为参考写的文件路径，以<code>/</code>开始</li>
<li>相对路径：就是以当前所在目录为参考写的文件路径</li>
<li>一个文件的路径可以写成绝对路径与相对路径两种形式</li>
</ul>
<hr>
<h2 id="2-Linux文件系统"><a href="#2-Linux文件系统" class="headerlink" title="2.Linux文件系统"></a>2.Linux文件系统</h2><blockquote>
<p>文件系统这一块的知识难记住，但是多装几次系统之后就可以记住的差不多了</p>
</blockquote>
<ul>
<li>Windows以盘符的方式管理，Linux以目录方式进行管理。但是两者都是树状的。</li>
<li>FHS标准<ul>
<li><code>/</code> 根目录：一般根目录只放根目录，一个Linux系统有且只有一个根目录，所有的文件都是从根目录开始的</li>
<li><code>/bin</code> <code>/usr/bin</code>：存放可执行二进制文件的目录</li>
<li><code>/home</code>：系统默认的用户目录 </li>
<li><code>/boot</code>：启动时的引导目录</li>
<li><code>/lib</code>：存放系统使用的函数库的目录，程序执行过程中，需要调用一些额外的参数时需要库函数的协助</li>
<li><code>/usr/incloud</code>:存放头文件的目录</li>
</ul>
</li>
<li>Linux系统中的一切皆是文件</li>
</ul>
<hr>
<h2 id="3-文件分类"><a href="#3-文件分类" class="headerlink" title="3.文件分类"></a>3.文件分类</h2><ul>
<li>普通文件：用<code>-</code>表示，就是普通的文件，比如一段视频，一段音频等</li>
<li>目录文件：用<code>d</code>表示，可以理解成Windows中的文件夹</li>
<li>设备文件：用<code>c</code>表示字符设备，用<code>b</code> 表示块设备，将每一个设备抽象成文件，比如鼠标键盘都是输入文件。一般在<code>/dev</code>目录下可以看到设备文件</li>
<li>链接文件 ：用你<code>l</code>表示，可以理解为WIndows中的快捷方式，还可以理解成一个指针。链接文件可以分为软连接和硬链接</li>
<li>管道文件：比较特殊，多用于进程中的通信</li>
</ul>
<hr>
<h2 id="4-文件权限"><a href="#4-文件权限" class="headerlink" title="4.文件权限"></a>4.文件权限</h2><ul>
<li>ls -l [pathname] 文件的具体属性，开头的10个字母表示文件的类型和权限，其中第一个字母表示我文件的类型，具体表示见上一条的内容，后9个字母被分为3组，分别表示所有者，所在组，其他组。每组权限包括可读、可写、可执行三种。显示的信息后面的两个名字表示文件的所有者和所有组。后面的数字表示文件的大小，以字节表示。再后面的就是文件最后一次修改的日期。</li>
<li><code>r</code> :读权限</li>
<li><code>w</code> :写权限</li>
<li><code>x</code> :执行权限   </li>
</ul>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux远程操作</title>
    <url>/2019/09/14/Linux%E8%BF%9C%E7%A8%8B%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<p>在很多时候需要使用其他物理机来远程操作另一台Linux主机，这个时候就需要使用Linux的远程操作命令</p>
<a id="more"></a>

<h2 id="1-SSH远程连接"><a href="#1-SSH远程连接" class="headerlink" title="1. SSH远程连接"></a>1. SSH远程连接</h2><blockquote>
<p>SSH是Secure Shell的缩写，是一种较为可靠的，专为远程登录会话和其他网络服务提供安全性的协议。SSH的客户端可以在几乎所有类Unix平台上使用</p>
</blockquote>
<h3 id="1-1-安装SSH"><a href="#1-1-安装SSH" class="headerlink" title="1.1 安装SSH"></a>1.1 安装SSH</h3><p>对于ubuntu系统来说，使用如下的命令就可以开启SSH连接</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install openssh-client</span><br><span class="line">sudo apt-get install openssh-server</span><br></pre></td></tr></table></figure>

<p>安装好之后可以使用如下命令来确定是否开启ssh-server</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps -e | grep ssh</span><br></pre></td></tr></table></figure>

<p>当输出结果出现<code>sshd</code> 的进程时，表示ssh服务已经开启，如果没有，使用下面的命令可以启动</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo /etc/init.d/ssh start</span><br></pre></td></tr></table></figure>

<p>同理可以使用类似命令停止和重启ssh服务</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo /etc/init.d/ssh stop</span><br><span class="line">sudo /etc/init.d/ssh restart</span><br></pre></td></tr></table></figure>

<h3 id="1-2-SSH远程登录"><a href="#1-2-SSH远程登录" class="headerlink" title="1.2 SSH远程登录"></a>1.2 SSH远程登录</h3><h4 id="1-2-1-口令登录"><a href="#1-2-1-口令登录" class="headerlink" title="1.2.1 口令登录"></a>1.2.1 口令登录</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh name@ip_addr <span class="comment">#或者下面的</span></span><br><span class="line">ssh -l name ip_addr</span><br></pre></td></tr></table></figure>

<p>使用上述命令就可以登录了</p>
<ul>
<li><code>-X</code>：调用图形界面</li>
<li><code>-p</code>：指定端口登录</li>
</ul>
<h4 id="1-2-2-秘钥登录"><a href="#1-2-2-秘钥登录" class="headerlink" title="1.2.2 秘钥登录"></a>1.2.2 秘钥登录</h4><p>这种登录方式是更为方便的，不用每次登录都输入密码</p>
<p>这种方法的步骤如下：</p>
<ul>
<li>首先在被连接的Linux主机上输入<code>ssh-keygen -t rsa</code> ，生成秘钥，这里的<code>-t rsa</code>表示使用<code>rsa</code>的加密算法</li>
<li>然后在当前用户的家目录下可以看到一个<code>.ssh</code>文件夹，下面有私钥文件<code>id_rsa</code> 和公钥文件<code>id_rsa.pb</code></li>
<li>将公钥文件的内容复制到远程操作的主机的<code>~/.ssh/authorized_key</code>文件中，可以使用复制，也可以使用命令<code>ssh-copy-id name@ip_addr</code>来实现这个功能</li>
</ul>
<h3 id="1-3-远程传输文件"><a href="#1-3-远程传输文件" class="headerlink" title="1.3 远程传输文件"></a>1.3 远程传输文件</h3><p>使用<code>scp</code>文件可以实现远程操作时的文件拷贝功能</p>
<p>将本地文件拷贝到远程</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scp filename RemoteUsername@RemoteIP:RemoteFile</span><br><span class="line">scp filename RemoteIP:RemoteFolder</span><br><span class="line">scp filename RemoteIP:RemoteFile</span><br></pre></td></tr></table></figure>

<p>远程文件拷贝到本地</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scp RemoteUsername@RemoteIP:RemoteFile filename</span><br><span class="line">scp RemoteIP:RemoteFile Filename</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：当传输文件改成目录时，使用 <code>-r</code> 参数</p>
</blockquote>
<p><font size=2>参考<a href="https://blog.csdn.net/li528405176/article/details/82810342" target="_blank" rel="noopener">Jack LDZ的博文：SSH简介及两种远程登录的方法</a></font></p>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>ssh</tag>
      </tags>
  </entry>
  <entry>
    <title>C++范围循环</title>
    <url>/2020/05/21/C++%E8%8C%83%E5%9B%B4%E5%BE%AA%E7%8E%AF/</url>
    <content><![CDATA[<p>范围循环用来遍历容器里的对象, C++11及以后的特性</p>
<a id="more"></a>

<ul>
<li><p>格式</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">typename</span> object: container)</span><br><span class="line">&#123;</span><br><span class="line">    loop body</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ForEach</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; values, <span class="keyword">void</span>(*func)(<span class="keyword">int</span>))</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> value : values) <span class="comment">//范围循环</span></span><br><span class="line">    &#123;</span><br><span class="line">        func(value);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintValue</span><span class="params">(<span class="keyword">int</span> value)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; value &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; values = &#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span> &#125;;</span><br><span class="line">    ForEach(values, PrintValue);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>比较常用的格式</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">auto</span> a: ai)</span><br><span class="line">    ;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">auto</span> &amp;a: ai)</span><br><span class="line">    ;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">auto</span> <span class="keyword">const</span> &amp;a : ai)</span><br><span class="line">    ;</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Verilog语法(1)</title>
    <url>/2020/01/15/Verilog%E8%AF%AD%E6%B3%95(1)/</url>
    <content><![CDATA[<p>之前记录的Verilog语法规则在电脑系统崩掉之后重装时全部丢失了，所以这里重新记录一下Verilog的基础语法以及在实战中的一些运用方法。</p>
<a id="more"></a>

<h1 id="1-电路设计的语法"><a href="#1-电路设计的语法" class="headerlink" title="1. 电路设计的语法"></a>1. 电路设计的语法</h1><h2 id="1-1-设计不使用的语法"><a href="#1-1-设计不使用的语法" class="headerlink" title="1.1 设计不使用的语法"></a>1.1 设计不使用的语法</h2><ul>
<li><code>initial</code>：设计时不使用，仿真时使用</li>
<li><code>task/function</code>：设计时不使用，仿真时很少用</li>
<li><code>for/while/repeat/forever</code>：设计不用，仿真很少用</li>
<li><code>integer</code>：设计不用</li>
<li>模块内部不能有X态、Z态，内部不能有三态接口</li>
<li><code>casex/casez</code>：设计和仿真都不用</li>
<li><code>force/wait/fork</code>：设计不用，仿真很少用</li>
<li><code>#5</code>：延时语句，设计不用，仿真常用</li>
</ul>
<h2 id="1-2-设计时使用的语法"><a href="#1-2-设计时使用的语法" class="headerlink" title="1.2 设计时使用的语法"></a>1.2 设计时使用的语法</h2><ul>
<li><code>reg/wire、parameter</code>：定义参数的语法</li>
<li><code>assign、always</code>：很常用的语法</li>
<li>一般只用<code>if else</code>和<code>case</code> 两种条件语句</li>
<li><code>+、-、*、/、%</code>：算术运算符可以使用</li>
<li><code>=，&lt;=</code>：赋值运算符，时序逻辑用<code>&lt;=</code>，组合逻辑用<code>=</code></li>
<li>关系运算符：<code>&gt;，&lt;，&gt;=，&lt;=</code></li>
<li>逻辑运算符：<code>&amp;&amp;，||，!</code></li>
<li>位运算符：<code>~，|，^，&amp;</code></li>
<li>移位运算符：<code>&lt;&lt;，&gt;&gt;</code></li>
<li>拼接运算符：<code>{}</code></li>
</ul>
<h1 id="2-电路设计的结构"><a href="#2-电路设计的结构" class="headerlink" title="2. 电路设计的结构"></a>2. 电路设计的结构</h1><ul>
<li><p>组合逻辑：</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">always</span> @ (*) <span class="keyword">begin</span></span><br><span class="line">	expression</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>同步复位时序逻辑</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">always</span> @ (<span class="keyword">posedge</span> clk) <span class="keyword">begin</span></span><br><span class="line">	<span class="keyword">if</span>(rst_n == <span class="number">1'b0</span>) <span class="keyword">begin</span></span><br><span class="line">		expression</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">begin</span></span><br><span class="line">		expression</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>异步复位时序逻辑</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">always</span> @ (<span class="keyword">posedge</span> clk <span class="keyword">or</span> <span class="keyword">negedge</span> rst_n) <span class="keyword">begin</span></span><br><span class="line">	<span class="keyword">if</span>(rst_n == <span class="number">1'b0</span>) <span class="keyword">begin</span></span><br><span class="line">		expression</span><br><span class="line">	<span class="keyword">else</span> <span class="keyword">begin</span></span><br><span class="line">		expression</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="3-电路设计要点"><a href="#3-电路设计要点" class="headerlink" title="3. 电路设计要点"></a>3. 电路设计要点</h1><ul>
<li>一个<code>always</code> 只产生一个信号：设计清晰简洁方便调试</li>
<li>一个信号只在一个<code>always</code>中产生</li>
<li><code>always</code> 模块要考虑到全部的情况</li>
<li>条件判断只用<code>if else</code>或者<code>case</code></li>
<li>含有<code>posedge</code>或者<code>negedge</code>的一定是时序电路</li>
<li>即改即用使用组合逻辑，延时改变使用时序逻辑</li>
</ul>
<h1 id="4-模块例化"><a href="#4-模块例化" class="headerlink" title="4.模块例化"></a>4.模块例化</h1><p>例化可以简单理解为编程语言中的函数功能（但是实际上并不一样），当有一个模块被例化两次之后，这两个模块都占有实际的电路结构，虽然这两个功能都有相同的功能，并不会共用一套电路，编程语言中的函数是随用随分配，这是例化和函数的一个区别</p>
<ul>
<li><p>例化的方法：</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="comment">//设计模块</span></span><br><span class="line"><span class="keyword">module</span> uart(</span><br><span class="line">	clk,</span><br><span class="line">	rst_n,</span><br><span class="line">	vld_in,</span><br><span class="line">	data_in,</span><br><span class="line">	uart_out,</span><br><span class="line">	uart_in,</span><br><span class="line">	vld_out,</span><br><span class="line">	data_out,</span><br><span class="line">	rdy_in</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">//例化方法：  </span></span><br><span class="line">uart u1(                <span class="comment">//例化的模块名，实例的模块名</span></span><br><span class="line">	<span class="variable">.clk</span>(clk_100M),</span><br><span class="line">	<span class="variable">.rst_n</span>(sys_rst_n),</span><br><span class="line">	<span class="variable">.vld_in</span>(bt_data_out_vld),</span><br><span class="line">	<span class="variable">.data_in</span>(bt_data_out),</span><br><span class="line">	<span class="variable">.uart_out</span>(uart_tx),</span><br><span class="line">	<span class="variable">.uart_in</span>(uart_rx),</span><br><span class="line">	<span class="variable">.vld_out</span>(uart_data_out_vld),</span><br><span class="line">	<span class="variable">.data_out</span>(uart_data_out),</span><br><span class="line">	<span class="variable">.rdy_in</span>(uart_in_rdy)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
</li>
<li><p>参数的例化</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="comment">//设计模块</span></span><br><span class="line"><span class="keyword">module</span> uart(</span><br><span class="line">	clk,</span><br><span class="line">	rst_n,</span><br><span class="line">	vld_in,</span><br><span class="line">	data_in,</span><br><span class="line">	uart_out,</span><br><span class="line">	uart_in,</span><br><span class="line">	vld_out,</span><br><span class="line">	data_out,</span><br><span class="line">	rdy_in</span><br><span class="line">);</span><br><span class="line"><span class="keyword">parameter</span> DATA_W = <span class="number">8</span>;</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line"><span class="comment">//例化方法：  </span></span><br><span class="line">    uart<span class="variable">#(.DATA_W(16))</span> u1(                <span class="comment">//例化的模块名，实例的模块名,这里将模块的8位位宽改成了16位的位宽</span></span><br><span class="line">	<span class="variable">.clk</span>(clk_100M),</span><br><span class="line">	<span class="variable">.rst_n</span>(sys_rst_n),</span><br><span class="line">	<span class="variable">.vld_in</span>(bt_data_out_vld),</span><br><span class="line">	<span class="variable">.data_in</span>(bt_data_out),</span><br><span class="line">	<span class="variable">.uart_out</span>(uart_tx),</span><br><span class="line">	<span class="variable">.uart_in</span>(uart_rx),</span><br><span class="line">	<span class="variable">.vld_out</span>(uart_data_out_vld),</span><br><span class="line">	<span class="variable">.data_out</span>(uart_data_out),</span><br><span class="line">	<span class="variable">.rdy_in</span>(uart_in_rdy)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="5-运算符"><a href="#5-运算符" class="headerlink" title="5. 运算符"></a>5. 运算符</h1><h2 id="5-1-信号类型reg-wire"><a href="#5-1-信号类型reg-wire" class="headerlink" title="5.1 信号类型reg/wire"></a>5.1 信号类型reg/wire</h2><ul>
<li>设计代码仅用<code>reg</code>和<code>wire</code></li>
<li>由本模块且是由<code>always</code>产生的信号，都用<code>reg</code></li>
<li>测试代码中，用<code>initial</code>产生的信号（一般是对测模块的输入），用<code>reg</code>类型</li>
<li>其他的都用<code>wire</code></li>
<li><code>reg</code>类型的信号，也不一定是产生寄存器的（比如组合逻辑中的一些信号，也是由<code>always</code>产生的，也是用<code>reg</code>，但是这里没有寄存器）</li>
</ul>
<h2 id="5-2-参数parameter"><a href="#5-2-参数parameter" class="headerlink" title="5.2 参数parameter"></a>5.2 参数parameter</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">parameter</span> CNT = <span class="number">12</span>;</span><br></pre></td></tr></table></figure>

<p>可以简单理解为C语言中的宏定义，例如上述代码的作用就是将整个模块中的<code>CNT</code>全部替换成12</p>
<h2 id="5-3-算术运算符"><a href="#5-3-算术运算符" class="headerlink" title="5.3 算术运算符"></a>5.3 算术运算符</h2><ul>
<li><code>+、-、*、/、%</code>：一般除法和取余比较少于，因为它们涉及到的逻辑比较复杂</li>
</ul>
<h2 id="5-4-关系运算符"><a href="#5-4-关系运算符" class="headerlink" title="5.4 关系运算符"></a>5.4 关系运算符</h2><p><code>&gt;=，&lt;=，==，&gt;，&lt;</code>：一般用于条件判断</p>
<h2 id="5-5-赋值运算符"><a href="#5-5-赋值运算符" class="headerlink" title="5.5 赋值运算符"></a>5.5 赋值运算符</h2><ul>
<li><code>&gt;=</code>：时序逻辑里使用</li>
<li><code>=</code>：组合逻辑里使用</li>
</ul>
<h2 id="5-6-逻辑运算符"><a href="#5-6-逻辑运算符" class="headerlink" title="5.6 逻辑运算符"></a>5.6 逻辑运算符</h2><ul>
<li><code>&amp;&amp;</code>：与</li>
<li><code>||</code>：或</li>
<li><code>!</code>：非</li>
<li>一般来说两边都是1bit，多用于条件判断</li>
</ul>
<h2 id="5-7-位运算符"><a href="#5-7-位运算符" class="headerlink" title="5.7 位运算符"></a>5.7 位运算符</h2><ul>
<li><code>&amp;</code>：按位与</li>
<li><code>|</code>：按位或</li>
<li><code>~</code>：按位取反</li>
<li><code>^</code>：按位异或</li>
<li>一般来说用于赋值运算</li>
</ul>
<h2 id="5-8-移位运算符"><a href="#5-8-移位运算符" class="headerlink" title="5.8 移位运算符"></a>5.8 移位运算符</h2><ul>
<li><code>&gt;&gt;</code>：右移</li>
<li><code>&lt;&lt;</code>：左移</li>
<li>一般用于乘除运算</li>
</ul>
<h2 id="5-9-拼接运算符"><a href="#5-9-拼接运算符" class="headerlink" title="5.9 拼接运算符"></a>5.9 拼接运算符</h2><ul>
<li><code>{}</code>：将不同bit的数拼接在一起</li>
<li><code>{3{a[0]}}</code>：表示<code>a[0]a[0]a[0]</code></li>
</ul>
]]></content>
      <tags>
        <tag>Verilog</tag>
      </tags>
  </entry>
  <entry>
    <title>Verilog语法(2)</title>
    <url>/2020/02/28/Verilog%E8%AF%AD%E6%B3%95(2)/</url>
    <content><![CDATA[<p>本篇主要记录Verilog语言一些我之前没有了解到的知识。</p>
<a id="more"></a>

<ul>
<li><p>连续赋值语句</p>
<p><code>assign [drive strength] [delay3] list_of_net_assignments</code> 中括号内为可以省略的，其中驱动强度默认为<code>strong0</code>和<code>strong1</code>。延迟类似于门级延迟</p>
<p>赋值延迟有三种方式：</p>
<ul>
<li><p>普通赋值延迟：<code>assign #delay_time out = i1&amp;i2;</code>，在这个句子中，每当<code>i1</code>或者<code>i2</code>有一个变化时，在经过延迟之后再对<code>out</code>进行赋值，但是注意：进行赋值时取的是实时值，而不是刚发生变化时的值，换句话说，当脉冲时间小于延迟时间，就不产生变化</p>
</li>
<li><p>隐式连续赋值延迟：</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">wire</span> #<span class="number">10</span> out = i1 &amp; i2;</span><br><span class="line"><span class="comment">//等价于</span></span><br><span class="line"><span class="keyword">wire</span> out;</span><br><span class="line"><span class="keyword">assign</span> #<span class="number">10</span> out = i1 &amp; i2;</span><br></pre></td></tr></table></figure>
</li>
<li><p>线网声明延迟</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">wire</span> #<span class="number">10</span> out;</span><br><span class="line"><span class="keyword">assign</span> out = i1 &amp; i2;</span><br><span class="line"><span class="comment">//等价于</span></span><br><span class="line"><span class="keyword">wire</span> #<span class="number">10</span> out = i1 &amp; i2;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>门级延迟的使用格式:<code>#(rise_min:rise_typ:rise_max,fall_min:fall_typ:fall_max,turnoff_min:turnoff_typ:turnoff_max)</code></p>
<p>举例</p>
<p><code>fulladder#(1:2:3,2:3:4,3:4:5) u0(port map);</code></p>
</li>
<li><p>非阻塞赋值<code>&lt;=</code>的赋值逻辑：并行执行，当产生变化时，仿真器将暂存右边的变量，然后当达到仿真时刻时就可以将右侧的值赋值给左侧</p>
<p>举例</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">always</span> @ (<span class="keyword">posedge</span> clk)</span><br><span class="line">	a = b;</span><br><span class="line"><span class="keyword">always</span> @ (<span class="keyword">posedge</span> clk)</span><br><span class="line">	b = a;</span><br><span class="line">	</span><br><span class="line"><span class="keyword">always</span> @ (<span class="keyword">posedge</span> clk)</span><br><span class="line">	a &lt;= b;</span><br><span class="line"><span class="keyword">always</span> @ (<span class="keyword">posedge</span> clk)</span><br><span class="line">	b &lt;= a;</span><br></pre></td></tr></table></figure>

<p>如果理解了逻辑，就可以发现第一种模式将产生竞争，最后两个变量会得到相同的值</p>
<p>第二种模式方可达到交换两变量值的目的</p>
<blockquote>
<p>应注意：连续赋值语句使用延时时，到了赋值时刻赋予左值的是右值得实时值，而不是语句书写时的值</p>
<p>而非阻塞赋值延时时，赋予的是赋值语句书写时的值</p>
</blockquote>
</li>
<li><p>过程语句赋值延迟和连续赋值延迟的问题</p>
<p>连续语句赋值的问题前面提到了，当使用<code>assign #delay_time commmad</code>的格式时，取的操作值是实时值，而在过程赋值中，延迟方式比较多变</p>
<ul>
<li>当使用<code>#delay_time command</code>，则是在延迟之后再执行操作</li>
<li>当使用<code>a &lt;= #10 in</code>的非阻塞式赋值时或者使用<code>a = #10 in</code>阻塞式赋值时，都是暂存变量，然后再等待10个单位之后，将暂存的值赋予左值，而不是像连续赋值一样实时赋值</li>
<li>同理可以延伸，在过程赋值语句中<code>a = @(posedge clk) b</code>就是在发生时立即计算b的值暂存，然后等满足条件了（也就是等来了时钟的上升沿）就将值赋给a</li>
</ul>
</li>
<li><p>事件控制: <strong>event</strong></p>
<p>可以定义一个事件，通过条件控制它的发生，然后根据它的变化执行其他操作，用<code>-&gt;</code>来触发事件，用<code>@</code>来接收事件</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">event</span> receive_data;</span><br><span class="line"></span><br><span class="line"><span class="keyword">always</span> @ (<span class="keyword">posedge</span> clk)</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">	<span class="keyword">if</span>(last_data_packet)</span><br><span class="line">		-&gt; receive_data;</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">always</span> @ (receive_data)</span><br><span class="line">	data_r = data;</span><br></pre></td></tr></table></figure>

<p>其实这种操作，可以使用类似于<code>flag</code>变量的形式来实现，可以不使用<code>event</code>的用法，可达到同样的目的</p>
</li>
<li><p>电平敏感控制：<strong>wait</strong></p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">always</span></span><br><span class="line">	<span class="keyword">wait</span>(enable) a &lt;= b;</span><br></pre></td></tr></table></figure>

<p>这种功能经常使用选择分支来实现，而不是例子中的这种</p>
</li>
<li><p><code>case</code>和、<code>casex</code>、<code>casez</code></p>
<ul>
<li><code>case</code>下的表达是可以使用<code>x</code>和<code>z</code>这两个值的</li>
<li><code>casex</code>和<code>casez</code>分别表示下面条件的<code>x</code>和<code>z</code>表示无关位，用<code>?</code>来理解</li>
</ul>
</li>
<li><p>顺序块和并行快</p>
<ul>
<li>使用<code>begin</code> <code>end</code>的就是顺序块</li>
<li>使用<code>fork</code> <code>join</code>的是并行块</li>
<li>顺序块中所有的语句是顺序执行的，除非有时序控制或者非阻塞赋值模块</li>
<li>并行块中所有语句是并发执行的，并且可以使用时序控制语句来控制执行的顺序，因此，并行块中语句无顺序要求</li>
</ul>
</li>
<li><p>块语句的特点</p>
<ul>
<li><p>嵌套块：并行块和顺序块可以混合使用</p>
</li>
<li><p>命名块和禁止命名块</p>
<p>可以给一个块取名，可以在块内定义局部变量，然后可以通过层次名来取其他块中的局部变量</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> top;</span><br><span class="line"></span><br><span class="line"><span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">	<span class="keyword">integer</span> i;</span><br><span class="line">	<span class="keyword">begin</span>: block1</span><br><span class="line">		<span class="keyword">integer</span> j; <span class="comment">//可以通过top.block1.j的方式来调用这个变量</span></span><br><span class="line">		...</span><br><span class="line">		...</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"><span class="comment">//下面举个嵌套块的例子</span></span><br><span class="line"><span class="keyword">initial</span> </span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">	x = <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">fork</span> </span><br><span class="line">		#<span class="number">5</span> y = <span class="number">1</span>;</span><br><span class="line">		#<span class="number">10</span> z = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">join</span></span><br><span class="line">	#<span class="number">20</span> x = &#123;y,z&#125;;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>可以使用<code>disable block1;</code>的方式禁用<code>block1</code></p>
</li>
</ul>
</li>
<li><p>生成块：在需要重复操作或者不确定模块中是否需要某个块时，可以使用生成块<code>generate</code>  <code>endgenerate</code></p>
<ul>
<li><p>可以使用<code>net reg integer real time realtime event</code></p>
</li>
<li><p>使用生成块可以极大地加快工作效率</p>
</li>
<li><p>控制生成块生成的循环计算，可以使用<code>genvar</code>声明一个临时变量</p>
</li>
<li><p>使用生成块多是结合循环语句、条件语句、case语句来使用的</p>
</li>
</ul>
</li>
<li><p>理解<code>@</code>符号，<code>@</code>符号可以理解为等待<code>@</code>后面的事件，再执行接下来的操作，比如<code>@(posedge clk) a = b;</code>就是指等到时钟上升沿来时，将b的值赋给a。<code>repeat(2) (@posedge clk);</code> <code>repeat</code>就是指重复两次后面的操作<code>@(posedge clk)</code>，而这个操作又是指等待上升沿到来时执行其后的语句，而这里的语句是空语句，所以这句话最终起到的作用就是等待两个时钟周期</p>
</li>
<li><p>task和function</p>
<ul>
<li><p>区别：task可以理解为普通的Verilog代码，可以包含时序逻辑，function只可以是纯组合逻辑；task可以调用其他task和function，function只能调用function；task可以有input、inout、output，function必须有一个及以上的input，不能有输出，但是可以返回一个值</p>
</li>
<li><p>task：在同一个模块中调用同一个task是对同一块地址进行操作，所以容易出错，使用<code>automatic</code>命令可以自动分配内存。<code>task automatic task_name;</code>，调用的使用格式和门语句还是很像的，port map的顺序和task中定义的顺序是一致的</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> <span class="keyword">automatic</span> task1;</span><br><span class="line"><span class="keyword">input</span> [<span class="number">3</span>:<span class="number">0</span>] a;</span><br><span class="line"><span class="keyword">input</span> [<span class="number">3</span>:<span class="number">0</span>] b;</span><br><span class="line"><span class="keyword">output</span> [<span class="number">4</span>:<span class="number">0</span>] out;</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">	#<span class="number">5</span> out = a + b;</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"><span class="keyword">endtask</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//调用</span></span><br><span class="line"><span class="keyword">initial</span> </span><br><span class="line">	taks1;</span><br></pre></td></tr></table></figure>
</li>
<li><p>function： function的使用方式和task基本是一致的，需要注意的是，function在定义时，隐式定义了一个寄存器变量，用来存放函数返回的值，这个值在函数被调用的地方被使用。同样使用<code>automatic</code>参数，可以为多次被调用的同一个函数开辟多个空间，避免错误，注意到使用递归函数的时候，就需要用到这个</p>
</li>
<li><p>函数还有其他的一些定义方法，比如<code>function integer cal(input [3:0] a);</code>可以返回一个常数；<code>function signed [63:0] cal(input [7:0] a);</code>可以返回一个位宽为64有符号数</p>
</li>
</ul>
</li>
<li><p><code>assign</code>和<code>deassign</code>：过程连续赋值，在过程语句里使用<code>assign</code>，在这个模块再次被触发之前，变量暂时被<code>assign</code>覆盖，然后等到<code>deassign</code>取消覆盖，得到它原来的值，这种方式十分糟糕，不建议使用</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">always</span> @ (<span class="keyword">negedge</span> clk)</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">	q &lt;= d;</span><br><span class="line">	q_n &lt;= ~d;</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">always</span> @ (reset)</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">	<span class="keyword">if</span>(reset) <span class="keyword">begin</span> <span class="comment">//暂时覆盖</span></span><br><span class="line">		<span class="keyword">assign</span> q &lt;= <span class="number">1'b0</span>;</span><br><span class="line">		<span class="keyword">assign</span> q_n &lt;= <span class="number">1'b1</span>;</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line">	<span class="keyword">else</span> <span class="keyword">begin</span> <span class="comment">//取消覆盖</span></span><br><span class="line">		<span class="keyword">deassign</span> q;</span><br><span class="line">		<span class="keyword">deassign</span> q_n;</span><br><span class="line">	<span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>force</code>和<code>release</code>：也是强制赋值，使用在仿真模块中，强制改变一个线网或者寄存器的值</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">	#<span class="number">50</span> <span class="keyword">force</span> q = a &amp; b;</span><br><span class="line">	#<span class="number">100</span> relaese q;</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>例化模块改变参数的另一种方法：例化的时候改变参数十分常见，前面也介绍了一种方法，通过<code>#(.id(id))</code>的方式。<code>defparam</code>也可以实现相同的功能，但是这种代码是十分糟糕的形式，不建议使用</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">defparam</span> u0<span class="variable">.id</span> = <span class="number">1</span>, u1<span class="variable">.id</span> = <span class="number">2</span>;</span><br><span class="line">cal u0(port_map);</span><br><span class="line">cal u1(port_map);</span><br></pre></td></tr></table></figure>
</li>
<li><p>`ifdef `ifndef `elsif `else `endif 这些语句和C语言的条件编译十分相似，都是找是否定义了相应的文本宏，文本宏的定义方式是`define ，上面的条件编译语句不允许使用逻辑运算</p>
</li>
<li><p>条件执行语句：只有在系统添加了相应的标志时，才会运行，否则都不运行</p>
<ul>
<li><p><code>$test$plusargs</code> ，可以指定<code>+DISPLAY_VAR</code>选项在程序运行时设置标志</p>
  <figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> test;</span><br><span class="line"><span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">    	<span class="keyword">if</span>($test$plusargs(<span class="string">"DISPLAY_VAR"</span>)) <span class="comment">//只有添加了这个标志才显示</span></span><br><span class="line"> 	 		<span class="built_in">$display</span>(<span class="string">"..."</span>);</span><br><span class="line">	  	<span class="keyword">else</span></span><br><span class="line"> 	 		$dispaly(<span class="string">"///"</span>);</span><br><span class="line">	  <span class="keyword">end</span></span><br><span class="line">	  </span><br><span class="line">	  <span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>$value$plusargs</code></p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">reg</span>[<span class="number">11</span>:<span class="number">0</span>] string_a;</span><br><span class="line"><span class="keyword">integer</span> period = <span class="number">10</span>;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">	<span class="keyword">if</span>(<span class="built_in">$value$plusargs</span>(<span class="string">"testname=s%"</span>,string_a))</span><br><span class="line">		<span class="built_in">$readmemh</span>(string_a,vectors);</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		<span class="built_in">$display</span>(<span class="string">"error"</span>);</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">if</span>($vale$plusargs(<span class="string">"clk_t=d%"</span>,period))</span><br><span class="line">		<span class="keyword">forever</span> <span class="variable">#(period/2)</span> clk = ~clk;</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		$dispaly(<span class="string">"error"</span>);</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure>

<p>要启用上述选项，用带<code>+testname = test1.vec  +clk_t = 10 testname = &quot;test1.vec&quot; period = 10</code>的命令行来启动仿真器</p>
</li>
</ul>
</li>
<li><p>时间尺度：<code>timescale 1ns/1ps</code>，这个语句在仿真时使用，但是并没有理解它的意思，其意义是时间测量单位\延迟的时间单位，只能写1、10、100，通俗的说就是参考时间单位\精度；</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="meta">`<span class="meta-keyword">timescale</span> 100ns/1ns</span></span><br><span class="line">#<span class="number">5</span> <span class="comment">//延迟500ns</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>文件操作</p>
<ul>
<li><p>打开文件：<code>fopen</code></p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">integer</span> handle1 , handle2, handle3;</span><br><span class="line"><span class="comment">//标准输出是打开的：decription = 32'h0000_0001;</span></span><br><span class="line"><span class="keyword">initial</span> </span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">	handle1 = <span class="built_in">$fopen</span>(<span class="string">"file1.out"</span>);<span class="comment">//handle1 = 32'h0000_0002;</span></span><br><span class="line">	handle2 = <span class="built_in">$fopen</span>(<span class="string">"file2.out"</span>);<span class="comment">//handle1 = 32'h0000_0004;</span></span><br><span class="line">	handle3 = <span class="built_in">$fopen</span>(<span class="string">"file3.out"</span>);<span class="comment">//handle1 = 32'h0000_0008;</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>写文件：<code>fdispaly</code> <code>fmonitor</code> <code>fwrite</code> <code>fstrobe</code></p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"><span class="keyword">integer</span> desc1, desc2;<span class="comment">//定义两个文件描述符</span></span><br><span class="line"><span class="keyword">initial</span></span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">	des1 = handle1 | <span class="number">1</span>;</span><br><span class="line">	$fdispaly(dsc1,<span class="string">"dispaly1"</span>);<span class="comment">//同时在file1和标准输出里写文件</span></span><br><span class="line">	</span><br><span class="line">	desc2 = handle3;</span><br><span class="line">	$fdispaly(desc2,<span class="string">"dispaly2"</span>);<span class="comment">//在file3里写文件</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>关闭文件：<code>fclose</code></p>
<p>关闭之后的文件就不可以写了，重新打开可能会获得另外一个通道</p>
</li>
</ul>
</li>
<li><p>显示层次的<code>m%</code>，使用输出时后面不用附加参数</p>
</li>
<li><p>在与<code>display</code>同时执行的语句中，两者执行的顺序并不明确，使用<code>strobe</code>可以确保是先进行运算，再显示</p>
</li>
<li><p>随机数生成函数</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$random</span>; </span><br><span class="line"><span class="built_in">$random</span>(seed);</span><br><span class="line"><span class="built_in">$random</span> % <span class="number">60</span>; <span class="comment">//-59到59</span></span><br><span class="line">&#123;<span class="built_in">$random</span>&#125; % <span class="number">60</span>; <span class="comment">//0到59</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>使用文件初始化存储器 <code>readmemb</code>和<code>readmemh</code></p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$readmemb</span>(<span class="string">"&lt;filename&gt;"</span>,&lt;memory_name&gt;,[&lt;start_addr&gt;],[&lt;finish_addr&gt;]);</span><br><span class="line"><span class="comment">//$readmemh使用方法一样</span></span><br></pre></td></tr></table></figure>

<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> top;</span><br><span class="line"></span><br><span class="line"><span class="keyword">reg</span>[<span class="number">7</span>:<span class="number">0</span>] memory_a [<span class="number">0</span>:<span class="number">7</span>];<span class="comment">//8 x 8存储器</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">	<span class="built_in">$readmemb</span>(<span class="string">"init.dat"</span>,memory_a);</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"><span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure>

<p>将<code>&quot;init.dat&quot;</code>文件中的内容写入到存储器中</p>
<p>文件里的格式如下</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line">@<span class="number">002</span> <span class="comment">//使用@表示文件的写入位置，使用十六进制表明，数据使用空格分开，未初始化默认为x</span></span><br><span class="line"><span class="number">00000000</span> <span class="number">00000001</span></span><br><span class="line"><span class="number">00010101</span> <span class="number">10010110</span> </span><br><span class="line"></span><br><span class="line">@<span class="number">006</span></span><br><span class="line"><span class="number">00001011</span> <span class="number">01111010</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>VCD文件转储：使用例子来说明转储文件的使用方法，主要是讲仿真信息，如输出，波形等信息转储到文件中</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">initial</span> </span><br><span class="line">	<span class="built_in">$dumpfile</span>(<span class="string">"file1.dmp"</span>);<span class="comment">//将仿真信息转储到file1.dmp中</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">initial</span> </span><br><span class="line">	<span class="built_in">$dumpvars</span>; <span class="comment">//不指定范围，则为转储所有变量信息</span></span><br><span class="line"><span class="keyword">initial</span></span><br><span class="line">	<span class="built_in">$dumpvars</span>(<span class="number">2</span>,top<span class="variable">.m2</span>);<span class="comment">//表示转储top.m2下两层的变量信息，使用0就是转储top.m2下所有变量的信息</span></span><br><span class="line">	</span><br><span class="line"><span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">	<span class="built_in">$dumpon</span>; <span class="comment">//开始转储</span></span><br><span class="line">	#<span class="number">1000000</span> <span class="built_in">$dumpoff</span>;<span class="comment">//仿真一段时间后停止</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">initial</span> </span><br><span class="line">    $dumpfall;<span class="comment">//生成一个检测点，转储所有VCD变量的当前值(执行的时刻，在这里就是仿真开始处)</span></span><br></pre></td></tr></table></figure>

<p><code>.vcd</code>格式是任何仿真器默认可以打开的，<code>.fsdb</code>也是较为常用的一种存储波形的文件格式</p>
</li>
</ul>
<p><font size=2>主要参考《Verilog+HDL数字设计与综合（第二版）》</font></p>
]]></content>
      <tags>
        <tag>Verilog</tag>
      </tags>
  </entry>
  <entry>
    <title>Verilog语法(3)</title>
    <url>/2020/03/01/Verilog%E8%AF%AD%E6%B3%95(3)/</url>
    <content><![CDATA[<p>前面记录了Verilog的设计常用语法、一些不常用的语法，这篇博客记录一些Verilog较为高级的用法</p>
<a id="more"></a>

<h3 id="时序和延迟"><a href="#时序和延迟" class="headerlink" title="时序和延迟"></a>时序和延迟</h3><ul>
<li><p>延迟模型：分布延迟，集总延迟、路径延迟（引脚到引脚的延迟）</p>
<ul>
<li><p>分布延迟：这个就是前面提到的分布元件上的延迟，比如一个逻辑门上的延迟，延迟可以使用逻辑门调用时的延迟表示，或者用连续赋值时的延迟表示</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">and</span> #<span class="number">5</span> a0(out, a, b);</span><br><span class="line"></span><br><span class="line"><span class="keyword">assign</span> #<span class="number">5</span> out = a &amp; b;</span><br></pre></td></tr></table></figure>
</li>
<li><p>集总延迟：这个就是将一个模块中的延迟全部集中到输出门上，前面的逻辑门不设延迟，表示方法和分布延迟表示类似，只是集总延迟全部表示到输出端上</p>
</li>
<li><p>路径延迟：这种延迟是最为常见的，就是表示从每个输入端到输出端的延迟，表示可以使用<code>=&gt;</code> 和<code>*&gt;</code>表示，前者表示单个引脚到引脚的延迟，后者表示源域中任意一个引脚到目标域所有引脚的延迟，延迟要使用在<code>specify  endspecify</code>块中，可以使用<code>specparam</code>参数定义延迟，还可以使用条件延迟语句表示延迟，如下面的例子（这个例子是不对的，只是为了展示多一些功能，实际代码是不能前后矛盾的)</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> top(out , a, b, c, d);</span><br><span class="line"><span class="keyword">input</span> a, b, c, d;</span><br><span class="line"><span class="keyword">output</span> out;</span><br><span class="line"></span><br><span class="line"><span class="keyword">specify</span> </span><br><span class="line">	(a=&gt;out) = <span class="number">9</span>;<span class="comment">/*并行，若是a和out都是[1:0],则这个句子等价于(a[0]=&gt;out[0]) = 9;(a[1]=&gt;out[1]) = 9;*/</span></span><br><span class="line">	</span><br><span class="line">	<span class="comment">//全连接 </span></span><br><span class="line">	(（a,b,c,d) *&gt; out) = <span class="number">9</span>; <span class="comment">//表示a,b,c,d的每一位到out的每一位的延迟都是9</span></span><br><span class="line">	</span><br><span class="line">	<span class="comment">//边沿敏感延迟</span></span><br><span class="line">	(<span class="keyword">posedge</span> clk =&gt; (out +: in)) = (<span class="number">1</span>,<span class="number">2</span>);</span><br><span class="line"><span class="comment">/*在clk的上升沿，从clk到out的路径，其上升延时是1，下降延时是2，从in到out的数据路径是同向传输，即out = in，若是-号，则是out = ~in;*/</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">//specparam</span></span><br><span class="line">	<span class="keyword">specparam</span>  a_to_out = <span class="number">9</span>;</span><br><span class="line">	(a=&gt;out) = a_to_out;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">//条件路径延迟</span></span><br><span class="line">	<span class="keyword">if</span>(a) (a=&gt;out) = <span class="number">9</span>;</span><br><span class="line">	<span class="keyword">if</span>(~a) (a=&gt;out) = <span class="number">11</span>;</span><br><span class="line"><span class="keyword">endspecify</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>路径延迟里的上升、下降、关断延迟</p>
<p>这一点在前面已经提到过了，这里具体说明一下，延迟值的个数只能是1、2、3、6、12这个5中形式，可以结合最小值:典型值:最大值的方式来使用</p>
<p>规则是</p>
<p>1个：所有延迟共用一个延迟值</p>
<p>2个：上升延迟(0-&gt;z，0-&gt;1，z-&gt;1)，下降延迟(1-&gt;z，z-&gt;0，1-&gt;0)</p>
<p>3个：上升延迟(0-&gt;1, z-&gt;1)，下降延迟(1-&gt;0, z-&gt;0)，关断延迟(1-&gt;z, 0-&gt;z)</p>
<p>6个：0-&gt;1, 1-&gt;0, 0-&gt;z, z-&gt;1, 1-&gt;z, z-&gt;0</p>
<p>12个： 0-&gt;1, 1-&gt;0, 0-&gt;z, z-&gt;1, 1-&gt;z, z-&gt;0, 0-&gt;x, x-&gt;1, 1-&gt;x, x-&gt;0, x-&gt;z, z-&gt;x</p>
</li>
<li><p>当没有指定x的路径延迟时，采用最为保守的方式来计算</p>
<ul>
<li>当x到已知态的转换消耗的最大时间</li>
<li>当已知态到x的转换消耗的最小时间</li>
</ul>
</li>
</ul>
</li>
<li><p>时序检查</p>
<ul>
<li><p><code>$setup</code>：有效时钟变沿到达之前的最下时间，就是在时钟到来之间要先建立好信号，这个信号需要在时钟到来之前建立好：<code>$setup(data_event, reference_event, limit);</code></p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">specify</span></span><br><span class="line">    $setup(data,<span class="keyword">posedge</span> clk,<span class="number">3</span>); <span class="comment">//data:被检查，posedge clk：参考信号，3:最小时间</span></span><br><span class="line"><span class="keyword">endspecify</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>$hold</code>：保持时间：<code>$hold(reference_event, data_event,limit);</code></p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">specify</span></span><br><span class="line">	$hold(<span class="keyword">posedge</span> rst_n, data, <span class="number">5</span>);</span><br><span class="line"><span class="keyword">endspecify</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>$width</code>：宽度检查：<code>$width(reference_event, limit);</code></p>
</li>
</ul>
</li>
</ul>
<h3 id="用户自定义原语UDP"><a href="#用户自定义原语UDP" class="headerlink" title="用户自定义原语UDP"></a>用户自定义原语UDP</h3><ul>
<li><p>用户自己定义一个原语，就好像Verilog自带的原语and、or一样，使用方式也是一样的</p>
</li>
<li><p>定义方式：</p>
<ul>
<li>输出必须在端口第一个，且只能有一个输出，输入可以有多个；输入输出都只能有一位</li>
<li>时序逻辑UDP需要将输出定义成reg型，且可以使用initial语句赋予初始值</li>
<li>primitive和module时同级的，定义时需要在module外，但是module可以例化primitive，但是primitive不可以调用任何模块以及其他原语</li>
<li>状态表的输入只允许是0、1、x，输入z将被认作x</li>
<li>无关项可以使用？表示</li>
<li>使用<code>table</code> <code>endtable</code>来写状态，就像数字电路里的真值表，书写的顺序要和前面端口列表里的顺序一致</li>
</ul>
<p>实例：逻辑电路</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">primitive</span> mux_4_to_1 (out,i0,i1,i2,i3,s0,s1);</span><br><span class="line"><span class="keyword">output</span> out;</span><br><span class="line"><span class="keyword">input</span> i0, i1, i2, i3, s0, s1;</span><br><span class="line"><span class="keyword">table</span></span><br><span class="line">	<span class="comment">//i0 i1 i2 i3 s0 s1 : out  按定义的顺序</span></span><br><span class="line">	   <span class="number">1</span>  ?  ?  ?  <span class="number">0</span>  <span class="number">0</span> : <span class="number">1</span>;</span><br><span class="line">	   <span class="number">0</span>  ?  ?  ?  <span class="number">0</span>  <span class="number">0</span> : <span class="number">0</span>;</span><br><span class="line">	   ?  <span class="number">1</span>  ?  ?  <span class="number">1</span>  <span class="number">0</span> : <span class="number">1</span>;</span><br><span class="line">       ?  <span class="number">0</span>  ?  ?  <span class="number">1</span>  <span class="number">0</span> : <span class="number">0</span>;</span><br><span class="line">       ?  ?  <span class="number">1</span>  ?  <span class="number">0</span>  <span class="number">1</span> : <span class="number">1</span>;</span><br><span class="line">       ?  ?  <span class="number">0</span>  ?  <span class="number">0</span>  <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">       ?  ?  ?  <span class="number">1</span>  <span class="number">1</span>  <span class="number">1</span> : <span class="number">1</span>;</span><br><span class="line">       ?  ?  ?  <span class="number">0</span>  <span class="number">1</span>  <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">       ?  ?  ?  ?  x  ? : x;</span><br><span class="line">       ?  ?  ?  ?  ?  x : x;</span><br><span class="line"><span class="keyword">endtable</span></span><br><span class="line"><span class="keyword">endprimitive</span></span><br></pre></td></tr></table></figure>

<p>实例：时序电路 电平敏感</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">primitive</span> latch(q, d, clk, clear)</span><br><span class="line"><span class="keyword">output</span> q;</span><br><span class="line"><span class="keyword">input</span> d, clk, clear;</span><br><span class="line"><span class="keyword">reg</span> q;</span><br><span class="line"><span class="keyword">initial</span> </span><br><span class="line">    q = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">table</span></span><br><span class="line">    <span class="comment">//  d clk clear :q : q+</span></span><br><span class="line">    	? ?     <span class="number">1</span>   :? : <span class="number">0</span>;</span><br><span class="line">    	<span class="number">1</span> <span class="number">1</span>     <span class="number">0</span>   :? : <span class="number">1</span>;</span><br><span class="line">    	<span class="number">0</span> <span class="number">1</span>     <span class="number">0</span>   :? : <span class="number">0</span>;</span><br><span class="line">    	? <span class="number">0</span>     <span class="number">0</span>   :? : -; <span class="comment">//保持不变</span></span><br><span class="line"><span class="keyword">endtable</span></span><br><span class="line"><span class="keyword">endprimitive</span></span><br></pre></td></tr></table></figure>

<p>实例：时序电路 边沿敏感</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">primitive</span> edge_dff(q, d, clk, clear)</span><br><span class="line">    <span class="keyword">output</span> q;</span><br><span class="line">	<span class="keyword">reg</span> q;</span><br><span class="line">    <span class="keyword">input</span> d, clk, clear;</span><br><span class="line">    <span class="keyword">initial</span> </span><br><span class="line">        q = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">table</span></span><br><span class="line">        ? ? <span class="number">1</span> : ? : <span class="number">0</span>;	<span class="comment">//复位</span></span><br><span class="line">        ? ? (<span class="number">10</span>):?: -;	<span class="comment">//清零跳转不影响</span></span><br><span class="line">        <span class="number">1</span> (<span class="number">10</span>) <span class="number">0</span>:?: <span class="number">1</span>;	<span class="comment">//下降沿锁存1</span></span><br><span class="line">        <span class="number">0</span> (<span class="number">10</span>) <span class="number">0</span>:?: <span class="number">0</span>;	<span class="comment">//下降沿锁存0</span></span><br><span class="line">        ? (<span class="number">1</span>x) <span class="number">0</span>:?: -;	<span class="comment">//由1变为不确定，不变</span></span><br><span class="line">        ? (<span class="number">0</span>?) <span class="number">0</span>:?: -;	<span class="comment">//由0变为不确定，不变</span></span><br><span class="line">        ? (x1) <span class="number">0</span>:?: -;	<span class="comment">//由不确定变1，不变</span></span><br><span class="line">        (??) ? <span class="number">0</span>:?: -;	<span class="comment">//时钟不变的时候，不变</span></span><br><span class="line">    <span class="keyword">endtable</span></span><br><span class="line"><span class="keyword">endprimitive</span></span><br></pre></td></tr></table></figure>

<p>一行里不能有多个跳变的变量</p>
<ul>
<li><p>关于跳变，还可以有如下的缩写</p>
<table>
<thead>
<tr>
<th>缩写</th>
<th>含义</th>
<th>解释</th>
</tr>
</thead>
<tbody><tr>
<td>?</td>
<td>0,1,x</td>
<td>不可用于输出</td>
</tr>
<tr>
<td>b</td>
<td>0,1</td>
<td>不可用于输出</td>
</tr>
<tr>
<td>-</td>
<td>不变</td>
<td>只用于输出</td>
</tr>
<tr>
<td>f</td>
<td>(10)</td>
<td>下降沿</td>
</tr>
<tr>
<td>r</td>
<td>(01)</td>
<td>上升沿</td>
</tr>
<tr>
<td>p</td>
<td>(0x)、(01)、(x1)</td>
<td>可能是上升沿</td>
</tr>
<tr>
<td>n</td>
<td>(1x)、(10)、(x0)</td>
<td>可能是下降沿</td>
</tr>
<tr>
<td>*</td>
<td>(??)</td>
<td>任意变化</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
</ul>
]]></content>
      <tags>
        <tag>Verilog</tag>
      </tags>
  </entry>
  <entry>
    <title>vim基本操作</title>
    <url>/2019/09/13/vim%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<p>vim的操作指令比较麻烦，但是熟练掌握了vim的使用方法后可以带来比较高的开发效率，所以在此篇博客上记录一些vim的使用方法。</p>
<a id="more"></a>

<h2 id="1-vim的操作模式"><a href="#1-vim的操作模式" class="headerlink" title="1. vim的操作模式"></a>1. vim的操作模式</h2><h3 id="1-1-命令模式"><a href="#1-1-命令模式" class="headerlink" title="1.1 命令模式"></a>1.1 命令模式</h3><p>打开vim时就处于命令模式，在此模式下输入<code>:</code> 进入最后一行模式；输入<code>i</code> 或者<code>a</code> 进入插入模式</p>
<p>在命令模式下可以输入指令来完成文本的编辑</p>
<h3 id="1-2-插入模式"><a href="#1-2-插入模式" class="headerlink" title="1.2 插入模式"></a>1.2 插入模式</h3><p>在插入模式下可以进行文本的输入和删除，就像Windows中打开记事本的模式一样</p>
<h3 id="1-3-最后一行模式"><a href="#1-3-最后一行模式" class="headerlink" title="1.3 最后一行模式"></a>1.3 最后一行模式</h3><p>在此模式下，可以输入命令来完成文本的编辑</p>
<blockquote>
<p>其实，主要模式大体分成两类，一类是输入文本的模式，就是插入模式，另一类是输入命令完成编辑的模式，就是命令模式和最后一行模式</p>
</blockquote>
<hr>
<h2 id="2-vim的基本操作"><a href="#2-vim的基本操作" class="headerlink" title="2. vim的基本操作"></a>2. vim的基本操作</h2><h3 id="2-1-打开文件"><a href="#2-1-打开文件" class="headerlink" title="2.1 打开文件"></a>2.1 打开文件</h3><p><strong><em>vim file</em></strong> </p>
<p>使用这个命令可以打开一个二进制文本文件，如果这个文件不存在，则会新建一个文本文件</p>
<p>如果使用 <code>vim file +n</code> 则打开一个文件，并将光标定位到第n行的行首</p>
<h3 id="2-2-保存文件"><a href="#2-2-保存文件" class="headerlink" title="2.2 保存文件"></a>2.2 保存文件</h3><p>保存文件在最后一行模式下，输入<code>:wq</code> 可以保存退出，输入<code>:q</code> 为退出，输入<code>:w</code> 为保存，输入时加上<code>!</code> 为强制执行该操作，输入<code>! commmand</code> 为暂时离开vim去终端执行命令，<code>:w filename</code> 保存到指定文件</p>
<hr>
<h2 id="3-vim的实用命令"><a href="#3-vim的实用命令" class="headerlink" title="3. vim的实用命令"></a>3. vim的实用命令</h2><h3 id="3-1-插入模式"><a href="#3-1-插入模式" class="headerlink" title="3.1 插入模式"></a>3.1 插入模式</h3><ul>
<li><code>a</code>：在光标右边插入文字</li>
<li><code>i</code>：在光标位置插入文字</li>
<li><code>o</code>：在光标下新建一行</li>
<li><code>O</code>：在光标上新建一行</li>
<li><code>I</code>：在光标所在行的行首插入文字</li>
<li><code>A</code>：在光标所在行的行尾插入文字</li>
</ul>
<h3 id="3-2-删除、修改功能"><a href="#3-2-删除、修改功能" class="headerlink" title="3.2 删除、修改功能"></a>3.2 删除、修改功能</h3><ul>
<li><code>[n]x</code>：删除光标后n个字符</li>
<li><code>[n]x</code>：删除光标前的n个字符</li>
<li><code>D</code>：删除光标开始到行尾的字符</li>
<li><code>[n]dd</code>：剪切从当前行开始的n行</li>
<li><code>[n]yy</code>：复制从当前行开始的n行</li>
<li><code>p</code>：将剪贴板上的内容粘贴</li>
<li><code>dG</code>：删除光标所在开始到文件尾的所有字符</li>
<li><code>J</code>：合并两行</li>
<li><code>.</code>：执行上一个操作</li>
<li><code>u</code>：撤销前一个命令</li>
<li><code>ctrl</code> <code>+</code> <code>r</code> ：反撤销</li>
</ul>
<h3 id="3-3-行定位功能"><a href="#3-3-行定位功能" class="headerlink" title="3.3 行定位功能"></a>3.3 行定位功能</h3><ul>
<li><code>ctrl + f</code>：向前滚动一个屏幕</li>
<li><code>ctrl + b</code>：向后滚动一个屏幕</li>
<li><code>gg</code>：到文件第一行行首</li>
<li><code>G</code>：到文件最后一行行首</li>
<li><code>:$</code>：到文件最后一行行尾</li>
<li><code>nG</code> 或者<code>ngg</code>：到指定行</li>
<li><code>/内容</code>：查找指定内容</li>
<li><code>h j k l</code> 移动</li>
<li><code>0</code> 移动到行首</li>
<li><code>$</code> 移动到行尾</li>
</ul>
<h3 id="3-4-文本查找功能"><a href="#3-4-文本查找功能" class="headerlink" title="3.4 文本查找功能"></a>3.4 文本查找功能</h3><ul>
<li><p><code>/字符串</code>：查找指定字符串</p>
</li>
<li><p><code>n</code>：查找下一个</p>
</li>
<li><p><code>N</code>：查找上一个</p>
</li>
<li><p><code>?字符串</code>：向上找一个</p>
</li>
<li><p><code>/^字符串</code>：查找以字符串开头的行</p>
</li>
<li><p><code>/字符串$</code>：查找以字符串结尾的行</p>
</li>
<li><p>光标移到单词下，使用<code>#</code>查找</p>
</li>
<li><p>光标移到单词下，使用<code>K</code>可以自动跳到man文档，不过这样跳到的是第一章</p>
<blockquote>
<p>可以先指定章节，再使用<code>K</code></p>
</blockquote>
</li>
</ul>
<h3 id="3-5-文本替换功能"><a href="#3-5-文本替换功能" class="headerlink" title="3.5 文本替换功能"></a>3.5 文本替换功能</h3><ul>
<li><code>r</code>：替换当前字符</li>
<li><code>:r filename</code>：在光标处插入另外一个文件</li>
<li><code>:s/p1/p2/g</code>：将当前行的p1用p2替换</li>
<li><code>:%s/p1/p2/g</code>：将文件中所有的p1都用p2替换</li>
<li><code>:n1,n2s/p1/p2/g</code>：将从n1行到n2行的所有p1用p2替换</li>
</ul>
<h3 id="3-6-可视模式"><a href="#3-6-可视模式" class="headerlink" title="3.6 可视模式"></a>3.6 可视模式</h3><ul>
<li>使用<code>v</code> 切换到可视模式下</li>
<li>使用<code>hjkl</code>移动光标选择文字</li>
<li>使用 <code>d</code> 剪切，<code>y</code> 复制， <code>p</code> / <code>P</code> 粘贴</li>
</ul>
<h3 id="3-7-缩进"><a href="#3-7-缩进" class="headerlink" title="3.7 缩进"></a>3.7 缩进</h3><ul>
<li><code>&gt;&gt;</code> : 右缩进</li>
<li><code>&lt;&lt;</code> : 左缩进  </li>
</ul>
<h3 id="3-8-分屏"><a href="#3-8-分屏" class="headerlink" title="3.8 分屏"></a>3.8 分屏</h3><ul>
<li><code>:sp</code> : 水平分屏</li>
<li><code>:vsp</code>：垂直分屏</li>
<li>可以在命令后面加上文件名，可以分屏打开另外一个文件</li>
<li><code>ctrl</code> <code>+</code> <code>ww</code> : 切换屏幕</li>
<li><code>:wqa</code> ：退出所有的窗口</li>
</ul>
<h2 id="4-配置文件"><a href="#4-配置文件" class="headerlink" title="4. 配置文件"></a>4. 配置文件</h2><ul>
<li>系统所有用户的配置文件：<code>/etc/vim/.vimrc</code></li>
<li>用户级别的配置文件 ：<code>~/vim/.vimrc</code></li>
</ul>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>vim</tag>
      </tags>
  </entry>
  <entry>
    <title>最大子列和</title>
    <url>/2020/02/13/%E6%9C%80%E5%A4%A7%E5%AD%90%E5%88%97%E5%92%8C%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>给定N个整数的序列{A1,A2,A3,……}</p>
<p>求最大的子列和：所谓最大子列和就是这个序列中一块连续的序列的和</p>
<a id="more"></a>

<h1 id="1-算法一：暴力算法"><a href="#1-算法一：暴力算法" class="headerlink" title="1. 算法一：暴力算法"></a>1. 算法一：暴力算法</h1><p>暴力算法，按照题意，循环两次表示首尾，算两个中间的和</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max_subseq_sum</span><span class="params">(<span class="keyword">int</span> A[], <span class="keyword">int</span> N)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sum, max_sum;</span><br><span class="line">    <span class="keyword">int</span> i j, k;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">    &#123;  </span><br><span class="line">    	<span class="keyword">for</span>(j = i; j &lt; N; j++)</span><br><span class="line">      	&#123;</span><br><span class="line">            sum = <span class="number">0</span>;</span><br><span class="line">        	<span class="keyword">for</span>(k = i; k &lt;= j; k++)</span><br><span class="line">          	&#123;</span><br><span class="line">            	sum += A[k];</span><br><span class="line">          	&#125;</span><br><span class="line">      		<span class="keyword">if</span>(sum &gt; max_sum)</span><br><span class="line">                max_sum = sum;</span><br><span class="line">      	&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max_sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个算法的时间复杂度是O(n^3)</p>
<h1 id="2-算法二：动点脑子的暴力算法"><a href="#2-算法二：动点脑子的暴力算法" class="headerlink" title="2. 算法二：动点脑子的暴力算法"></a>2. 算法二：动点脑子的暴力算法</h1><p>算从每个i开始的最大子列和</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max_subseq_sum</span><span class="params">(<span class="keyword">int</span> A[],<span class="keyword">int</span> N)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> sum, max_sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> i, j;</span><br><span class="line">    <span class="keyword">for</span>(i = <span class="number">0</span>;i &lt; N;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        sum = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(j = i;j &lt; N; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            sum += A[j];</span><br><span class="line">            <span class="keyword">if</span>(sum&gt;max_sum)</span><br><span class="line">                max_sum = sum;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max_sum；</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>时间复杂度是O(n^2)</p>
<p>O(n^2) 一般可以转换成O(nlogn)的复杂度</p>
<h1 id="3-算法三-分治法"><a href="#3-算法三-分治法" class="headerlink" title="3. 算法三 分治法"></a>3. 算法三 分治法</h1><p>将序列一分为2</p>
<p>然后分别找出左右两边的最大子列和</p>
<p>然后找出跨越边界的最大子列和</p>
<p>在找左右的最大子列和的时候可以继续使用分治法</p>
<p>分治完之后就要和，就是那个跨越边界的问题</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">T(N) = <span class="number">2</span>T(N/<span class="number">2</span>) + O(N) <span class="comment">//整个是T(N),那么分治之后就是T(N/2)，然后合并的时候跨越边界需要一次遍历就是O(N)</span></span><br><span class="line">	 = <span class="number">2</span>(<span class="number">2</span>T(N/<span class="number">4</span>)+O(N/<span class="number">2</span>))+O(N)</span><br><span class="line">	 = ……</span><br><span class="line">     = <span class="number">2</span>^k T(<span class="number">1</span>) + kO(N)	<span class="comment">//</span></span><br><span class="line">N/(<span class="number">2</span>^k) = <span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>可以计算得到时间复杂度是O(nlogn)</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max3</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b, <span class="keyword">int</span> c)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> A&gt;B?A&gt;C?A:C:B&gt;C?B:C;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">divide_and_conqur</span><span class="params">(<span class="keyword">int</span> <span class="built_in">list</span>[], <span class="keyword">int</span> left, <span class="keyword">int</span> right)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> max_left_sum,max_right_sum;		<span class="comment">//左侧和，右侧和</span></span><br><span class="line">    <span class="keyword">int</span> max_left_border_sum, max_right_border_sum;	<span class="comment">//左侧最大和，右侧最大和</span></span><br><span class="line">    <span class="keyword">int</span> left_border_sum, right_border_sum;<span class="comment">//左侧边缘数字，靠近中间的，右边靠近中间的数字的和</span></span><br><span class="line">    <span class="keyword">int</span> center ,i;</span><br><span class="line">    </span><br><span class="line">   	<span class="keyword">if</span>(left == right)	<span class="comment">//左边==右边，分治到只剩一个数字了，到头了</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">list</span>[left] &gt; <span class="number">0</span>)	<span class="comment">//大于0就返回，否则就是一个数字都没有最大</span></span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">list</span>[left];		<span class="comment">//左右合一了，返回左边或者右边区别不大</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    cneter = (left + right)/<span class="number">2</span>;</span><br><span class="line">    max_left_sum = divide_and_conqur(<span class="built_in">list</span>, left, center);		<span class="comment">//递归分</span></span><br><span class="line">    max_rignt_sum = divide_and_conqur(<span class="built_in">list</span>,center+<span class="number">1</span>,right);</span><br><span class="line">    </span><br><span class="line">    max_left_border_sum = <span class="number">0</span>;	<span class="comment">//</span></span><br><span class="line">    left_border_sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(i=center; i&gt;= left; i--)	<span class="comment">//从靠近中间开始找，找到最边缘开始的最大值</span></span><br><span class="line">    &#123;</span><br><span class="line">        left_border_sum += <span class="built_in">list</span>[i];</span><br><span class="line">        <span class="keyword">if</span>(left_border_sum &gt; max_left_border_sum)</span><br><span class="line">            max_left_border_sum = left_border_sum;</span><br><span class="line">    &#125;</span><br><span class="line">    max_right_border_sum = <span class="number">0</span>;</span><br><span class="line">    right_border_sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(i=center+<span class="number">1</span>; i&lt;= right; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        right_border_sum += <span class="built_in">list</span>[i];</span><br><span class="line">        <span class="keyword">if</span>(right_border_sum &gt; max_right_border_sum)</span><br><span class="line">            max_right_border_sum = right_border_sum;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> max3(max_left_sum,max_right_sum, max_left_border_sum + max_right_border_sum);</span><br><span class="line">    <span class="comment">//最大值必然产生于左边最大值、右边最大值，或者两个边缘的最大值的和</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max_subseq_sum</span><span class="params">(<span class="keyword">int</span> <span class="built_in">list</span>[], <span class="keyword">int</span> N)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> divide_and_conqur(<span class="built_in">list</span>, <span class="number">0</span>, N<span class="number">-1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="4-算法四：在线处理算法"><a href="#4-算法四：在线处理算法" class="headerlink" title="4. 算法四：在线处理算法"></a>4. 算法四：在线处理算法</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max_subseq_sum</span><span class="params">(<span class="keyword">int</span> A[], <span class="keyword">int</span> N)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> sum, max_sum = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">int</span> i;</span><br><span class="line">	sum = max_sum = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span>(i=<span class="number">0</span>; i&lt;N; i++)</span><br><span class="line">	&#123;	</span><br><span class="line">		sum += A[i];</span><br><span class="line">		<span class="keyword">if</span>(sum&gt;max_sum)</span><br><span class="line">			max_sum = sum;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span>(sum&lt;<span class="number">0</span>)</span><br><span class="line">			sum = o;	<span class="comment">//负数不可能使下面的和要大，所以抛弃它</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> max_sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个算法的时间复杂度是O(n)</p>
<p>这个算法很妙哇</p>
]]></content>
      <tags>
        <tag>DS</tag>
      </tags>
  </entry>
  <entry>
    <title>数组的指针与指针的数组</title>
    <url>/2020/03/07/%E6%95%B0%E7%BB%84%E7%9A%84%E6%8C%87%E9%92%88%E4%B8%8E%E6%8C%87%E9%92%88%E7%9A%84%E6%95%B0%E7%BB%84/</url>
    <content><![CDATA[<p>C语言中数组和指针是分不开的，这篇文章主要来记录一下C语言中数组和指针的关系</p>
<a id="more"></a>

<h1 id="1-简单的数组和指针的关系"><a href="#1-简单的数组和指针的关系" class="headerlink" title="1. 简单的数组和指针的关系"></a>1. 简单的数组和指针的关系</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a[<span class="number">10</span>]; <span class="comment">//定义了一个长度为10的整型数组</span></span><br><span class="line"><span class="keyword">int</span> *p;		<span class="comment">//定义一个整型指针</span></span><br><span class="line"></span><br><span class="line">p = a;		<span class="comment">//将数组a的地址赋给指针p</span></span><br><span class="line">a[<span class="number">0</span>] = <span class="number">1</span>;	<span class="comment">//给数组的首数字赋值</span></span><br><span class="line">*(p+<span class="number">1</span>) = <span class="number">10</span>;<span class="comment">//给数组的第二个数字赋值</span></span><br></pre></td></tr></table></figure>

<p>从上面的例子可以看出，当我们定义了一个数组的时候，其实是在内存中申请了一块相应大小的空间，数组的名称<code>a</code>代表的是数组的首元素的地址。我们也知道，指针是一种变量，里面存放的就是地址这种数，所以可以把<code>a</code>的值赋值给指针<code>p</code>，因为它们从本质上讲都是地址变量（没有这说法，自造的）</p>
<p>我们可以使用数组下标的方式来访问数组里的元素，也可以使用将指针的指向移动的方式来访问数组里的元素，就像上面的例子的最后两条语句，两种方式在本质上是几乎一样的，都是将等号右值填入到等号左边所指代的地址空间里去，只不过表示这个地址空间的方式不太一样。</p>
<p>当然在上面的例子里,    <code>p</code>和<code>a</code>的位置是不一样的，只不过<code>p</code>和<code>a</code>对应的内存空间里存放了相同的地址，所以就可以使用不同的方式去操作这个数组，而能得到相同的操作结果</p>
<h1 id="2-数组的指针和指针的数组"><a href="#2-数组的指针和指针的数组" class="headerlink" title="2. 数组的指针和指针的数组"></a>2. 数组的指针和指针的数组</h1><p>简单一维数组前面已经讨论过了，这种容易引起混淆的更多出现在二维数组里</p>
<p>首先要明确定义数组的<code>[]</code>的优先级是要高于定义指针的<code>*</code>的优先级的，再来看下面这个例子</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> *p_a[<span class="number">10</span>]; <span class="comment">//定义了一个指针的数组，这个数组有10个元素，每个元素都是一个指针，里面存放地址，</span></span><br><span class="line">			  <span class="comment">//这个定义申请了10个指针内存大小，数组名是p_a，指针名字用下标引用</span></span><br><span class="line">			  <span class="comment">//没有赋值，里面指针的指向并不明确</span></span><br><span class="line"><span class="keyword">int</span> (*p_p)[<span class="number">10</span>]; <span class="comment">//定义了一个数组的指针，这个指针指向一个含有10个元素的数组，这个定义只申请了</span></span><br><span class="line">			  <span class="comment">//一个指针的内存大小,这个数组是匿名数组</span></span><br><span class="line">			  <span class="comment">//这个指针的指向是明确的，指向内存空间中一段连续的10个int大小的内存区域的首地址</span></span><br></pre></td></tr></table></figure>

<p>指针加一的步长和指针指向的数据类型是相关的，看上面的例子，<code>p_a</code>是指针数组，数组里的值是地址，这个地址是指向<code>int</code>型变量，所以步长就是一个<code>int</code>变量的长度；<code>p_p</code>是数组指针，指针指向的是一个<code>int [10]</code>匿名数组，步长就是10个<code>int</code>型变量的长度</p>
]]></content>
      <tags>
        <tag>C/C++</tag>
      </tags>
  </entry>
  <entry>
    <title>git基础</title>
    <url>/2020/01/15/git%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>git是一种很好用且流行版本管理软件，这里介绍一下它的使用方法</p>
<a id="more"></a>

<h2 id="1-安装方法"><a href="#1-安装方法" class="headerlink" title="1.安装方法"></a>1.安装方法</h2><ul>
<li>Windows：直接download，但还是Windows的gitGUI 界面十分丑陋，使用git bash体验更佳</li>
<li>Linux ： sudo apt-get install git</li>
</ul>
<h2 id="2-使用的一些基本方法"><a href="#2-使用的一些基本方法" class="headerlink" title="2. 使用的一些基本方法"></a>2. 使用的一些基本方法</h2><ul>
<li><p>进入本地你想要变成仓库的文件夹 输入git init使之变成一个仓库</p>
</li>
<li><p>初次使用时要先输入用户的信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config user.name <span class="string">"your_name"</span></span><br><span class="line">git config user.email <span class="string">"your_email"</span></span><br></pre></td></tr></table></figure>

<p>设置之后，可使用</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config user.name</span><br><span class="line">git config user.email</span><br></pre></td></tr></table></figure>

<p>查看设置是否正确</p>
</li>
<li><p>使用 git add filename 来将文件变成可提交的状态</p>
<blockquote>
<p> git add . 就是添加所有的文件</p>
</blockquote>
</li>
<li><p>使用  git commit f提交文件 提交的文件必须处于可以提交的状态</p>
<blockquote>
<p> 一般使用的是git commit -m “message”的形式来提交</p>
</blockquote>
</li>
</ul>
<h2 id="3-工作区和暂存区"><a href="#3-工作区和暂存区" class="headerlink" title="3.工作区和暂存区"></a>3.工作区和暂存区</h2><ul>
<li>工作区就是电脑中能看到的目录，也就是你正在工作的目录</li>
<li>暂存区：stage，就是你在工作区中作出一些修改之后，可以通过add指令将工作区的文件放到暂存区中，只有暂存区的文件可以通过git commit来提交</li>
<li>提交到的位置叫做master分支，这是你在创建仓库时git自动为你创建的一条分支</li>
</ul>
<h2 id="4-日志和修改"><a href="#4-日志和修改" class="headerlink" title="4. 日志和修改"></a>4. 日志和修改</h2><ul>
<li><p>使用git log可查看修改日志</p>
</li>
<li><p>使用 git diff可以显示unstaged 状态的文件与上一次commit的文件的不同</p>
<p>如果要查看已经add但未提交的文件与上一次提交的文件的不同，需要使用 git diff –cached 理查看<br>同时查看staged状态和untagged状态的diff ，要使用git diff HEAD来查看</p>
</li>
<li><p>版本回退</p>
<p>git中用HEAD表示当前的版本</p>
<p>上个版本是HEAD^<br>上上个是HEAD^^<br>上一百个版本是HEAD～100</p>
</li>
<li><p>git reset –hard HEAD^ 可回退到上个版本</p>
</li>
<li><p>git reset –hard  commit id 可以回退到任意你能找到ID的版本，ID没必要写全，能分辨出来就好</p>
</li>
<li><p>git reflog 可以查看你的git的每一次操作，使用这个功能可以查看未来版本的commit id</p>
</li>
<li><p>git checkout –filename命令可以将工作区的内容回到最近一次git commit或者git add时的状态</p>
</li>
<li><p>使用git reset HEAD filename可以将暂存区的文件回退到工作区</p>
</li>
<li><p>删除文件</p>
<p>使用git rm 可以删除一个文件，删除之后再commit</p>
</li>
</ul>
<h2 id="5-分支管理"><a href="#5-分支管理" class="headerlink" title="5. 分支管理"></a>5. 分支管理</h2><ul>
<li><p>创建与合并分支：创建一个新的分支之后生成一个新的分支指针，这个指针指向新的分支，然后进行修改，如果需要合并，只需要将master指针指向你新建的分支的指针就好了</p>
</li>
<li><p>合并之后可以删除分支</p>
</li>
<li><p>git branch dev</p>
<p>git checkout dev  /git switch dev<br>代表创建一个新分支dev并切换到dev分支<br>git checkout -b dev，-b参数表示创建并切换<br>也可以用git switch -c dev 创建并切换一个新分支</p>
</li>
<li><p>git branch 查看所有分支</p>
<p>当前分支前有一个*号</p>
</li>
<li><p>git merge dev 将dev分支合并到master分支</p>
<p>git log –graph可以看到命令分支图<br>加上 no-ff参数可以做普通合并，即能看到合并的历史，而默认的fast-forward模式合并之后看不出来做过合并</p>
</li>
<li><p>git branch -d dev 合并之后就可以删除分支了</p>
</li>
<li><p>bug分支：使用git stash命令可以将当前工作现场隐藏起来，等修复bug之后再继续工作。</p>
<p>git stash list可以查看隐藏的工作现场被隐藏到哪里去了<br>使用git stash apply可以恢复，再使用git stash drop来删除stash内容<br>上述两步也可以用git stash pop来一步实现<br>修复之后可以用git cheer-pick <commit>命令将master上修好bug提交的修改复制到当前工作分支上</p>
</li>
<li><p>多人协作：git remote -v 可查看远程库信息</p>
</li>
</ul>
<p>git push origin branch_name可以推送本地分支，如果失败则用git pull 抓取之后再push<br>建立本地分支和远程分支的关联：git branch –set-upstream branch_name origin/branch_name</p>
<h2 id="6-远程仓库"><a href="#6-远程仓库" class="headerlink" title="6. 远程仓库"></a>6. 远程仓库</h2><ul>
<li><p>登陆GitHub，创建一个新的仓库</p>
</li>
<li><p>然后根据GitHub的提示，在本地的仓库下运行一些命令，将自己本地仓库和GitHub仓库关联</p>
<p>git remote add origin <a href="mailto:git@github.com">git@github.com</a>:userid/reponame.git</p>
</li>
<li><p>将本地仓库推送到GitHub上</p>
<p>git push origin master<br>注意第一次推送一个仓库时，加上 -u参数<br>在第一次使用git push 或者git clone功能时，会收到一个SSH警告，输入yes回车即可，代表已经把GitHub的key添加到本机的一个信任列表里了</p>
</li>
<li><p>git clone <a href="mailto:git@github.com">git@github.com</a>:userid/reponame即可将远程库克隆到本地上</p>
<p>git不仅仅支持ssh协议，也支持https协议，但是相对来说ssh协议的速度是最快的。</p>
</li>
<li><p>GitHub</p>
<ul>
<li>fork：在自己的账号下克隆了一个仓库</li>
<li>git clone <a href="mailto:git@github.com">git@github.com</a>:userid/reponame.git</li>
<li>使用pull request给官方仓库来贡献代码</li>
</ul>
</li>
</ul>
]]></content>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>构造函数初始化列表</title>
    <url>/2020/05/19/%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E5%88%9D%E5%A7%8B%E5%8C%96%E5%88%97%E8%A1%A8/</url>
    <content><![CDATA[<p>构造函数初始化列表，就是那个冒号的写法</p>
<a id="more"></a>

<p>这种方法是为了写起来方便的，也没有特别的含义</p>
<p>用法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> a, b,c;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    A(<span class="keyword">int</span> ax, <span class="keyword">int</span> bx, <span class="keyword">int</span> cx):a(ax),b(bx),c(cx)&#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>仅仅是为了简洁，就相当于下面</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">A(<span class="keyword">int</span> ax, <span class="keyword">int</span> bx, <span class="keyword">int</span> cx)</span><br><span class="line">&#123;</span><br><span class="line">    a = ax;</span><br><span class="line">    b = bx;</span><br><span class="line">    c = cx;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>但是有几点值得注意的</p>
<ul>
<li><p>可以在冒号后面写一部分，在函数体写一部分</p>
</li>
<li><p>赋值的顺序是按照类声明中的顺序出现的，而不是书写的顺序</p>
<p>比如上面的例子如果构造函数这么写</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">A(<span class="keyword">int</span> ax, <span class="keyword">int</span> bx, <span class="keyword">int</span> cx):c(cx),a(ax),b(bx)</span><br><span class="line">&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>其结果仍然是a,b,c的赋值顺序，而不是按照c,a,b的顺序</p>
<p>但是这个是在执行函数体之前执行的，所以部分在函数体内部赋值的不受影响</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">int</span> a, b, c;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	A(<span class="keyword">int</span> ax, <span class="keyword">int</span> bx, <span class="keyword">int</span> cx) :c(cx), b(c)</span><br><span class="line">	&#123;</span><br><span class="line">		a = ax;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Print</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="string">" "</span> &lt;&lt; b &lt;&lt; <span class="string">" "</span> &lt;&lt; c &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">A <span class="title">d</span><span class="params">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span></span>;</span><br><span class="line">	d.Print();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行的结果是</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1 -858993460 3</span><br></pre></td></tr></table></figure>

<p>可看执行的顺序大概是</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">b = c;</span><br><span class="line">c = cx;</span><br><span class="line">a = ax;</span><br></pre></td></tr></table></figure>
</li>
<li><p>对于const，只能用这种方式赋值</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">const</span> <span class="keyword">int</span> len;</span><br><span class="line">	<span class="keyword">int</span>* a;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	A(<span class="keyword">int</span> l):len(l)</span><br><span class="line">	&#123;</span><br><span class="line">		a = <span class="keyword">new</span> <span class="keyword">int</span>[len];</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">A <span class="title">b</span><span class="params">(<span class="number">1</span>)</span></span>;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cin</span>.<span class="built_in">get</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>最开始提到的两种等效关系，其实实现方式是有区别的，在下面的这个例子中会体现</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">  &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  	A()</span><br><span class="line">    &#123;</span><br><span class="line">          <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"Create"</span>&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">      A(<span class="keyword">int</span> x)</span><br><span class="line">      &#123;</span><br><span class="line">          <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"Create with"</span> &lt;&lt; x&lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  </span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">B</span></span></span><br><span class="line"><span class="class">  &#123;</span></span><br><span class="line">      <span class="keyword">private</span>:</span><br><span class="line">      	<span class="keyword">int</span> x, y;</span><br><span class="line">      	A m_a;</span><br><span class="line">      <span class="keyword">public</span>:</span><br><span class="line">      	B(<span class="keyword">int</span> a)</span><br><span class="line">          &#123;</span><br><span class="line">              m_a = A(a);</span><br><span class="line">          &#125;</span><br><span class="line">  &#125;;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，使用</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">A <span class="title">e</span><span class="params">(<span class="number">8</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p>会输出</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Create</span><br><span class="line">Create with <span class="number">8</span></span><br></pre></td></tr></table></figure>

<p>这里B被创造了两次？why?</p>
<p>因为在实例化的时候就使用A  m_a,调用了一次构造函数</p>
<p>然后在赋值的时候又调用了一次构造函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	A()</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"Create"</span>&lt;&lt;<span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    A(<span class="keyword">int</span> x)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"Create with"</span> &lt;&lt; x&lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	<span class="keyword">int</span> x, y;</span><br><span class="line">    	A m_a;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	B(<span class="keyword">int</span> a) :m_a(A(a))&#123;&#125;  <span class="comment">//或者写成 m_a(a)</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这样就不会调用两次构造函数，因为创造实例的时候调用一次构造函数之后就没有调用了</p>
</li>
</ul>
<p>  <font size = 2> 参考博文:<a href="http://c.biancheng.net/view/2223.html" target="_blank" rel="noopener">C++构造函数初始化列表</a></font></p>
]]></content>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Gcc和Makefile</title>
    <url>/2020/07/10/gcc%E5%92%8Cmakefile/</url>
    <content><![CDATA[<p>在Linux终端下写代码，没有像VS这种的集成开发环境，就需要使用Gcc和Makefile来执行编译。</p>
<a id="more"></a>

<h1 id="0-前言"><a href="#0-前言" class="headerlink" title="0. 前言"></a>0. 前言</h1><p>现在我使用Linux写代码时，编译单个文件使用的是</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc hello.c -o hello</span><br></pre></td></tr></table></figure>

<p>会得到一个hello文件（可执行程序）（.elf文件，也就是Win下的.exe文件)</p>
<p>如果编译多文件，要使用如下命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc a.c b.c c.c -o out</span><br></pre></td></tr></table></figure>

<p>如果每次编译写这么多，太麻烦了，所以需要掌握Makefile编写代码</p>
<hr>
<h1 id="1-C-C-程序编译过程"><a href="#1-C-C-程序编译过程" class="headerlink" title="1. C/C++程序编译过程"></a>1. C/C++程序编译过程</h1><ul>
<li><p>C语言</p>
<ul>
<li><p>预处理: 将.h 和 .c文件展开（宏定义替换、头文件粘贴复制） 由<code>.c</code>文件到<code>.i</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -E a.c -o a.i</span><br></pre></td></tr></table></figure>

<p>还可指定头文件的目录，使用<code>-I</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -E -I ./head a.c -o a.i</span><br></pre></td></tr></table></figure>
</li>
<li><p>编译: 由每个 <code>.i</code>文件生成一个<code>.s</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -S a.i -o a.s</span><br></pre></td></tr></table></figure>
</li>
<li><p>汇编: 由每个<code>.s</code>文件生成一个<code>.o</code>的目标文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc -c a.s -o a.o</span><br></pre></td></tr></table></figure>

<p>或者使用as命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">as a.s -o a.o</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>链接: 由这么多个目标文件链接成为一个可执行文件</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc hello.o -o hello</span><br></pre></td></tr></table></figure>

<p>  或者使用ld</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ld a.o b.o -o out</span><br></pre></td></tr></table></figure>

<p>上面就是C语言的编译的微观过程了</p>
</li>
<li><p>C++</p>
<p>C++的编译过程和C语言是类似的，这里不做赘述</p>
</li>
<li><p>编译过程中加入宏，使用<code>-D</code></p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc hello.c -o out -I ./head -D DEBUG</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用<code>-O</code>参数优化程序，优化等级为0~3，依次递增</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc hello,c -o out -I ./head -D DEBUG -O2</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>-Wall</code> ：输出所有的警示</p>
</li>
<li><p><code>-g</code>：使用gdb调试时加入一些调试信息</p>
</li>
</ul>
<hr>
<h1 id="2-Makefile文件"><a href="#2-Makefile文件" class="headerlink" title="2. Makefile文件"></a>2. Makefile文件</h1><p>大型C/C++项目开发一定是要用Makefile的</p>
<h2 id="2-1-基础知识"><a href="#2-1-基础知识" class="headerlink" title="2.1 基础知识"></a>2.1 基础知识</h2><ul>
<li><p>新建文本文档，取名为Makefile</p>
</li>
<li><p><code>#</code> : 注释</p>
</li>
<li><p>显式规则</p>
<ul>
<li><p>语法规则</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">目标文件:依赖文件</span></span><br><span class="line">[TAB]指令</span><br></pre></td></tr></table></figure>
<p>第一个目标文件是我的最终目标，所以要反着写makefile</p>
</li>
</ul>
</li>
</ul>
  <figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">hello:hello.o</span></span><br><span class="line">	gcc hello.o -o hello</span><br><span class="line"><span class="section">hello.o:hello.s</span></span><br><span class="line">	gcc -c hello.s -o hello.o</span><br><span class="line"><span class="section">hello.s:hello.i</span></span><br><span class="line">	gcc -S hello.i -o hello.s</span><br><span class="line"><span class="section">hello.i:hello.c</span></span><br><span class="line">	gcc -E hello.c -o hello.i</span><br></pre></td></tr></table></figure>

<p>  使用<code>make</code>执行</p>
<ul>
<li><p>伪目标: 不需要目标文件的指令</p>
<p>  加上下面的代码可以执行清除</p>
  <figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>:</span></span><br><span class="line">clear： <span class="comment">#这个名字是任意取的，在后续调用时使用</span></span><br><span class="line">	rm -rf hello.o hello.s hello.i</span><br></pre></td></tr></table></figure>

<p>  在执行是使用<code>make clear</code>可以执行伪目标，不加<code>clear</code>就不执行</p>
</li>
</ul>
<h2 id="2-2-变量"><a href="#2-2-变量" class="headerlink" title="2.2 变量"></a>2.2 变量</h2><ul>
<li><p><code>=</code> 替换</p>
</li>
<li><p><code>+=</code> 追加</p>
</li>
<li><p><code>:=</code> 恒等于</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">TAR = hello</span><br><span class="line">OBJ = hello1.o hello2.o</span><br><span class="line">CC := gcc</span><br><span class="line"></span><br><span class="line"><span class="variable">$(TAR)</span>:<span class="variable">$(OBJ)</span></span><br><span class="line">	<span class="variable">$(CC)</span> <span class="variable">$(OBJ)</span> -o <span class="variable">$(TAR)</span></span><br><span class="line"></span><br><span class="line"><span class="section">hello1.o:hello1.c</span></span><br><span class="line">	<span class="variable">$(CC)</span> -c hello1.c -o hello1.o</span><br><span class="line"><span class="section">hello2.o:hello2.c</span></span><br><span class="line">	<span class="variable">$(CC)</span> -c hello2.c -o hello2.o</span><br><span class="line"></span><br><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>:</span></span><br><span class="line"><span class="section">cleanall:</span></span><br><span class="line">	rm -rf <span class="variable">$(OBJ)</span> <span class="variable">$(TAR)</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">	rm -rf <span class="variable">$(OBJ)</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="2-3-隐含规则"><a href="#2-3-隐含规则" class="headerlink" title="2.3 隐含规则"></a>2.3 隐含规则</h2><ul>
<li><p><code>%.c</code> <code>%.o</code> 通配符，任意的单个文件</p>
</li>
<li><p><code>*.c</code> <code>*.o</code> 所有的文件</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">TAR = hello</span><br><span class="line">OBJ = hello1.o hello2.o</span><br><span class="line">CC := gcc</span><br><span class="line"></span><br><span class="line"><span class="variable">$(TAR)</span>:<span class="variable">$(OBJ)</span></span><br><span class="line">	<span class="variable">$(CC)</span> <span class="variable">$(OBJ)</span> -o <span class="variable">$(TAR)</span></span><br><span class="line"></span><br><span class="line"><span class="section">%.o:%.c</span></span><br><span class="line">	<span class="variable">$(CC)</span> -c %.c -o %.o</span><br><span class="line"></span><br><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>:aq</span></span><br><span class="line"><span class="section">cleanall:</span></span><br><span class="line">	rm -rf <span class="variable">$(OBJ)</span> <span class="variable">$(TAR)</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">	rm -rf <span class="variable">$(OBJ)</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>$^</code> 所有的依赖文件</p>
</li>
<li><p><code>$@</code> 所有的目标文件</p>
</li>
<li><p><code>$&lt;</code> 所有的依赖文件中的第一个文件</p>
</li>
<li><p><code>$*</code> 不包括扩展名的目标文件名称</p>
</li>
<li><p><code>$+</code> 所有的依赖文件，以空格分开，并以出现的先后为序，可能包含重复的依赖文件</p>
</li>
<li><p><code>$?</code> 所有时间戳比目标文件晚的依赖文件，以空格分开</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">TAR = hello</span><br><span class="line">OBJ = hello1.o hello2.o</span><br><span class="line">CC := gcc</span><br><span class="line"></span><br><span class="line"><span class="variable">$(TAR)</span>:<span class="variable">$(OBJ)</span></span><br><span class="line">	<span class="variable">$(CC)</span> <span class="variable">$^</span> -o <span class="variable">$@</span></span><br><span class="line"></span><br><span class="line"><span class="section">%.o:%.c</span></span><br><span class="line">	<span class="variable">$(CC)</span> -c <span class="variable">$^</span> -o <span class="variable">$@</span></span><br><span class="line"></span><br><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>:aq</span></span><br><span class="line"><span class="section">cleanall:</span></span><br><span class="line">	rm -rf <span class="variable">$(OBJ)</span> <span class="variable">$(TAR)</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">	rm -rf <span class="variable">$(OBJ)</span></span><br></pre></td></tr></table></figure>

<p>makefile文件中的内容还有很多，比如函数一类的，详细还是参见说明文档</p>
</li>
</ul>
]]></content>
      <tags>
        <tag>C/C++</tag>
        <tag>Gcc</tag>
      </tags>
  </entry>
  <entry>
    <title>有关static</title>
    <url>/2020/05/19/%E6%9C%89%E5%85%B3static/</url>
    <content><![CDATA[<p>有关C++中static关键字的一些用法</p>
<a id="more"></a>



<h1 id="1-用于函数、变量的声明"><a href="#1-用于函数、变量的声明" class="headerlink" title="1. 用于函数、变量的声明"></a>1. 用于函数、变量的声明</h1><p>在一个文件中定义了一个函数，然后要在另一个文件中使用这个变量？这个时候最常见的方法就是在需要使用的那个文件中声明一下这个变量</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> a;</span><br></pre></td></tr></table></figure>

<p>表示这个文件中使用的a来自于另一个文件中定义的全局变量。</p>
<p>如果不加extern关键字，就会有linking error，因为找到了两个a变量，有冲突。</p>
<p>那么如何让这个变量在其他文件中不可见，而只在此文件中有文件作用域呢？</p>
<p>使用static关键字即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> a;</span><br></pre></td></tr></table></figure>

<p>这样其他文件中再</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a;</span><br></pre></td></tr></table></figure>

<p>也不会产生冲突。</p>
<p>函数也是同理,static函数只会在当前文件作用域中有效，其他文件无法使用。如果是普通的函数，其他文件使用只需加上声明就可以有linker链接过去了。</p>
<p>这里有一个很有意思的例子</p>
<p>当在一个文件中写下面这段代码时</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun1</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fun();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fun1();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>仅编译这个文件时，可以通过编译，虽然此文件中没有定义fun()，但是给了声明，编译器可以通过这个单元的编译。但是如果在整个程序中都没有这个函数的定义，那么单文件依然可以编译，但是链接会出错,因为没有fun（）的定义。</p>
<p>如果将上面的代码改动一下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun1</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//fun();</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fun1();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此时调用fun1()时不再调用fun()，此时即使没有给fun()的定义也并不影响链接，所以可以通过编译链接。</p>
<p>但是如果改成下面这个代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun1</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fun();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//fun1();</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此时，虽然程序不调用fun1()了，也就不会用到fun()，但是链接还是错误！这不是和上面的是矛盾的吗？并不，因为虽然这个文件中没有用到fun1()，但是fun1()可能被其他文件调用，因为fun1()的形式以及决定了，它要运行就必须有fun()的定义。</p>
<p>但是做了如下更改后，却发现又可以通过了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">fun1</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fun();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//fun1();</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此时，虽然还是没有定义fun()，但是fun1()是静态的，不在此文件中使用，也不可能被其他文件使用，这时就没有链接错误了。</p>
<h1 id="2-class-和-struct中的static"><a href="#2-class-和-struct中的static" class="headerlink" title="2. class 和 struct中的static"></a>2. class 和 struct中的static</h1><p>class和struct中的static是很有意思的，也像其他的静态变量一样，代表这个变量或函数在这个空间域里是只有一份的。举个例子</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> count;</span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> student::count;<span class="comment">//全局定义一下这个变量</span></span><br></pre></td></tr></table></figure>

<p>在这个例子中，所有的student实例都是公用一个count的，对其中一个count操作，对其他的也会有影响。</p>
<p>方法（即类里的函数)也是一样。</p>
<p>还可以发现，这种变量和函数都是不依赖于具体的实例的，所以可以直接通过类来调用这种变量和函数，实际上它们也就是这么工作的。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">student student1;</span><br><span class="line">student1.count ++;</span><br></pre></td></tr></table></figure>

<p>上面的第二行代码，在实际运行中是以下面的方式运行的，而不是通过实例来调用</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">student::count++;</span><br></pre></td></tr></table></figure>

<p>当然，可以直接使用上面的方式写代码，使用类名引用。</p>
<p>静态数据成员可以被初始化，但是只能在类体外进行初始化。当然在构造一个对象时也不能给它赋在参数表中。</p>
<p><strong>使用static时要在全局定义这个变量</strong>,否则是无法解析这个符号的linking error，因为它在类声明中只能声明，没有分配空间，所以需要分配它的空间。（这个并不确定，因为貌似在编译时会被分配空间，但是这样貌似解释不通）</p>
<blockquote>
<p>全局变量的声明是通过extern关键字的</p>
</blockquote>
<p>需要注意的是，由于这种方法和变量都不依赖于实例，自然这种方法不能操作具体实例里的非静态变量。</p>
<h1 id="3-普通的用法"><a href="#3-普通的用法" class="headerlink" title="3. 普通的用法"></a>3. 普通的用法</h1><p>用在函数中，多次调用这个函数时这样量不会重新分配空间，运行结束也不会销毁。</p>
<p>强行变长了生命周期</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">static</span> Singleton* s_Instance;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">static</span> Singleton&amp; <span class="title">get</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> *s_Instance;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Hello</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line">Singleton* s_Instance = <span class="literal">nullptr</span>;</span><br></pre></td></tr></table></figure>

<p>这里返回一个实例:单例设计模式，即这个类只有一个实例</p>
<p>可以修改</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">static</span> Singleton&amp; <span class="title">get</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">static</span> Singleton Instance;</span><br><span class="line">        <span class="keyword">return</span> Instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这里如果不加static，则Instance返回时就没有了，自然也就不能引用这个变量了</p>
<p>当然可以不使用引用，这样返回一个Instance的复制也可以</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">static</span> Singleton <span class="title">get</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        Singleton Instance;</span><br><span class="line">        <span class="keyword">return</span> Instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>





]]></content>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>python基础语法</title>
    <url>/2020/03/02/python%E8%AF%AD%E6%B3%95/</url>
    <content><![CDATA[<p>这个学期学习数据库原理需要使用python，所以在这里记录一下python的一些基础语法</p>
<a id="more"></a>

<h1 id="1-基本数据类型"><a href="#1-基本数据类型" class="headerlink" title="1. 基本数据类型"></a>1. 基本数据类型</h1><h2 id="1-1-数字"><a href="#1-1-数字" class="headerlink" title="1.1 数字"></a>1.1 数字</h2><ul>
<li><p><strong>整数</strong>：<code>int</code>，没有进一步的划分了</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">int(<span class="number">1.1</span>)		<span class="comment">#转成int,得到1，其他类型也有类似用法</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>浮点数</strong>：<code>float</code>，也没有进一步的划分了</p>
<blockquote>
<p><strong>除法</strong></p>
<ul>
<li><code>/</code>：返回浮点数</li>
<li><code>//</code>：返回整数，这个就像是C语言中的除法，取结果的整数部分</li>
</ul>
</blockquote>
<blockquote>
<p><strong>type（）</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&gt;type(<span class="number">1</span>) 			<span class="comment">#整形int</span></span><br><span class="line">&gt;type(<span class="number">1</span>+<span class="number">0.1</span>)			<span class="comment">#浮点型float</span></span><br><span class="line">&gt;type(<span class="number">1</span>*<span class="number">0.1</span>)			<span class="comment">#浮点型float</span></span><br><span class="line">&gt;type(<span class="number">2</span>/<span class="number">2</span>)			<span class="comment">#整形除以整形得到浮点型</span></span><br><span class="line">&gt;type(<span class="number">2</span>//<span class="number">2</span>)			<span class="comment">#要想得到浮点型，需要使用 //</span></span><br></pre></td></tr></table></figure>
</blockquote>
<blockquote>
<p><strong>数制</strong></p>
<ul>
<li><code>0b</code>：二进制</li>
<li><code>0o</code>：八进制</li>
<li><code>0x</code>：十六进制</li>
<li><code>bin()</code>：转成二进制数，<code>bin(0xE)</code>得到<code>0b1110</code></li>
<li><code>oct()</code>：转成八进制</li>
<li><code>hex()</code>：转成十六进制</li>
</ul>
</blockquote>
</li>
<li><p><strong>布尔类型</strong></p>
<ul>
<li>真：<code>True</code>，非0数</li>
<li>假：<code>False</code>，0</li>
</ul>
<blockquote>
<p>在python中，除了0可以表示False，其他的表示空的也可以表示False</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">bool(<span class="string">'abc'</span>)		<span class="comment">#True</span></span><br><span class="line">bool(<span class="string">''</span>)		<span class="comment">#False</span></span><br><span class="line">bool([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>])	<span class="comment">#True</span></span><br><span class="line">bool(&#123;&#125;)		<span class="comment">#False</span></span><br><span class="line">bool(<span class="literal">None</span>)		<span class="comment">#False</span></span><br></pre></td></tr></table></figure>
</blockquote>
</li>
<li><p><strong>复数</strong></p>
<ul>
<li><p>平常应用到的比较少</p>
</li>
<li><p><code>j</code>：这个可以表示复数</p>
</li>
</ul>
</li>
</ul>
<h2 id="1-2-序列"><a href="#1-2-序列" class="headerlink" title="1.2 序列"></a>1.2 序列</h2><h3 id="1-2-1-字符串str"><a href="#1-2-1-字符串str" class="headerlink" title="1.2.1 字符串str"></a>1.2.1 字符串str</h3><h4 id="1-2-1-1-字符串的表示"><a href="#1-2-1-1-字符串的表示" class="headerlink" title="1.2.1.1 字符串的表示"></a>1.2.1.1 字符串的表示</h4><ul>
<li><p>表示：单引号，双引号，三引号都可以表示字符串，字符串里还有引号就可以用双引号，或者使用转义字符<code>\</code></p>
</li>
<li><p>三引号的字符串是可以写多行的（写代码一行不要超过79个字符），使用<code>\</code>也可以换行</p>
</li>
<li><p><code>print</code>函数：显示，可以使用转义字符，在idle中并不会显示出转义字符转义之后的格式，只会显示原格式</p>
</li>
<li><p>在字符串之间加上一个<code>r</code>或者<code>R</code>，变为原始字符串</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(<span class="string">r'hello \n world'</span>)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="1-2-1-2-转义字符"><a href="#1-2-1-2-转义字符" class="headerlink" title="1.2.1.2 转义字符"></a>1.2.1.2 转义字符</h4><ul>
<li>转义字符是一种特殊的字符：无法“看见”的字符；与语言本身有冲突的字符</li>
<li><code>\n</code>：换行符</li>
<li><code>\\</code>：<code>\</code>字符</li>
<li><code>\&#39;</code>：单引号</li>
<li><code>\t</code>：制表符</li>
<li><code>\r</code>：回车字符</li>
</ul>
<h4 id="1-2-1-3-字符串的运算"><a href="#1-2-1-3-字符串的运算" class="headerlink" title="1.2.1.3 字符串的运算"></a>1.2.1.3 字符串的运算</h4><p>相比C，python的字符串运算实在是太舒服了</p>
<ul>
<li><p>合并字符串：<code>+</code></p>
</li>
<li><p>重复：<code>*</code>，内容重复，但是结果是一个字符串，不是三个</p>
</li>
<li><p>截取字符：<code>[]</code>，下标，从0开始编号，输入负数就是从后面开始选取，注意负数就是从-1开始的，不是-0</p>
</li>
<li><p>截取片段：<code>[m:n]</code>，截取从m到n-1个字符，左闭右开，可结合前面讲的负数表示，如果要表示最后一位，n就要是最后一位的后一位，但是这是不存在的，但是python可以执行，且写最后一位的后x位都是可以的，即输入的数字大于整体长度，是会按照最大的来取的，并不算错误（C语言这里就会崩掉）</p>
<p>如果有缺省<code>[:3]</code>,就是从0位到3位，<code>[3:]</code>就是从3位到最后一位，都缺省就是整个字符串</p>
</li>
</ul>
<h3 id="1-2-2-列表list"><a href="#1-2-2-列表list" class="headerlink" title="1.2.2 列表list"></a>1.2.2 列表list</h3><ul>
<li><p>定义：<code>[1,2,3,4,5,6]</code>，用<code>,</code>隔开元素，最后再加一个逗号也不会报错，最后一个是空的，是不存在的，不能被访问</p>
</li>
<li><p>列表里的元素是可以加入任意元素的，也不要求同一列表内的元素类型相同，还可以嵌套列表（二维数组）</p>
</li>
<li><p>也还是使用<code>[]</code>来取列表中的元素，使用方法大致是一样的，区别在于当使用<code>[:]</code>时，得到的是一个列表，而不是其中元素的类型</p>
</li>
<li><p><code>+</code>：合并列表</p>
</li>
<li><p><code>*</code>：重复，得到<strong>一个</strong> 元素重复几次的列表</p>
</li>
</ul>
<h3 id="1-2-3-元组tuple"><a href="#1-2-3-元组tuple" class="headerlink" title="1.2.3 元组tuple"></a>1.2.3 元组tuple</h3><ul>
<li>定义：<code>(1,2,3,4,5,6)</code>，当内部只有一个元素时，也要用小括号隔开，否则会有歧义，空的不需要加逗号，一个空括号就好了</li>
<li>元组的内部元素也不要求是同一种元素，访问方式和列表也是类似的</li>
<li><code>+</code> 、<code>*</code>：这个和列表也是一样</li>
<li>元组不可变，列表可变</li>
</ul>
<h3 id="1-2-4-序列的一些共有特点"><a href="#1-2-4-序列的一些共有特点" class="headerlink" title="1.2.4 序列的一些共有特点"></a>1.2.4 序列的一些共有特点</h3><ul>
<li><p>每个元素都有自己的编号</p>
</li>
<li><p>切片：<code>[m:n:l]</code>，m是起始，n是终止后一位，l是步长</p>
</li>
<li><p><code>+</code> <code>*</code></p>
</li>
<li><p><code>in</code>：是否在这个序列中，返回值是布尔类型，<code>not</code>加上之后就是判断是否不在</p>
</li>
<li><p><code>len</code>：长度统计函数 <code>len[1,2]</code></p>
</li>
<li><p><code>max</code>：最大值 <code>max([1,2,3])</code>；字符串也可以，比较ASCII编码；同理也有最小值</p>
<blockquote>
<p>有意思的是，<strong>序列之间也是可以比较大小</strong>的，就和数字比大小一样，一位一位的比，是有顺序的</p>
</blockquote>
</li>
<li><p><code>ord()</code>：显示字符的ASCII码</p>
</li>
<li><p>使用<code>list</code>、<code>tuple</code>应该可实现相互转换，但是不能转换数字，<code>str</code>可以转换数字</p>
</li>
</ul>
<h2 id="1-3-集合set"><a href="#1-3-集合set" class="headerlink" title="1.3 集合set"></a>1.3 集合set</h2><ul>
<li>集合是无序的：由于是无序的，所以没有索引和切片</li>
<li>定义：<code>{1,2,3,4,5}</code>或者<code>set(&#39;12345&#39;)</code></li>
<li>无重复元素：互异性</li>
<li>支持<code>len</code>和<code>in</code></li>
<li>可以做“减法”：<code>-</code>，求两个集合的差</li>
<li><code>&amp;</code>：做交集</li>
<li><code>|</code>：做并集，由此可以注意到集合是没有<code>+</code>拼接的</li>
<li><code>^</code>：异或，不公共的元素</li>
<li>空集合：<code>set()</code>就是定义一个空集合；不是<code>{}</code>，这个是个字典</li>
</ul>
<h2 id="1-4-字典dict"><a href="#1-4-字典dict" class="headerlink" title="1.4 字典dict"></a>1.4 字典dict</h2><ul>
<li><p>key value键值对应</p>
<blockquote>
<p>有点像C语言的enum</p>
</blockquote>
</li>
<li><p><code>{key1:value1,key2:value2...}</code></p>
</li>
<li><p><code>{&#39;Q&#39;:&quot;新月打击&quot;,‘W&#39;:&quot;苍白之瀑“}</code></p>
</li>
<li><p>字典也是无序的</p>
</li>
<li><p>访问方式：<code>[key]</code>，<code>{&#39;Q&#39;:&quot;新月打击&quot;,‘W&#39;:&quot;苍白之瀑“}[&#39;Q&#39;]</code>就访问了“新月打击”，一个字典中是不能有相同的key值的，相同的会被再次赋值，最后只剩一个</p>
</li>
<li><p>value：可以使用任意类型，甚至是一个dict；key：必须是不可变的类型，比如int、float、str，但是list是不行的，tuple是不可变的</p>
</li>
<li><p>空字典：<code>{}</code></p>
</li>
</ul>
<h1 id="2-运算符"><a href="#2-运算符" class="headerlink" title="2. 运算符"></a>2. 运算符</h1><h2 id="2-1-变量"><a href="#2-1-变量" class="headerlink" title="2.1 变量"></a>2.1 变量</h2><ul>
<li><p>定义：直接定义就好，<code>=</code>赋值，不用特别指明类型</p>
</li>
<li><p>命名规则</p>
</li>
<li><p>保留关键字</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> keyword</span><br><span class="line">keyword.kwlist</span><br></pre></td></tr></table></figure>
</li>
<li><p>函数名可以定义成变量，但是这个函数就不能再用了，这就是错误的，所以千万不要用常用函数名来做变量名，如果不慎这么干了，可以用<code>del</code>关键字删除变量</p>
</li>
<li><pre><code class="python">a = <span class="number">1</span> <span class="comment">#a得到一个地址</span>
b = a <span class="comment">#b和a得到相同地址</span>
a = <span class="number">3</span> <span class="comment">#a的地址改变，b不改变</span>
print(b)    <span class="comment">#得到b = 1</span>

a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>] <span class="comment">#a得到一个地址</span>
b = a    <span class="comment">#b得到和a一样的地址</span>
a[<span class="number">0</span>] = <span class="string">'1'</span>    <span class="comment">#a和b的地址都不改变，在原先基础上上变</span>
print(b)    <span class="comment">#得到b = ['1',2,3]</span>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">  为什么会出现这种情况：</span><br><span class="line"></span><br><span class="line">  值类型：int、float、complex、bool、str、tuple，因为它们是**不可变**的，想要改变时会生成一个新的，开辟了一个新的地址，原地址的没有被改变</span><br><span class="line"></span><br><span class="line">  引用类型：list、dict、set，因为它们是**可变**的，在原来的基础上改变</span><br><span class="line"></span><br><span class="line">  &gt; 可使用&#96;print(id(a))&#96;来查看地址，就可以知道修改了什么</span><br><span class="line"></span><br><span class="line">- &#96;append&#96;：追加元素，列表可以用，元组不可用，因为元组不可改变</span><br><span class="line"></span><br><span class="line">- 注意：元组里的嵌套列表是可以改变的</span><br><span class="line"></span><br><span class="line">## 2.2 运算符</span><br><span class="line"></span><br><span class="line">- 算术运算符：&#96;**&#96;</span><br><span class="line"></span><br><span class="line">- 赋值运算符：python是没有自增、自减运算符的，赋值运算符的优先级很低</span><br><span class="line"></span><br><span class="line">- 关系运算符：返回值时bool</span><br><span class="line"></span><br><span class="line">- 逻辑运算符：&#96;and&#96; &#96;or&#96; &#96;not&#96;，操作数是bool，返回值也是bool，&#96;not&#96;高于&#96;or&#96;高于&#96;and&#96;</span><br><span class="line"></span><br><span class="line">  &gt; 空字符串、列表、元组、集合、字典是False，有值就是True</span><br><span class="line"></span><br><span class="line">  &gt; and和or的返回规律</span><br><span class="line">  &gt;</span><br><span class="line">  &gt; 返回最后一个需要使用的操作数</span><br><span class="line">  &gt;</span><br><span class="line">  &gt; &#96;&#96;&#96;python</span><br><span class="line">  &gt; 1 and 2 #返回2 因为读到1，还不能确定，再看一个，所以返回2</span><br><span class="line">  &gt; 0 and 1 #返回0 因为读到0就gg了</span><br><span class="line">  &gt; 1 or 2 #返回1，因为读到1就是真了，不用看</span><br><span class="line">  &gt; 0 or 2 #返回2，因为读到0还不能确定，再看一个2，所以返回2</span><br><span class="line">  &gt;</span><br></pre></td></tr></table></figure></code></pre>
</li>
<li><p>位运算符：<code>&amp;</code> <code>|</code> <code>^</code> <code>~</code> <code>&gt;&gt;</code> <code>&lt;&lt;</code> </p>
</li>
<li><p>成员运算符：<code>in</code> <code>not in</code>：在或不在，返回bool</p>
<blockquote>
<p>字典成员运算符：</p>
<p>字典的成员运算是查找key的，不是value</p>
</blockquote>
</li>
<li><p>身份运算符</p>
<p><code>is</code> <code>is not</code> ：返回bool</p>
<p>针对对象而言的，比较两个变量的身份是否相同（简单理解成内存地址id是否相同）（你的面包不是我的面包）</p>
<p>对象的三个特征：值、身份、类型</p>
<blockquote>
<p><code>isinstance(对象,类型)</code>可以查看是否是相应类型，类型可以使用元组表示多种对象</p>
<p>那为什么不用type判断呢？因为它不能判断子类型  </p>
</blockquote>
</li>
</ul>
<h1 id="3-表达式"><a href="#3-表达式" class="headerlink" title="3.表达式"></a>3.表达式</h1><ul>
<li>python语句末尾不强制加分号，但是可以加</li>
<li>python靠缩进区分代码和代码块，因此对代码的格式要求十分严格，python就不可以混淆压缩，所以做python开源性很好</li>
<li>可以证明：条件、循环、分支就可以完成程序功能了</li>
</ul>
<h2 id="3-1-条件控制"><a href="#3-1-条件控制" class="headerlink" title="3.1 条件控制"></a>3.1 条件控制</h2><ul>
<li><p><code>if</code> <code>else</code></p>
</li>
<li><p>后面用<code>:</code>表示进入条件控制语句</p>
</li>
<li><p>多级嵌套</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> condition:</span><br><span class="line">	<span class="keyword">pass</span></span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">	<span class="keyword">if</span> condition:</span><br><span class="line">		<span class="keyword">pass</span></span><br><span class="line">	<span class="keyword">else</span>:</span><br><span class="line">		<span class="keyword">pass</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>elif</code>：这个关键字可简化代码嵌套级别</p>
</li>
<li><p><code>if</code>可以单独使用</p>
</li>
<li><p><code>pass</code>是空语句，为了保持代码的完整性，在构造基本结构时经常使用</p>
</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = <span class="number">4</span></span><br><span class="line">b = <span class="number">3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(a&gt;b):</span><br><span class="line">	print(<span class="string">"a&gt;b"</span>)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">	print(<span class="string">"a&lt;b"</span>)</span><br></pre></td></tr></table></figure>

<h2 id="3-2-循环"><a href="#3-2-循环" class="headerlink" title="3.2 循环"></a>3.2 循环</h2><ul>
<li><p><code>while</code> <code>for</code></p>
</li>
<li><pre><code class="python"><span class="keyword">while</span> condition:
    <span class="keyword">pass</span>

<span class="comment">#还可以结合else使用</span>
<span class="keyword">while</span> condition:
    <span class="keyword">pass</span>
<span class="keyword">else</span>:
    <span class="keyword">pass</span>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">- 递归时&#96;while&#96;比较常用</span><br><span class="line"></span><br><span class="line">- &#96;&#96;&#96;python</span><br><span class="line">  for target_list in expression_list:</span><br><span class="line">      pass</span><br></pre></td></tr></table></figure></code></pre>
</li>
<li><p><code>for</code>循环用来遍历/循环 序列或者集合、字典</p>
</li>
<li><p><code>else</code>也可以结合使用，遍历完了之后执行这个语句，这个用的应该也不多</p>
</li>
<li><p>使用<code>break</code>语句跳出当前循环，连后面的<code>else</code>语句都不执行了，因为跳出了整个循环语句</p>
</li>
<li><p>使用<code>continue</code>可以跳过此次循环</p>
</li>
<li><p><code>for(i=0;i&lt;10;i++)</code>的python写法：<code>range(起始,终止数字(不包括）,步长)</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> <span class="keyword">in</span> range(<span class="number">0</span>,<span class="number">10</span>):</span><br><span class="line">	<span class="keyword">pass</span></span><br></pre></td></tr></table></figure>




</li>
</ul>
<h1 id="4-包、模块"><a href="#4-包、模块" class="headerlink" title="4. 包、模块"></a>4. 包、模块</h1><ul>
<li><p>包理解为文件夹，模块就理解为文件夹中的文件，在文件中写一个一个的类会比较好，类里面有函数和变量</p>
</li>
<li><p>一般不要在模块中写业务代码，不是一个很好的习惯，一般写在类里面</p>
</li>
<li><p>命名：包的名字就是文件夹名字，不同包里可以有相同的模块名称<code>top.module1</code> ，然后模块的名字就是文件的名字。</p>
<blockquote>
<p>包所在的文件夹下要有_<em>init_\</em>.py模块才能成为包，不然不是包，这个文件本身也是一个模块，里面可以不放置内容，只是用来标志这是一个包，这个</p>
<p>命名空间：指定是在哪个包里面的模块。包里面还会有子包。比如包名是seven，里面一个模块是module,可以使用seven.module来表示使用的是这个包。但是<em>init\</em>.py是一个特殊的模块，它的名字就是包的名字seven</p>
</blockquote>
</li>
<li><p>import导入模块，不能够导入模块中的变量</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> module_name</span><br><span class="line"><span class="comment">#使用时要使用 . </span></span><br><span class="line"></span><br><span class="line"><span class="comment">#下面是module1文件的内容</span></span><br><span class="line">a = <span class="number">4</span></span><br><span class="line">print(a)</span><br><span class="line"></span><br><span class="line"><span class="comment">#下面是module2文件中的内容，引入一定要按照命名空间的原则，即路径要明确</span></span><br><span class="line"><span class="keyword">import</span> module1 </span><br><span class="line">print(module2.a)</span><br></pre></td></tr></table></figure>

<p>引入之后会自动生成一个_pycache_文件夹，里面有一个.pyc文件，这是python生成的自解码文件，基础运用时不需要做过多了解</p>
<blockquote>
<p>vscode技巧：</p>
<p>在设置中的 files exclude中可以设置隐藏一些不必要的文件</p>
</blockquote>
<p>但是每次引用模块中的变量时都需要写一遍模块名，很麻烦，这里可以使用as关键字解决这个问题</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> module1 <span class="keyword">as</span> m</span><br><span class="line">print(m.a)</span><br></pre></td></tr></table></figure>
</li>
<li><p>from关键字导入模块内变量、函数、模块，这种方式不需要命名空间</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#上面的代码可以使用下面的方式实现</span></span><br><span class="line"><span class="keyword">from</span> module1 <span class="keyword">import</span> a</span><br><span class="line">print(a)</span><br><span class="line"><span class="comment">#这个方式可以导入函数</span></span><br><span class="line"><span class="keyword">from</span> module_name <span class="keyword">import</span> var <span class="function"><span class="keyword">def</span>...</span></span><br><span class="line"><span class="function">#使用 <span class="title">from</span> <span class="title">module</span> <span class="title">import</span> * 可以引入全部的变量</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>在模块中使用内置属性 <em>all\</em> = [‘a’,’c’] 可以在使用*导入时只导入a和c两个变量，而不是全部的变量</p>
</blockquote>
</li>
<li><p>如何一次导入多个变量，使用<code>,</code>分隔，如果太多怎么换行，在行尾加上一个<code>\</code>，但是很难看，可以使用<code>()</code>来在变量列表中换行，在导入的变量两边加上括号</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> module <span class="keyword">import</span>(a,b,c,d,</span><br><span class="line">                  e,f,g)</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> module <span class="keyword">import</span> a,b,c,d,\</span><br><span class="line">					e,f,g</span><br><span class="line">    </span><br><span class="line"><span class="keyword">from</span> module <span class="keyword">import</span> a,b,c,d,e,f,g</span><br></pre></td></tr></table></figure>
</li>
<li><p>import一个包的时候，下面的<code>__init__.py</code>文件会被python自动执行，不需要手动执行，所以经常在这个文件中做一些初始化的事情。使用from导入变量的时候，也会有这么一个过程；导入一个模块的时候也会有相同的结果</p>
</li>
<li><p><code>__init__.py</code>文件中的一些注意点</p>
<ul>
<li><code>__all__ = [&#39;module1&#39;]</code>可以选择导入保重的哪些模块</li>
<li>批量导入: 如果一系列的模块都需要引用一些相同的库，可以将引用的模块语句(import语句)写在<code>__init__.py</code>文件中，这样只需要导入这个文件对应的一个包就好了，但是在使用的时候需要注意命名空间的问题</li>
</ul>
</li>
<li><p>包和模块是不会重复导入的，导入了一次之后就不会再导入了</p>
</li>
<li><p>不要循环导入，循环的引用是不好的设计方式，尽量不要这样设计，使用其他方式避免这种引用</p>
</li>
</ul>
<h1 id="5-函数"><a href="#5-函数" class="headerlink" title="5.函数"></a>5.函数</h1><ul>
<li><p><code>round()</code>，可以接受两个参数，第一个是数字，第二个是精确到的位数，缺省第二个表示0</p>
</li>
<li><p><code>help()</code>，在命令行中查看内置函数的作用</p>
</li>
<li><p>定义</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">funcname</span><span class="params">(parameter_list)</span>:</span></span><br><span class="line">	<span class="keyword">pass</span></span><br></pre></td></tr></table></figure>

<ul>
<li>参数列表可以没有</li>
<li><code>return</code>返回，无就是None</li>
<li>函数名称不要使用python内置的函数名称，否则会出现莫名其妙的情况</li>
</ul>
</li>
<li><p>设置最大递归深度</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys</span><br><span class="line">sys.setrecursionlimit(<span class="number">1000000</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p>如何让函数返回多个结果</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">damage</span><span class="params">(skill1,skill2)</span>:</span></span><br><span class="line">    damage1 = skill1*<span class="number">3</span></span><br><span class="line">    damage2 = skill2*<span class="number">2</span></span><br><span class="line">    <span class="keyword">return</span> damage1,damage2</span><br><span class="line">damages = damage(<span class="number">3</span>,<span class="number">6</span>)</span><br></pre></td></tr></table></figure>

<p>输出的数据damage是元组(tuple)类型，元组可以使用[]这种序号的方式访问，但是这种方式并不是什么好的方式</p>
<p>可以使用下面的方式，序列解包的方式，这种方式使变量名有意义</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">damage_skill1,damage_skill2 = damage(<span class="number">3</span>,<span class="number">6</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p>序列解包</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a,b,c = <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span></span><br><span class="line"><span class="comment">#等价于下面的三句话</span></span><br><span class="line">a = <span class="number">1</span></span><br><span class="line">b = <span class="number">2</span></span><br><span class="line">c = <span class="number">3</span></span><br><span class="line"></span><br><span class="line">d = <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span> <span class="comment">#d是一个元组</span></span><br><span class="line">a,b,c = d <span class="comment">#这和上面的例子的结果一样</span></span><br><span class="line">a,b,c = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>] <span class="comment">#这样也可以</span></span><br><span class="line">a,b,c = (<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>) <span class="comment">#感觉元组括号似乎可以省？</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>参数</p>
<ul>
<li><p>必须参数: 调用的时候需要给它赋值</p>
</li>
<li><p>关键字参数: 调用的时候指定变量值</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#必须参数</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(x,y)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> x+y </span><br><span class="line"><span class="comment">#关键字参数</span></span><br><span class="line">add(y=<span class="number">3</span>, x=<span class="number">2</span>) <span class="comment">#方便，可读性</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>默认参数（缺省值): 必须参数要放在前面，如果顺序不对，使用关键字参数的方式传递</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(x,y=<span class="number">0</span>,z=<span class="number">0</span>)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> x+y+z</span><br><span class="line"></span><br><span class="line">add(<span class="number">1</span>,<span class="number">2</span>) <span class="comment">#x=1,y=2,z=0</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h1 id="6-面向对象"><a href="#6-面向对象" class="headerlink" title="6.面向对象"></a>6.面向对象</h1><ul>
<li><p>类：封装代码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span><span class="params">()</span>:</span></span><br><span class="line">    name = <span class="string">''</span></span><br><span class="line">    age = <span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">print_file</span><span class="params">(self)</span>:</span>    <span class="comment">#这个self是不可以少的，不接受参数也要加这个参数,当然可以叫做其他名字，并不一定是self</span></span><br><span class="line">        print(<span class="string">'name:'</span> +self.name)</span><br><span class="line">        print(<span class="string">'age:'</span> + str(self.age))</span><br></pre></td></tr></table></figure>
</li>
<li><p>实例化</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">student = Student()</span><br><span class="line">student.print_file()</span><br></pre></td></tr></table></figure>
</li>
<li><p>做实际的项目的时候，将类的使用放在其他的文件模块中，定义和实例化在不同的模块中（类也可以使用from引入)</p>
</li>
<li><p>设计面向对象时，要学会找行为和执行行为的主体，以及类的特征，类就像是一个模板，通过这个模板可以产生许多个对象</p>
</li>
<li><p>构造函数</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span><span class="params">()</span>:</span></span><br><span class="line">    s_sum = <span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,name,age)</span>:</span>	<span class="comment">#自动运行，但是也可以使用student._init_()调用,只可返回None</span></span><br><span class="line">        self.name = name</span><br><span class="line">        self.age = age</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">do_homework</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"homework"</span>)</span><br><span class="line">        </span><br><span class="line">student1 = Student(<span class="string">"张三"</span>,<span class="number">18</span>)</span><br><span class="line">print(student.name) <span class="comment">#实例变量</span></span><br><span class="line">print(Student.name)	<span class="comment">#类变量</span></span><br></pre></td></tr></table></figure>

<ul>
<li>构造函数可以用来生成不同的对象</li>
</ul>
</li>
<li><p>类变量和实例变量</p>
<ul>
<li>类变量和类相关，实例变量和对象相关，上面的例子中name和age是类变量，self.name和self.age是实例变量</li>
<li>访问的实例变量不存在时，就去类变量中查找</li>
</ul>
</li>
<li><p><code>_dict_</code>变量，对象下的所有变量字典，可以做验证使用</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(student1.__dict__)</span><br></pre></td></tr></table></figure>

<p>类的<code>_dict_</code>变量里有很多变量，也可以使用上诉方法查看</p>
</li>
<li><p><code>self</code></p>
<ul>
<li>实例方法中参数列表中要放入<code>self</code>，调用时不需要传入这个变量</li>
<li><code>self</code>是指当前调用的某个方法的对象，代表的是实例，而不是类</li>
<li><code>self</code>是不能隐藏的，C++中的<code>this</code>就不需要指明，需要区分</li>
</ul>
</li>
<li><p>如何在实例方法中访问类变量，也可操作类变量</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span><span class="params">()</span>:</span></span><br><span class="line">    s_sum = <span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,name,age)</span>:</span></span><br><span class="line">        self.name = name</span><br><span class="line">        self.age = age</span><br><span class="line">        print(Student.s_sum)<span class="comment">#访问类变量方法1</span></span><br><span class="line">        print(self._class_.s_sum) <span class="comment">#方法2</span></span><br><span class="line">        self.__class__.s_sum += <span class="number">1</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">do_homework</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"homework"</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p>类方法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@classmethod</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">plus_sum</span><span class="params">(cls)</span>:</span> <span class="comment">#cls就像那个self，也不是唯一的，可以改名字，实例方法和类方法判断是上面是否有@classmethod</span></span><br><span class="line">    cls.s_sum += <span class="number">1</span></span><br><span class="line">    </span><br><span class="line">Student.plus_sum()</span><br><span class="line">student1 = Student(<span class="string">"zhangsan"</span>,<span class="number">19</span>)</span><br><span class="line">student1.plus_sum() <span class="comment">#对象调用也是可以的，只不过这在逻辑上说不通</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>静态方法: 可以使用类方法代替，建议不要经常使用，感觉和类联系不大</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@staticmethod</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(x,y)</span>:</span></span><br><span class="line">    print(x+y)</span><br></pre></td></tr></table></figure>

<ul>
<li>不需要传入<code>self</code>这种参数</li>
</ul>
<blockquote>
<p>Q:类方法和静态方法是否可以访问实例变量?</p>
<p>A:不可以，实例需要实例化才有的，实例变量不一定有的</p>
</blockquote>
</li>
<li><p>公开和私有成员(变量和方法) </p>
<p><code>public</code>  <code>private</code></p>
<p>上面提到的都是不安全的，外部不应该可以随意访问内部变量</p>
<p>使用类里面的方法来修改对象内的变量</p>
<p>默认是<code>public</code>的</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span><span class="params">()</span>:</span></span><br><span class="line">    s_sum = <span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,name,age,score)</span>:</span></span><br><span class="line">        self.name = name</span><br><span class="line">        self.age = age</span><br><span class="line">       	self.__score = <span class="number">0</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">do_homework</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"homework"</span>)</span><br><span class="line"><span class="meta">    @classmethod</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">plus_sum</span><span class="params">(cls)</span>:</span></span><br><span class="line">        cls.plus_sum += <span class="number">1</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">marking</span><span class="params">(self,score)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> score&lt;<span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="string">'不要打负分'</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">        	self.score = score</span><br></pre></td></tr></table></figure>

<p>在变量前面加<code>__</code>表示是私有的，但是前后都有的话就不是了，比如构造函数</p>
<p>但是再调用时会发现还是可以在外部调用，因为因为找不到又自动声明了一个同名变量</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">student1 = Student(<span class="string">'zhangsan'</span>,<span class="number">19</span>,<span class="number">34</span>)</span><br><span class="line">student1.__score = <span class="number">23</span> <span class="comment">#声明了一个变量</span></span><br><span class="line">print(student.__socre) <span class="comment">#访问的是内部的变量</span></span><br></pre></td></tr></table></figure>

<p>保护的机制很简单，就是改个名字</p>
<p>上面的私有变量会被自动更名，然后可以访问</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(student1._Student__score)</span><br></pre></td></tr></table></figure>
</li>
<li><p>继承性</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span><span class="params">()</span>:</span></span><br><span class="line">    sum = <span class="number">0</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,name,age)</span>:</span></span><br><span class="line">        self.name = name</span><br><span class="line">        self,age = age</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> file <span class="keyword">import</span> People</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span><span class="params">(People)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,school,name,age)</span>:</span></span><br><span class="line">        self.school = school</span><br><span class="line">        People.__init__(self,name,age)<span class="comment">#这里需要传入self</span></span><br></pre></td></tr></table></figure>

<p>为什么需要传入self?</p>
<p>实例化时调用构造函数是自动的，但是上面代码中就是一个普通的调用，但是这里是一个类调用，不是实例调用，所以需要使用self</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> file <span class="keyword">import</span> People</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span><span class="params">(People)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,school,name,age)</span>:</span></span><br><span class="line">        self.school = school</span><br><span class="line">        super(Student,self).__init__(name,age) <span class="comment">#super()找父类</span></span><br></pre></td></tr></table></figure>

<p>同名方法优先调用子类方法</p>
</li>
</ul>
<h1 id="7-正则表达式"><a href="#7-正则表达式" class="headerlink" title="7. 正则表达式"></a>7. 正则表达式</h1>]]></content>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>线性表</title>
    <url>/2020/02/13/%E7%BA%BF%E6%80%A7%E8%A1%A8/</url>
    <content><![CDATA[<p>最简单的一种数据结构</p>
<a id="more"></a>

<h1 id="1-线性表"><a href="#1-线性表" class="headerlink" title="1. 线性表"></a>1. 线性表</h1><ul>
<li><p>数据对象：n个元素构成的有序序列</p>
</li>
<li><p>操作集</p>
<ul>
<li><code>list init_list()</code></li>
<li><code>element_type find_kth(list L, int k)</code></li>
<li><code>int find(element_type x, list L)</code></li>
<li><code>void insert(element_type x, int i, list L)</code></li>
<li><code>void delete(int i, list L)</code></li>
<li><code>int length(list L)</code></li>
</ul>
</li>
<li><p>存储方式</p>
<ul>
<li>数组</li>
<li>链表</li>
</ul>
</li>
</ul>
<h1 id="2-实现方式"><a href="#2-实现方式" class="headerlink" title="2. 实现方式"></a>2. 实现方式</h1><h2 id="2-1-顺序存储"><a href="#2-1-顺序存储" class="headerlink" title="2.1 顺序存储"></a>2.1 顺序存储</h2><ul>
<li><p>定义</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">LNode</span>&#123;</span></span><br><span class="line">    element_type data[MAX];</span><br><span class="line">	<span class="keyword">int</span> last;</span><br><span class="line">&#125;<span class="built_in">list</span>, *Plist;</span><br></pre></td></tr></table></figure>
</li>
<li><p>创建</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">plist</span><br><span class="line">    init_list()</span><br><span class="line">&#123;</span><br><span class="line">    plist p_list;</span><br><span class="line">    p_list = (plist)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct LNode));</span><br><span class="line">    p_list-&gt;last = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">return</span> p-<span class="built_in">list</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>查找</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">find</span><span class="params">(element_type x, plist p_list)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(i &lt;= p_list-&gt;last &amp;&amp; p_list-&gt;data[i]!=x)</span><br><span class="line">        i++;</span><br><span class="line">   <span class="keyword">if</span>(i &gt; p_list-&gt;last)</span><br><span class="line">       <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> i;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>插入</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(element_type x, plist p_list, <span class="keyword">int</span> i)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> j;</span><br><span class="line">    <span class="keyword">if</span>(p_list-&gt;last == MAX - <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>(i&lt;<span class="number">1</span> || i&gt;p_list-&gt;last+<span class="number">1</span>)<span class="comment">//落在1和n+1之间，last是表长度-1</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">for</span>(j = p_list-&gt;last;j&gt;=i<span class="number">-1</span>;j--)<span class="comment">//将从下标为1的开始全部后移一位</span></span><br><span class="line">        p_list-&gt;data[j+<span class="number">1</span>] = p_list-&gt;data[j];</span><br><span class="line">    p_list-&gt;data[i<span class="number">-1</span>] = x;</span><br><span class="line">    p_list-&gt;last++;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>删除</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">delete</span><span class="params">(<span class="keyword">int</span> i, plist p_list)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> j;</span><br><span class="line">    <span class="keyword">if</span>(i&lt;<span class="number">1</span>||i&gt;p_list-&gt;last+<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">for</span>(j=i; j&lt;=p_list-&gt;last; j++)</span><br><span class="line">        p_list-&gt;data[j<span class="number">-1</span>] = p_list-&gt;data[j];</span><br><span class="line">    p_list-&gt;last--;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="2-2-链式存储"><a href="#2-2-链式存储" class="headerlink" title="2.2 链式存储"></a>2.2 链式存储</h2><ul>
<li><p>定义</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	element_type data;</span><br><span class="line">	LNode* next;</span><br><span class="line">&#125;*plist;</span><br></pre></td></tr></table></figure>
</li>
<li><p>求表长</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">length</span><span class="params">(plist p_list)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    plist p = p_list;</span><br><span class="line">    <span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(p)</span><br><span class="line">    &#123;</span><br><span class="line">       p = p-&gt;next;</span><br><span class="line">       j++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> j;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>查找</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="built_in">list</span> <span class="title">find_kth</span><span class="params">(plist p_list)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    plist p;</span><br><span class="line">    p = p_list;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(p != <span class="literal">NULL</span> &amp;&amp; i &lt; k)</span><br><span class="line">    &#123;   p = p-&gt;next;</span><br><span class="line">    	i++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(i == k) <span class="keyword">return</span> p;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="built_in">list</span> <span class="title">find</span><span class="params">(element_type x, plist p_list)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    plist p;</span><br><span class="line">    p = p_list;</span><br><span class="line">    <span class="keyword">while</span>(p!=NUll&amp;&amp;p-&gt;data != x)</span><br><span class="line">        p = p-&gt;next;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>插入</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">plist <span class="title">insert</span><span class="params">(element_type x,<span class="keyword">int</span> i, plist p_list)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   	plist p, s;</span><br><span class="line">    <span class="keyword">if</span>(i == <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        s = (plist) <span class="built_in">malloc</span> (<span class="keyword">sizeof</span>(struct LNode));</span><br><span class="line">        s-&gt;data = x;</span><br><span class="line">        s-&gt;next = p_list;</span><br><span class="line">        <span class="keyword">return</span> s;</span><br><span class="line">    &#125;</span><br><span class="line">    p = find_kth(i<span class="number">-1</span>, p_list);</span><br><span class="line">    <span class="keyword">if</span>(p==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        s = (plist)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct LNode));</span><br><span class="line">        s-&gt;data = x;</span><br><span class="line">        s-&gt;next = p-&gt;next;</span><br><span class="line">        p-&gt;next = s;</span><br><span class="line">        <span class="keyword">return</span> p_list;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>删除</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">plist <span class="title">delete</span><span class="params">(<span class="keyword">int</span> i; plist p_list)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    plist p ,s;</span><br><span class="line">    <span class="keyword">if</span>(i==<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        s = p_list;</span><br><span class="line">        <span class="keyword">if</span>(!p_list)</span><br><span class="line">            p_lsit = p_list-&gt;next;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">        <span class="built_in">free</span>(s);</span><br><span class="line">        <span class="keyword">return</span> p_list;</span><br><span class="line">    &#125;</span><br><span class="line">    p = find_kth(i<span class="number">-1</span>, p_list);</span><br><span class="line">    <span class="keyword">if</span>(p == <span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(p-&gt;next == <span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        s = p-&gt;next;</span><br><span class="line">        p-&gt;next = s-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(s);</span><br><span class="line">        <span class="keyword">return</span> p_list;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>







</li>
</ul>
]]></content>
      <tags>
        <tag>DS</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式</title>
    <url>/2020/01/16/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<p>正则表达式的应用十分广泛</p>
<a id="more"></a>

<h1 id="1-Linux文本处理和正则表达式"><a href="#1-Linux文本处理和正则表达式" class="headerlink" title="1. Linux文本处理和正则表达式"></a>1. Linux文本处理和正则表达式</h1><h2 id="1-1-grep"><a href="#1-1-grep" class="headerlink" title="1.1 grep"></a>1.1 grep</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep [options] pattern [file...]</span><br></pre></td></tr></table></figure>

<ul>
<li>最简单的用法就是<code>grep text file</code>，就是在<code>file</code>中过滤出有<code>text</code>内容的行</li>
<li><code>-v</code>：不包括字符串的行</li>
<li><code>-i</code>：忽略大小写</li>
<li><code>-n</code>：匹配的行号</li>
<li><code>-c</code>：匹配的行数</li>
<li><code>-o</code>：只显示字符串，不显示其他内容</li>
<li><code>-q</code>：静默模式，不做任何显示，但是这个命令是有返回值的，可以判断是否含有字符串</li>
<li><code>-An</code>：显示A的后n行</li>
<li><code>-Bn</code>：显示B的前n行</li>
<li><code>-Cn</code>：显示C的前n行与后n行</li>
<li><code>-e</code>：搜索多个关键字，或的关系</li>
<li><code>-w</code>：匹配整个单词</li>
</ul>
<p>grep强大之处在于其支持正则表达式，下面介绍一下正则表达式</p>
<h2 id="1-2-正则表达式"><a href="#1-2-正则表达式" class="headerlink" title="1.2 正则表达式"></a>1.2 正则表达式</h2><p>用特定的表达式代替文本的一部分，有点类似通配符，但是和通配符有很大区别，用正则表达式最好加上双引号</p>
<p>很多地方都可以使用正则表达式，所以很有必要掌握</p>
<ul>
<li>使用<code>man 7 regex</code>可以查看正则表达式的用法</li>
<li>正则表达式中用来实现功能表达式的叫做元字符：字符匹配、匹配次数、位置锚定、分组</li>
<li>字符匹配<ul>
<li><code>.</code>：匹配单一字符，要表达<code>.</code>本身，使用转义<code>\</code>，中括号里的<code>.</code>就表示其本身，无需转义</li>
<li><code>[]</code>：匹配中括号内的任意一个字符，<code>^</code>取反，表示匹配中括号内不存在的字符</li>
<li><code>[:alnum:]</code>：字母或数字</li>
<li><code>[:alpha:]</code>：匹配任意大小写字母</li>
<li><code>[:lower:]</code>：匹配小写字母</li>
<li><code>[:upper:]</code>：匹配大写字母</li>
<li><code>[:blank:]</code>：匹配空白字符</li>
<li><code>[:space:]</code>：水平和垂直的空白字符，比<code>[:blank:]</code>的范围广</li>
<li><code>[:cntrl:]</code>：匹配不可打印的控制字符</li>
<li><code>[:digit:]</code>：匹配十进制数</li>
<li><code>[:xdigit:]</code>：匹配十六进制数</li>
<li><code>[:graph:]</code>：匹配为可打印的非空白字符</li>
<li><code>[:print:]</code>：匹配可打印字符</li>
<li><code>[:punct:]</code>：匹配标点符号</li>
</ul>
</li>
<li>匹配次数：用在要指定次数的字符后面，用于指定前面的字符要出现的次数<ul>
<li><code>*</code>：表示前面的字符任意次，包括0次</li>
<li><code>.*</code>：表示匹配任意长度的字符，表示任意字符的任意次</li>
<li><code>\?</code>：表示匹配前面的字符0或1次</li>
<li><code>\+</code>：表示匹配前面的字符至少1次</li>
<li><code>\{n\}</code>：表示匹配前面的字符n次</li>
<li><code>\{m,n\}</code>：表示匹配前面的字符串至少m次，至多n次，m，n可为缺省值</li>
</ul>
</li>
<li>位置锚定<ul>
<li><code>^</code>：行首锚定，用于模式的最左侧</li>
<li><code>$</code>：行尾锚定，用于模式的最右侧</li>
<li><code>^PATTERN$</code>：用于模式匹配整行<ul>
<li><code>^$</code>：空行</li>
<li><code>^[[:space:]]*$</code>：空白行</li>
</ul>
</li>
<li><code>\&lt;</code> or <code>\b</code>：词首锚定</li>
<li><code>\&gt;</code> or <code>\b</code>：词尾锚定</li>
<li><code>\&lt;PATTERN\&gt;</code>：匹配整个单词</li>
</ul>
</li>
<li>分组<ul>
<li><code>\(\)</code>：将一个或多个字符捆绑在一起，当做一个整体进行处理，如 <code>\(root\)\+</code></li>
<li>分组括号中的模式匹配到的内容会被正则表达式引擎记录于内部的变量中，这些变量的命名方式为<code>\1 \2 \3</code></li>
<li><code>\1</code>：表示从左侧起第一个左括号以及与之匹配右括号之间的模式所匹配到的字符<ul>
<li>举例：<code>\(ab\+\(cd\)\+\)</code>：<ul>
<li><code>\1</code>：<code>ab\+\(cd\)\+</code></li>
<li><code>\2</code>：<code>cd</code></li>
</ul>
</li>
</ul>
</li>
<li>后向引用：引用前面的分组括号中的模式所匹配字符，而非模式本身<ul>
<li>举例：当出现<code>\(r..t\)</code>时，第一次匹配到了<code>root</code>，使用<code>\1</code>匹配到的就是<code>root</code>了，而不是<code>r..t</code></li>
</ul>
</li>
<li><code>\|</code>：或者</li>
</ul>
</li>
</ul>
<h2 id="1-3-扩展的正则表达式"><a href="#1-3-扩展的正则表达式" class="headerlink" title="1.3 扩展的正则表达式"></a>1.3 扩展的正则表达式</h2><ul>
<li><code>egrep</code> = <code>grep -E</code></li>
<li><code>egrep [options] pattern [file...]</code></li>
<li>扩展的正则表达式和基本的正则表达式的区别基本上就是少<code>\</code>，写起来更加方便，具体使用看具体情况</li>
</ul>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>vim</tag>
      </tags>
  </entry>
  <entry>
    <title>循环不变式</title>
    <url>/2020/09/20/%E5%BE%AA%E7%8E%AF%E4%B8%8D%E5%8F%98%E5%BC%8F/</url>
    <content><![CDATA[<p>循环不变式式证明算法正确性的一个重要方法，本篇文章将记录一下循环不变式。</p>
<a id="more"></a>

<p>百科里给循环不变式的定义为“每次循环体执行前后均为真的谓词”。最为关键的是找到合适的“断言”。</p>
<p>要通过这个证明算法的正确性，需要证明三点：</p>
<ul>
<li>初始化：循环的第一次迭代开始前，断言为真。</li>
<li>保持：类似于数学归纳法的第二步，循环的某次迭代之前断言为真，那么循环迭代之后，断言仍为真。</li>
<li>终止：循环终止时，得到的结果就是自己想要的结果。</li>
</ul>
<p>下面通过一个常见的例子来说明循环不变式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> <span class="built_in">size</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(i&lt;<span class="built_in">size</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        sum = sum + a[i];</span><br><span class="line">        i = i + <span class="number">1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是一个十分简答的求和运算，不难得出“sum = 前k个数的和”，这便是断言，下面说明一下断言的正确性。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> <span class="built_in">size</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// M(k) = 0, 此时k = 0，所以初始化成立。</span></span><br><span class="line">    <span class="keyword">while</span>(i&lt;<span class="built_in">size</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        sum = sum + a[i];</span><br><span class="line">        <span class="comment">// M(k + 1) = 前 k + 1个数的和 = M(k) + a[k]</span></span><br><span class="line">        i = i + <span class="number">1</span>;</span><br><span class="line">        <span class="comment">//转换之后，M(k) = 前k个数的和</span></span><br><span class="line">	&#125;</span><br><span class="line">    <span class="keyword">return</span> sum; <span class="comment">//求完之后，k = size，即sum等于前size个数的和，确实符合自己想要的结果。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>能用循环不变式验证的算法还有很多的，以后慢慢体会。</p>
]]></content>
      <tags>
        <tag>Algorithm</tag>
      </tags>
  </entry>
</search>
